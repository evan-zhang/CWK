# -*- coding: utf-8 -*-
"""Wiki 编译 Prompt 与页面输出 Schema（简化版：无 claim、无权限）。"""
from __future__ import annotations

# 简化：不再抽取 Claim 证据组（无权限场景，claim 细粒度证据归属无意义）；
# 编译产物仅为 Markdown 页面正文 + summary 摘要。
COMPILE_SYSTEM_PROMPT = """你是一名严谨的 Wiki 编译引擎（COMPILER，而非创作者）。你只整理、归纳、抽取给定原文片段中的事实，绝不创作或补充原文没有的信息。

任务：把给定文档的若干原文片段编译成一个 Wiki 页面（Markdown 正文 + 一句话摘要）。

【编译规则】
1. 你只是编译器：逐字贴近原文，不润色、不发挥、不引入外部知识；页面用 Markdown 分节（## 标题 + 段落）。
2. 首行约定：输出的 summary 字段首行必须是 "SUMMARY: " 后接一句话摘要（供快速预览）。
3. 冲突处理（Conflict）：若片段之间对同一事实相互矛盾，在页面中用"冲突/待核"小节并列呈现，并各自标注来源；不要自行裁定，也不要用新结论覆盖旧结论。
4. 零幻觉：绝对只使用提供的原文片段。
5. 严格输出：必须输出合法 JSON（如下 Schema），不要任何解释、前言或代码围栏。

【输出 Schema】
{
  "title": "页面标题",
  "page_type": "summary",
  "summary": "SUMMARY: 一句话摘要",
  "markdown": "完整页面 Markdown（含冲突小节如有）"
}
"""

COMPILE_USER_TEMPLATE = """文档标题：{file_name}
文档版本：{version_id}

以下为文档原文片段（chunk_id 已标注），请只基于这些片段编译：
{chunks}

请按系统指令编译 Wiki 页面（注意冲突单列、零幻觉、SUMMARY 首行约定）。"""


def format_chunks(chunks: list) -> str:
    lines = []
    for c in chunks:
        lines.append(f"[chunk_id={c.get('chunk_id')}]\n{c.get('text', '')}")
    return "\n\n".join(lines)


# ---------------------------------------------------------------------------
# 摘要页生成（阶段 1：每篇文档先生成一页「摘要页」，源文件 = 该文档）
# ---------------------------------------------------------------------------
SUMMARY_PROMPT = (
    "你是知识库摘要助手。阅读整篇文档，仅输出 JSON：\n"
    '{"title": "简洁的摘要页标题（<=120 字）",\n'
    ' "summary": "一句话概述全文主旨（<=200 字）",\n'
    ' "markdown": "# 标题\\n\\n结构化摘要正文，列出核心要点（每条事实必须来自原文）"}\n'
    "严格要求：\n"
    "1. 仅使用文档中真实存在的内容，不得编造。\n"
    "2. markdown 中每条要点都应能在原文中找到出处（chunk 级）。\n"
    "3. 不生成引用其它文档的内容。"
)


def build_summary_user_content(doc_name, text):
    return "【文档】" + (doc_name or "") + "\n" + "【正文】\n" + (text or "") + "\n"


# ---------------------------------------------------------------------------
# 分类（阶段 5：taxonomy，仅用于文件夹导航，非安全边界）
# ---------------------------------------------------------------------------
TAXONOMY_PROMPT = (
    "你是知识库分类助手。给定一条 Wiki 页面（标题 + 正文摘要），"
    "判断它最适合归入哪个一级主题文件夹。仅输出 JSON：\n"
    '{"folder": "一个简短稳定的文件夹名（如 人物 / 概念 / 流程 / 事件 / 其他）"}\n'
    "要求：命名稳定，便于多篇文档聚合到同一文件夹；不要加标点。"
)


def build_taxonomy_user_content(title, summary):
    return f"【标题】{title}\n【摘要】{summary}\n"


# ---------------------------------------------------------------------------
# 批量分类（整批一次性规划，避免逐页发散）
# 一次调用返回 {assignments:[{slug, path}]}，按 chunk 切分，分块间 feed-forward。
# ---------------------------------------------------------------------------
TAXONOMY_PLAN_SYSTEM_PROMPT = """你是知识库导航目录的组织助手。把下面给出的每一个条目分配到一个目录路径（分类），使整批落在一棵一致的树上。

<existing_folders>
{existing}
</existing_folders>

<items>
{items}
</items>

<instructions>
为每个条目输出一个分类路径：从宽泛到具体的文件夹标签数组（最多 2 级）。分类依据条目「本质上是什么」（稳定的归类书架），而非它在某篇文档里的角色。

如何为每个条目选路径：
1. 若 <existing_folders> 里有合适的文件夹，复用其「完全一致」的标签（逐字一致），不要发明同义文件夹。
2. 若没有合适的，创建一个新的、宽泛且持久的文件夹（如 组织 -> 「组织」、法律概念 -> 「法律概念」、地点 -> 「地点」）。目录不必很小——大多数条目都有天然的归属，宁可新创一个合理的一级文件夹，也不要让它们无家可归。同类条目归到同一个文件夹。
3. 仅当某条目确实不属于任何持久主题时才给空路径 []（必须极少见）。没有匹配的现有文件夹不是给 [] 的理由，应创建文件夹。

其他规则：
- 同类条目放在同一级的同一文件夹下，不要把一个等价条目比它的兄弟更深一层。
- 优先单一宽泛的一级文件夹；只有真正持久的、被多个条目共享的子领域才加第二级。
- 不要使用条目类型（entity/concept）作为文件夹名；不要在单个标签里使用斜杠。
- <items> 中的每个 slug 必须在输出里恰好出现一次。

### JSON 格式规则
- 只输出合法 JSON，不要前言。
- JSON 字符串值内不要使用字面换行。
</instructions>

输出格式：
{
  "assignments": [
    {"slug": "entity/zhang-san", "path": ["人物"]},
    {"slug": "concept/spring-festival", "path": ["节日", "传统节日"]}
  ]
}"""


def build_taxonomy_plan_user_content(items: list, existing: str = "") -> str:
    """items: [(slug, title, summary), ...]"""
    lines = []
    for slug, title, summary in items:
        lines.append(f"- slug: {slug} | 标题: {title} | 摘要: {summary}")
    body = "\n".join(lines)
    return f"<existing_folders>\n{existing}\n</existing_folders>\n\n<items>\n{body}\n</items>"


# ---------------------------------------------------------------------------
# 实体 / 概念抽取（单次合并抽取）
# 仅在 MAP 阶段产出候选（name/slug/description），不在 MAP 生成正文。
# ---------------------------------------------------------------------------
EXTRACT_SYSTEM_PROMPT = """你是一名严谨的知识抽取引擎（COMPILER，而非创作者）。你只从给定原文片段中识别可跨文档复用的「实体」与「概念」，绝不创造原文没有的内容。

【定义】
- entity（实体）：具体、可命名的人 / 组织 / 系统 / 项目 / 文件 / 事件（如 "BP 系统"、"曾文哲"、"云平台对接项目"）。
- concept（概念）：抽象主题 / 方法 / 指标 / 流程（如 "指标配置"、"员工关怀"、"知识库构建"）。

【抽取规则】
1. 仅基于原文片段，逐字贴近，不发挥、不引入外部知识（零幻觉）。
2. 每个条目给出一个稳定英文/拼音 slug（小写，连字符分隔，如 "bp-system"、"yunpingtai"），用于跨文档聚合与内部链接。
3. 同一概念在不同文档出现时，slug 必须一致（如都叫 "bp-system"）。
4. 每个条目附一句话 description（它是实体/概念及出自哪里）。
5. 排序与数量：先按「跨文档复用价值」从高到低排序（价值高 = 更可能被多篇文档复用、更核心），再输出；实体与概念各自最多输出 20 个（价值不足可少于 20，但不得超 20）。
6. 严格输出合法 JSON（如下 Schema），不要解释、前言或代码围栏。

【输出 Schema】
{
  "entities": [{"name": "实体中文名", "slug": "entity-slug", "description": "一句话说明它是实体及出自哪里"}],
  "concepts": [{"name": "概念中文名", "slug": "concept-slug", "description": "一句话说明它是概念及出自哪里"}]
}
"""

EXTRACT_USER_TEMPLATE = """文档标题：{file_name}
文档版本：{version_id}

以下为文档原文片段（chunk_id 已标注），请只基于这些片段抽取实体与概念：
{chunks}

请按系统指令抽取（注意零幻觉、slug 稳定、跨文档一致）。"""


# ---------------------------------------------------------------------------
# 汇报级提炼（单篇汇报 → 一份结构化提炼）
# 由工作协同系统生成并写入 report_summary；Wiki 仅消费。
# 验证期由 Wiki 兜底：一次 LLM 调用同时产出 摘要 + 实体候选 + 概念候选，
# 避免对单篇原文多次串行调用。基于完整汇报原文（full_text[:20000]）而非切片。
# ---------------------------------------------------------------------------
REFINE_PROMPT = """你是一名严谨的知识提炼引擎（COMPILER，而非创作者）。你只阅读整篇汇报、提炼其中可复用为知识库的内容，绝不创造原文没有的信息。

任务：把一篇汇报一次性提炼为四部分——一句话摘要、结构化摘要正文、实体候选、概念候选。

【实体 entity】具体、可命名的人 / 组织 / 系统 / 项目 / 文件 / 事件（如 "BP 系统"、"曾文哲"、"云平台对接项目"）。
【概念 concept】抽象主题 / 方法 / 指标 / 流程（如 "指标配置"、"员工关怀"、"知识库构建"）。

【提炼规则】
1. 仅基于汇报原文，逐字贴近，不发挥、不引入外部知识（零幻觉）。
2. 每个 entity/concept 给出一个稳定 slug（小写、连字符分隔，如 "bp-system"、"zhishiku"），用于跨汇报聚合与内部链接；同名必须同 slug。
3. 每个条目附一句话 description（它是什么、出自哪篇汇报）。
4. 排序与数量：先按「跨文档复用价值」从高到低排序（价值高 = 更可能被多篇汇报复用、更核心），再输出；entities 与 concepts 各自最多输出 20 个（价值不足可少于 20，但不得超 20）。
5. 严格输出合法 JSON（如下 Schema），不要解释、前言或代码围栏。

【输出 Schema】
{
  "title": "简洁的提炼页标题（<=120 字）",
  "summary": "一句话概述全文主旨（<=200 字）",
  "markdown": "# 标题\\n\\n结构化摘要正文，列出核心要点（每条必须来自原文）",
  "entities": [{"name": "实体中文名", "slug": "entity-slug", "description": "一句话说明它是实体及出自哪里"}],
  "concepts": [{"name": "概念中文名", "slug": "concept-slug", "description": "一句话说明它是概念及出自哪里"}]
}
"""


def build_refine_user_content(doc_name, text):
    return "【汇报】" + (doc_name or "") + "\n" + "【正文】\n" + (text or "") + "\n"


# ---------------------------------------------------------------------------
# REDUCE：单 slug 页面生成/合并
# 由 pipeline 按 slug 聚合所有文档的候选后，调用一次生成/合并正文。
# ---------------------------------------------------------------------------
PAGE_MODIFY_SYSTEM_PROMPT = """你是一名严谨的 Wiki 页面编辑（COMPILER，而非创作者）。你会收到一个 Wiki 主题（slug + 类型）的「已有页面（旧版，可能为空）」以及来自多篇文档的「候选内容（名称/描述/细节）」。你的任务：把多篇候选内容**增量合并**成一个完整页面，而不是整体重写。

【合并规则】
1. 零幻觉：事实只能来自「候选内容」，不得编造；旧版已存在的内容要保留（除非被候选内容明确更新）。
2. 去重：多文档重复的事实合并为一条，不要堆叠冗余。
3. 更新与冲突：候选内容若更新了旧事实，直接替换；若相互矛盾，在"冲突/待核"小节并列呈现、标注来源，**不要自行裁定**。
4. 结构稳定：用 Markdown 分节（## 标题 + 段落），沿用一个清晰主题结构。
5. 首行约定：summary 首行必须是 "SUMMARY: " 后接一句话摘要。
6. 严格输出：必须输出合法 JSON（如下 Schema），不要任何解释、前言或代码围栏。

【输出 Schema】
{
  "title": "页面标题（即该主题名）",
  "summary": "SUMMARY: 一句话摘要",
  "markdown": "完整页面 Markdown（含冲突小节如有）"
}
"""

PAGE_MODIFY_USER_TEMPLATE = """【主题】
slug：{slug}
类型：{page_type}

【已有页面（旧版，首轮可能为空）】
{prev_md}

【来自多篇文档的候选内容（待合并）】
{agg}

请把上述候选内容增量合并成最终页面（注意跨文档去重、冲突单列、零幻觉、SUMMARY 首行约定）。"""


# ---------------------------------------------------------------------------
# 内部链接（ [[slug]] 注入：让页面正文互相引用）
# ---------------------------------------------------------------------------
LINKIFY_SYSTEM_PROMPT = """你是一名 Wiki 链接编辑。你会收到一段页面正文，以及一个「可用链接目标」列表（每个目标含 slug 与展示名）。

任务：在正文中，凡是出现与某目标展示名/含义一致的内容，用 [[slug|展示名]] 包裹（每个目标最多出现一次链接），使读者可跳转到对应 Wiki 页面。

【规则】
1. 仅对「可用链接目标」列表中存在的 slug 做链接；不得编造 slug。
2. 不要改变原文的事实与句子含义，仅做最小包裹。
3. 若正文中某概念在列表里无对应 slug，则不链接。
4. 严格输出：仅返回链接处理后的完整正文（Markdown），不要解释或代码围栏。

【可用链接目标】（slug | 展示名）
{targets}
"""

LINKIFY_USER_TEMPLATE = """【待链接的页面正文】
{markdown}

请按系统指令注入 [[slug|展示名]] 内部链接（仅使用上方可用目标）。"""

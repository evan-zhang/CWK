# PR-001 独立复审报告（r2）

> 审核会话：`K32Oib59`（Claude Code / Opus）
> 日期：2026-08-19
> 方式：只读审查 PRD、DESIGN、plan、references、r1 审核/整改记录，并抽样核对现有代码；未修改文件、cron、Cloud、DocDB 或生产数据。

## 1. Verdict

**CONDITIONAL PASS**。

架构方向、安全模型、16 RT 拆分、代码所有权和试点边界成立；用户提出的共享本体、用户独立权限/Key、宿主机采集、沙箱只读、多知识空间、Profile 共创、三态多标签路由、冷归档八项诉求均形成“需求 → 设计 → 计划 → 门禁”链路。

RT-011 开工前需关闭两项 Major，并在 RT-011 契约包显式冻结三项 schema 细节。

## 2. Major

### MJ-1：Access grant 状态命名漂移

计划中的迁移 grant 状态必须与 PRD/DESIGN 的合法枚举一致。建议 legacy 导入先进入 `granted`，未经权威源复核不得升级为 `active`；到期/复核失败使用 `revalidation_due`，禁止新增 `pending_revalidation`。

### MJ-2：Route job 幂等键字段漂移

计划必须与 DESIGN §8.2 统一：

```text
route_job_key = tenant_id + report_key + canonical_sha256 + profile_sha256
projection_key = route_job_key + space_id + projector_version
```

禁止并存 `report_version`、`profile_sha` 两套命名。

## 3. Minor 与对应 RT

1. Gate 映射补最终 Claude Code 复审 receipt。
2. RT-011 冻结 canonical JSON 规范与 `profile_sha256` 字节公式。
3. RT-011 冻结 `QueryRequest v1`：`space_selector` 为数组，元素为 opaque `space_id`。
4. RT-013 技术方案明确 binding secret 轮换的双写、原子 pointer 切换与旧目录墓碑。
5. VG-A 只验逻辑拒绝与墓碑；派生 index/cache 清理放 VG-C。
6. RT-022 为 `archive_no_index` 设置独立 limit、并发和超时，默认更保守。
7. 讨论记录未闭合问题逐项绑定 RT/gate。
8. 设计示例使用随机 opaque `space_id`，不能让 `tbs/sfe` slug 进入 Broker 持久契约。
9. RT-025 恢复时核对 Profile 的 `space_id` 集合与派生目录一一对应。
10. RT-011 冻结 `object_id` 生成规则，避免 RT-014/016 各自定义。

## 4. 代码现实性结论

- `cwk_collect_live.py` 当前存在 `--app-key` 与临时明文参数文件，必须由 RT-017 在 Credential Broker 边界后改造。
- `cwk_nightly_pipeline.py` 仍依赖仓库级路径，留到 RT-026 做 legacy façade，RT-018 不应修改。
- `cwk_wiki_query.py` 同时有任意 `--mirror-root` 与 `raw_loader` 注入点；RT-022 可复用库函数，但必须新增 Broker 入口，不向沙箱暴露 legacy CLI。
- `cwk_entity_catalog.py` 是 RT-021 主要修改面；需新增 tenant/space 投影，并隔离全局 entity family registry。
- 当前代码尚无 `tenant_id` / `CWK_INSTANCE_ROOT` 多租户运行时，16 RT 应以新模块为主并受 feature flag 隔离。

## 5. RT-011 放行条件

1. 关闭 MJ-1、MJ-2。
2. RT-011 明确冻结：`object_id` 规则、RFC 8785/JCS 类 canonical JSON 与 domain-separated hash、`QueryRequest v1` 字段、Access grant 七状态枚举。
3. 讨论记录开放问题绑定到具体 RT/gate。
4. 后续 external unknown 一律只能输出 `verified | conservative_unknown`，不能替业务或外部系统拍板。

## 6. 后续必须关闭的 external unknown

- report ID 是否全局唯一；默认使用 `source_namespace + report_id`。
- 双身份下正文、reply、node、attachment 是否一致；验证前全部 tenant overlay。
- 是否存在公司级变更流、权威 grant 与撤权事件；未知时使用 lease/fail closed。
- Gateway 可信 Agent 身份是否不可伪造；未证明则阻断 RT-023 试点。
- Secret backend 与轮换接口。
- 撤权 SLA、冷归档保留期、吞吐/延迟、RTO/RPO；必须由对应 RT 实测。
- 真实 OpenClaw Tool transport 的限流/SLA。
- 2～3 人 14 天和 5～10 人 30 天为开发完成后的发布门禁，不能由 RT-026 短测试替代。

## 7. 结论

完成两项 Major 与 RT-011 schema 冻结要求后，可进入 RT-011；其他 Minor 在对应 RT 的技术方案与独立验收中关闭。

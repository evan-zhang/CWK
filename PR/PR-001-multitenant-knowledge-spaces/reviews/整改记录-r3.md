# PR-001 第三轮交叉复核整改记录

> 日期：2026-08-19
> 范围：仅文档；未修改代码、cron、Cloud、DocDB、生产镜像或真实密钥。

## 闭环

1. DAG 已改为 RT-018/020/021/022/023/VG-D 汇入 RT-024。
2. PRD FR-02、DESIGN C-02、开发计划 RT-012 与威胁模型已冻结同一 Tenant 状态图和操作权限矩阵；删除 `disabled` 别名。
3. Grant 非候选集合已补 `granted`。
4. Profile 状态统一为 `draft/proposed/preview/confirmed/active/superseded`；回滚是追加式 `profile_pointer_rollback` 事件，不存在 `rolled_back` 状态。
5. RouteDecision 统一为 `space_ids[]`；QueryRequest 统一为 `space_selector[]`，元素是 opaque `space_id`。
6. RT-012 独占 `cwk_tenant_cli.py` dispatcher/provider registry；RT-013/019/026 只新增独立 provider。架构图改为 Collector 发布 versioned envelope/event，由 provider 解耦 Router。
7. 同时闭合 r3 Opus Minor：RT-011 输出补 sample/extension/hash 输入契约，审核资料加入引用；G0 最终 receipt 留待下一轮复审绑定。

## 下一步

必须由新的 Claude Code Opus 会话复核整改后的最终文本。无 Blocker/Major 才能闭合 G0 并启动 RT-011。

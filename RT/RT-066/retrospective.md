# RT-066 复盘

## 目标达成了吗

达成。2026-09-19 线上检索 8787 与问答 8790 已切到按成员表判定（`RAG_AUTHZ_MODE=grants`）。

- 切换前两道核对都是 0：逐支有效令牌 × 逐个库 6 处比对完全一致；体检无问题。
- 切换前后同一套 20 项行为探测**逐项相同**，用户无感。
- 现场证明成员表实时生效：收回问答服务对 `spbp-2027` 的只读授权 → 12 秒后该库检索 403，
  同一时刻 `cwork-3m` 仍是 200；恢复授权 → 12 秒后回到 200。全程没有动任何令牌。

## 做对了什么

- **先证明前提，再动开关**。等价性核对用的是线上那份判定代码本身，不是另写一遍的近似实现，
  所以"切过去行为不变"是被证明的，不是被期望的。
- **留了一条能分辨真假的判据**。只看"切换后一切正常"无法区分"成员表在起作用"和"开关根本没生效"——
  收回再恢复一条授权，才真正把这件事钉死。
- **重建前先渲染配置比对**，确认只多出两个变量，端口和挂载都没动。
  这个动作在 RT-061、RT-062 各抓到过一次会造成事故的差异，这次继续做，干净通过。
- 带 `KB_BIND_ADDR=0.0.0.0` 重建，没有重蹈"服务掉出局域网"的覆辙。

## 踩到什么

- RT 的 `type` 我填了枚举里没有的 `Change`，门禁直接红；而我的命令用管道取了尾行，
  退出码被 `tail` 吃掉，占号提交照样提交了。**`cmd | tail -1` 会吞掉退出码**，
  在门禁这种"必须拦住"的地方不能这么写。已补修正提交。
- 上一个 RT 收口时也踩过类似的坑：`meta.yaml` 用 `in-progress`、`index.yaml` 用 `in_progress`，
  两处拼写不同导致状态只改了一半。收口脚本应当按"改完再读回确认"来写。

## 留给下一次

- **回退没有真跑**。备份在 `ops/backup-rt066/`，回退 = 拷回两个 compose 覆盖文件 + 同样的重建命令。
  切换本身已经把"改开关 + 重建"这条路径走通，方向相反而已；但严格说回退是纸面验证，不是演练验证。
- **下一步该补访问审计**：切到成员表之后，"谁在什么时候被拒"更值得留痕。
  服务端为防正文泄露完全不记请求，建议只记「时间 / 令牌 ID / 库 / 结果码」。
- 同事注册之后仍然读不到任何东西，要么给令牌、要么加成员——这一步现在可以在十几秒内完成了。

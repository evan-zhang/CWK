# RT-043 收口复盘

## 结果

统一摄取管道已完成并通过主线合并：源适配、originals 只增、格式处理、raw/index/provenance/state 三账发布和 reconcile 均已落地。功能分支由 `bf64bba` 合并，`03cfae9` 随生产可用性验证关闭花名册。

## 验证

- `tests/test_kb_ingest.py` 覆盖幂等、崩溃恢复、账本一致性、源故障与版本链。
- RT-045 两真库验收验证实际摄取和覆盖率对账；RT-050 后续增量刷新继续复用该链路。
- 真实产物可被 doctor 和只读网关读取。

## 教训

“第二遍零写入”不足以单独证明 write-once；需要绕过状态账直接破坏写入检查，确认判据真的会红。

## 遗留

AI 落位提议、精编产物、DocDB 拆分器和成本护栏属于后续增强，不影响本 RT 的 v1 目标。

# RT-015 开发任务

## T-01 契约冻结
- [x] `PR/PR-001-multitenant-knowledge-spaces/contracts/rt015/schemas/access_grant_record.schema.json`
- [x] `.../state_transition_event.schema.json`
- [x] `.../revoke_intent.schema.json`
- [x] `.../revoke_receipt.schema.json`
- [x] `.../access_tombstone.schema.json`
- [x] `.../cleanup_outbox.schema.json`
- [x] `.../authority_receipt.schema.json`
- [x] `.../tenant_view_record.schema.json`

## T-02 Access Ledger
- [x] `scripts/cwk_access_ledger.py`：
  - [x] `compute_grant_key`（domain-separated H(tenant, report_key)）
  - [x] `AccessLedger` + tenant subdir 幂等创建
  - [x] `observe`（discovered/granted only；tenant status gating；
        actor/reason log injection）
  - [x] `_authoritative_promote`（`promote_to_active`/`refresh_lease`）
  - [x] `mark_revalidation_due`
  - [x] `revoke`（七步 crash-safe 流水线）
  - [x] `check_query_eligibility` + `read_grant_snapshot` +
        `list_query_eligible`（严格 AgentContextSnapshot）
  - [x] `iter_events` / `read_tombstone`
  - [x] `iter_cleanup_outbox` / `ack_cleanup_task`
  - [x] `recover` 幂等
  - [x] `AuthorityAdapter` + `_FailClosedAuthority` +
        `FakeSigningAuthority` +
        `_TEST_AUTHORITY_TOKEN` module-private

## T-03 Tenant View Store
- [x] `scripts/cwk_tenant_view.py`：
  - [x] `TenantViewStore.__init__(layout, ledger, shared_store)`
  - [x] `upsert_overlay`（RT-011 validate + snapshot 双 ACL +
        canonical read_version + CAS + 再次 recheck）
  - [x] `read_view`（前置 + 后置 recheck，防 in-flight revoke）
  - [x] `purge_for_revoked_grant`（幂等 + tombstone 保护）
  - [x] `recover`（tmp 孤儿清理）
  - [x] 拒绝 reply/node full payload；拒绝 body 字段；`view_key`
        与 grant_key 一致

## T-04 测试
- [x] `tests/_rt015_helpers.py`：`LedgerFixture`、
      `FakeAuthorityContext`、observation/canonical/promote helpers
- [x] `tests/test_rt015_schemas.py`：schema deep-forbidden + 字段规范
      + `grant_key` 派生 + RT-011~014 零漂移
- [x] `tests/test_rt015_ledger.py`：observation / authority / lease /
      eligibility / list / isolation
- [x] `tests/test_rt015_revocation.py`：基本撤权 / 双撤权 / 事件 /
      outbox / crash injection / recovery / isolation
- [x] `tests/test_rt015_view.py`：overlay contract / canonical linkage /
      双 ACL / purge
- [x] `tests/test_rt015_attacks.py`：log injection / path / symlink /
      hardlink / tamper / preexisting temp / error opacity /
      enumeration / directory permissions

## T-05 文档
- [x] `RT/RT-015/rt-intake.md`
- [x] `RT/RT-015/specs/需求契约.md`
- [x] `RT/RT-015/specs/技术方案.md`
- [x] `RT/RT-015/tasks/开发任务.md`
- [x] `RT/RT-015/reports/实现记录.md`
- [x] `RT/RT-015/reports/交付验证报告.md`

## T-06 交付验证前检查
- [x] `git diff --check` 干净（RT-011~014 冻结物零漂移）
- [x] RT-015 定向测试全部 PASS
- [x] RT-011~014 回归全部 PASS
- [x] 全套测试 PASS
- [x] 敏感扫描（`CWORK_APP_KEY` / `.env` / 明文密钥）无命中
- [x] 未提交 `.serena/` 或 `.spec-workflow/` 目录
- [x] 未修改 RT-011~014 任何模块或 schema

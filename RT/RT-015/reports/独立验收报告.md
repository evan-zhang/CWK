# RT-015 独立验收报告（黑盒）

> 目的：由独立验收 Agent（`agent-rt015-verify-opus`，与实现 Agent
> `agent-rt015-impl` 完全无共享状态）基于 rt/RT-002-interactive-action-center
> 基线，对 RT-015 交付物 `Access Ledger + Tenant View Store + 撤权核心`
> 做端到端黑盒验收。仅新增本报告，未修改任何实现 / schema / fixture /
> 测试 / PRD / DESIGN / 计划 / RT index / 冻结 legacy 文件。

## 0. 元信息

| 字段 | 值 |
| --- | --- |
| Worktree | `/Users/evan/.openclaw/gateways/life/state/workspace-life/projects/CWK/.worktrees/openclaw-worktree-cwk-rt015-verify-opus` |
| Branch | `agent/cwk-rt015-verify-opus` |
| HEAD 待验收 | `9b115ed` — “RT-015 implementation: Access Ledger, Tenant View, revocation core (G3/VG-A input)” |
| 基线 commit | `e483f67` — “RT-014: record independent acceptance” |
| 上游基线分支 | `rt/RT-002-interactive-action-center` |
| Python | `/opt/homebrew/bin/python3.11` → `Python 3.11.14`（NFR-09 合规） |
| 系统时钟 | 2026-08-19 UTC |
| Verifier 与 impl 分离 | 独立 worktree、独立 shell、未共享内存/临时目录 |
| Scratch 目录 | `/tmp/rt015-verify.N5LiU2/` (mktemp -d)；结束时清理 |
| 真实凭据 | 未访问 `CWORK_APP_KEY` / 工作协同 / DocDB / Cloud / Gateway / cron |

## 1. 前置阅读与研判范围

已完整阅读并交叉核对：

- `PR/PR-001-multitenant-knowledge-spaces/PRD.md`（FR-08 grant ledger 唯一
  SoR、FR-07 tenant view overlay 不复制正文、FR-17 双 ACL 与请求禁字段、
  非目标 M4/G7、SLA=conservative_unknown、NFR-01/02/03/04）。
- `PR/PR-001-multitenant-knowledge-spaces/DESIGN.md`（INV-01~07；C-08
  Access Ledger 状态机；C-09 tenant view；C-13 二次 ACL；§10 撤权流水线；
  §5 表述、SLA 表述、tenant view record 契约）。
- `PR/PR-001-multitenant-knowledge-spaces/plans/开发计划.md` §2/§7/§8
  （RT-015 依赖、VG-A、G3 消费）以及 `§21/§22` 生产放量条件。
- `PR/PR-001-multitenant-knowledge-spaces/references/`
  安全威胁模型（T-01/02/04/06/10/12）、当前架构与差距、讨论决策记录。
- `RT/RT-011~RT-014` 交付验证 + 独立验收报告，全部 PASS + 冻结物 SHA。
- `RT/RT-015/rt-intake.md`、`../specs/需求契约.md`、`../specs/技术方案.md`、
  `../reports/交付验证报告.md`、`../reports/实现记录.md`。

未凭实现内部私有属性做正向白盒假设——所有黑盒攻击均通过 `AccessLedger`
/`TenantViewStore` 的公开接口触发；只在 crash injection 场景直接读写
`registry/access-ledger/<tenant>/` 下的持久文件模拟宕机残留。

## 2. Verdict / 门禁裁决

- **verdict = PASS**
- `contract_conformance = PASS`
- `rt011_rt012_rt013_rt014_frozen_drift = ZERO_BYTES`（`git ls-tree`
  在 HEAD~1 / HEAD 上对所有 frozen 目录 & 模块比对，见 §4）
- `attack_matrix = ALL_PASS`（9 类 / 97 个独立黑盒 case）
- `regression = PASS`（RT-015 定向 124 + 全库 discover 817 skipped=7）
- `error_opacity = PASS`（`AccessDenied.__str__ = "[denied] access denied"`；
  `AccessLedgerError.__str__` 无路径/正文/URL/credential/agent_id）
- `sensitive_scan = CLEAN`（未命中真实密钥、AppKey、Bearer、`.env` 值）
- `git_diff_check = 0`
- **G3 允许？** ❌ 不允许由本次验收单独 close。G3 需要 RT-014、RT-015
  与 RT-016 三部曲整体通过；RT-016 尚未交付，因此本报告只能声明
  “RT-015 侧已具备 G3 前置 receipt”。
- **VG-A 允许？** ✅ **可以 schedule**。RT-015 的必要证据（同一 canonical
  可被 A/B 共享；A/B view 独立；A 撤权后逻辑拒绝与墓碑立即生效且 B
  保持可用；未知 tenant/report/object 不泄露存在性）在本次黑盒验收
  完整复现，见 §3.D。VG-A 集成层受体的 receipt 由后续独立 verifier
  在集成环境上下文中判定。
- **not_allowed = { close_G3_alone, close_G6, close_G7, declare_M3,
  declare_migration, declare_real_gateway_identity_verified,
  declare_sandbox_transport_verified, declare_pilot_ready,
  declare_production_ready, upgrade_verified_shared_extensions_vN,
  modify_RT-011_frozen_artefacts, modify_RT-012_frozen_artefacts,
  modify_RT-013_frozen_artefacts, modify_RT-014_frozen_artefacts,
  declare_real_authority_integrated }**
- `next_rt_gate = RT-015 verify PASS → RT-016 可开工、VG-A 可组织；
  真实生产迁移仍闭环于 G6/G7`

## 3. 独立黑盒攻击矩阵

Attack 脚本均位于 `mktemp -d` 后的 `/tmp/rt015-verify.N5LiU2/`（结束时
删除）。所有 case 只通过公开接口调用；crash-injection 只操作持久文件
（不 monkey-patch 内部函数）。

统计口径：`case = 独立断言`，不合并同一断言的多个 subclaim。

### A. 不可信输入不能伪造身份 / 路径 / 关键契约（15/15 PASS）

命令：`python3.11 /tmp/rt015-verify.N5LiU2/attack_A_input_contract.py`

- A1  bare `dict` 冒充 `AgentContextSnapshot` → `AccessDenied(reason="snapshot_type")`.
- A2  `types.SimpleNamespace` 冒充 snapshot → `AccessDenied`.
- A3  查询 API (`check_query_eligibility` / `read_grant_snapshot`
  / `list_query_eligible`) 拒绝 `tenant_id=` kwarg（`TypeError`）.
- A4  未知 `tenant_id`（RT-012 registry 未登记）→ `AccessLedgerError`.
- A5  wrong `schema` 字段 → `ContractError`.
- A6  observation `initial_status="active"` → RT-011 schema 拒绝
  （observation 只能 `discovered/granted`）.
- A7  observation `mirror_root` 注入 → deep-forbidden 拒绝.
- A8  observation `path` 注入 → deep-forbidden 拒绝.
- A9  observation `agent_id` 注入 → deep-forbidden 拒绝.
- A10 traversal-like `report_id="../../../etc/passwd"` → `REPORT_ID_REGEX`
  拒绝.
- A11 默认 fail-closed authority adapter 拒绝任何 receipt → `AuthorityRejected`.
- A12 A snapshot 尝试查询 B 授权 → `AccessDenied`.
- A13 stale `tenant_auth_epoch` snapshot → `AccessDenied(reason="stale_tenant_auth_epoch")`.
- A14 `tenant_status="draft"` 快照 → `AccessDenied`（只允许 pilot/active）.
- A15 `purge_for_revoked_grant(report_id="../evil")` → `AccessLedgerError`（regex）.

### B. Observation 永不到 active；authority 全 fail-closed（13/13 PASS）

命令：`python3.11 /tmp/rt015-verify.N5LiU2/attack_B_authority.py`

- B1  `observe` 后 grant.status == `granted`.
- B2  granted 状态被 `check_query_eligibility` 拒绝.
- B3  默认 fail-closed adapter：schema-valid unknown-signer receipt
  被 `AuthorityRejected`.
- B4  签名错误 → `AuthorityRejected`.
- B5  receipt 中 grant_key 与 (tenant, report_id) 不匹配 → `AuthorityRejected`.
- B6  `receipt_type="lease_refresh"` 用于 promote → `AuthorityRejected`.
- B7  receipt 中 `lease_expires_at` 已过期 → `AuthorityRejected`.
- B8  lease 超期后再查询（now=+10000s） → `AccessDenied(reason=~lease)`.
- B9  `mark_revalidation_due` → 立即 `AccessDenied`（无宽限）.
- B10 receipt `permission_source="random_source_9"` → 拒绝（enum）.
- B11 A 的 receipt 用于 B → `AuthorityRejected`.
- B12 `lease_ttl_seconds=999999` > `LEASE_TTL_MAX_SECONDS=900` → 拒绝.
- B13 `observe` 后即使不进 promote，查询依然被拒绝.

### C. 一 Agent 一 tenant；epoch bump 使旧 snapshot 失效（7/7 PASS）

命令：`python3.11 /tmp/rt015-verify.N5LiU2/attack_C_epoch_cache.py`

- C1  `TenantRegistry.bump_auth_epoch` 后，旧 snapshot 二次 check 拒绝.
- C2  grant.auth_epoch 相对于 live tenant 变 stale → deny.
- C3  `refresh_lease` 用新 receipt → 恢复 queryable（重新对齐 auth_epoch）.
- C4  in-flight bump（两次 check 之间）→ 第二次 check 拒绝.
- C5  view read 前后各 recheck；epoch bump 后 read_view → `ViewDenied`.
- C6  A snapshot 查询 B 的 grant → `AccessDenied`.
- C7  `list_query_eligible(snapshot=A)` 结果只含 A 的 grants.

### D. A/B 共享 canonical，其余全隔离（9/9 PASS）

命令：`python3.11 /tmp/rt015-verify.N5LiU2/attack_D_isolation.py`

- D1  grant_key(A) ≠ grant_key(B)（同 tenant→报告的 domain-separated
  H(tenant, report_key)）.
- D2  同一 canonical SHA 只写 1 份 shared object（RT-014 已保证；本次
  用 `read_version` 双读校验）.
- D3  A/B 各自 snapshot 均可查询自己的 active grant.
- D4  A 已 upsert view；B 用 B snapshot 读 → `ViewNotFound`（B tenant 目录
  下不存在该 view_key，因 view_key 也是 H(tenant, report_key)）.
- D5  `revoke(A)` 后 A 查询被 `AccessDenied`，B 查询仍返回 active grant.
- D6  A revoke 后 canonical 仍可通过 `SharedEvidenceStore.read_version`
  读回（RT-014 SoR 不受 RT-015 撤权影响）.
- D7  直接 truncate A 的 `grants/*.json` 为 `b"not-json"` →
  A 读被 `AccessDenied` / `GrantCorruption`，B 同报告的 grant 依然
  active、queryable.
- D8  A `bump_auth_epoch` → B eligibility 完全不受影响.
- D9  `list_query_eligible(B)` 结果集合内 tenant_id 全部等于 B.

### E. 撤权流水线 crash-safe / 幂等 / 不可复活（12/12 PASS）

命令：`python3.11 /tmp/rt015-verify.N5LiU2/attack_E_crash.py`

- E1  `revoke(A)` 后再次 `revoke(A)` → 同一 `RevokeReceipt.txn_id`.
- E2  revoke 后 `check_query_eligibility` → `AccessDenied`.
- E3  tombstone 落盘存在 → `read_tombstone(...)` 非 None.
- E4  cleanup outbox 存在且 `consumers ⊇ {"tenant_view","space_index","cache"}`.
- E5  crash 模拟：unlink `revoke-receipts/<txn>.receipt` 后，二次
  `revoke` 抛 `RevocationInProgress`（契约明确“revoked+receipt 缺失
  → 等待 recover 完成”），查询仍持续 deny.
- E6  crash 模拟：unlink tombstone 后，grant 状态仍 `revoked`，
  `check_query_eligibility` 继续 deny——**不可复活语义成立**.
- E7  revoke 后再次 `observe` / `promote_to_active` → `GrantStateError`
  （tombstone 优先，观察/促升全部拒绝）.
- E8  `ack_cleanup_task(consumer=X)`：第一次移除返回 `False`，重复 ack
  同一 consumer 返回 `False`；acknowledged 到最后一个 consumer 返回
  `True`；outbox unlink 后再 ack 返回 `False`.
- E9  `consumer="unknown_consumer_x"` → `AccessLedgerError(code="contract")`.
- E10 `recover(actor,reason)` 返回 `RecoveryReport` dataclass.
- E11 `revoke(不存在的 grant)` → `GrantNotFound`.
- E12 `revoke` 两次：只在第一次 `TenantRegistry.bump_auth_epoch`；第二次
  epoch 不再递增（幂等 bump）.

### F. Tenant view overlay-only；无 body/credential/host path（12/12 PASS）

命令：`python3.11 /tmp/rt015-verify.N5LiU2/attack_F_view_overlay.py`

- F1  基础 upsert 通过.
- F2  view 中 `body` → RT-011 v1 additionalProperties:false 拒绝.
- F3  `reply_overlay[0].body=...` → `_reject_full_payload_overlays`
  或 schema 层拒绝.
- F4  `node_overlay[0].text=...` → 同上.
- F5  view 中 `credential` → 拒绝（deep-forbidden / additionalProperties）.
- F6  view 中 `path="/Users/..."` → 拒绝.
- F7  envelope.tenant_id ≠ snapshot.tenant_id → `ViewDenied`.
- F8  伪造 canonical_sha256 未发布 → `CanonicalMissing`.
- F9  upsert 后 revoke，再 `read_view` → `ViewDenied`（双 ACL 二次
  recheck 命中）.
- F10 磁盘扫描 `tenants/<t>/views/*.json` 不含 `body / credential /
  password / canonical body / app_key_leak / leaked / temporary_url /
  /Users/ / host_path` 任一字节序列.
- F11 `attachment_permissions[0].temporary_url = 12345`（非字符串） →
  schema/合约拒绝.
- F12 `reply_overlay[0].raw="..."` → schema/合约拒绝.

### G. 日志注入 / 路径穿越 / 符号硬链接 / TOCTOU / 篡改（16/16 PASS）

命令：`python3.11 /tmp/rt015-verify.N5LiU2/attack_G_fs_and_logs.py`

- G1  `actor="admin\x00pwn"` → `LogInjectionDetected`.
- G2  `reason=".."\rhi"` → `LogInjectionDetected`.
- G3  `reason="ingest\nhi"` → `LogInjectionDetected`.
- G4  `actor="admin\tpwn"` → `LogInjectionDetected`.
- G5  `actor="admin_中文"` (非 ASCII) → `LogInjectionDetected`.
- G6  `actor=""` → `LogInjectionDetected`.
- G7  `actor="a"*500`（超长）→ `LogInjectionDetected`.
- G8  symlink 替换 `grants/<gk>.json` → 之后读被 fail closed.
- G9  hardlink 攻击者 payload 到 `grants/<gk>.json` → 读拒绝
  （JCS 反向复核 / envelope-vs-grant 交叉检查 / grant_key 派生检查）.
- G10 grant 文件位翻转 → `GrantCorruption` / `AccessDenied`.
- G11 grant 文件尾部追加空格（破坏 canonical byte round-trip） →
  拒绝.
- G12 `AccessDenied.__str__` = `"[denied] access denied"` — 不含
  reason 详情、路径、credential.
- G13 `LogInjectionDetected.__str__` = `"[log_injection] log injection
  detected in 'actor'"` — 无 `/Users`、无 NUL 泄漏.
- G14 用 symlink 替换 `cleanup-outbox/` 子目录后 `iter_cleanup_outbox`
  fail closed（O_NOFOLLOW 生效）.
- G15 在 `grants/` 放置无关文件（`unrelated_file.json`）；触发一次
  `mark_revalidation_due` 后无关文件字节不变.
- G16 全目录扫描 `registry/access-ledger/<tenant>/` 未命中真实
  `CWORK_APP_KEY / app_key / AppKey` 值（只允许 schema 中的字段名字符串）.

### H. 无枚举/裸跨-tenant API；错误 → contract 而非 internal（9/9 PASS）

命令：`python3.11 /tmp/rt015-verify.N5LiU2/attack_H_enum.py`

- H1  反射断言 `AccessLedger` 无 `rollback / reactivate / unrevoke /
  delete_grant / list_all_tenants / has_report_id /
  resolve_by_report_id / list_all_grants / purge_grant / delete_tombstone
  / clear_tombstone / raw_grant_bytes / read_by_object_id /
  existing_report_ids / iter_all_tenants`；`TenantViewStore` 无 `list_all
  / delete_view / purge_all / read_by_object_id / iter_all_tenants /
  list_tenants`.
- H2  `AL.__all__` / `TV.__all__` 均不含 `main/cli/app/run/entrypoint`.
- H3  `observe({"foo":"bar"})` → `ContractError`（RT-011 schema 拦截），
  非 internal.
- H4  bogus `tenant_id="t_"+"b"*26` → `unknown_tenant`.
- H5  `read_grant_snapshot(不存在 report_id)` → `GrantNotFound`.
- H6  `iter_events(不存在 report_id)` → 空迭代 / `GrantNotFound`.
- H7  `purge_for_revoked_grant("does_not_exist")` → `purge_refused` /
  `GrantNotFound` — 不 silent-success.
- H8  `iter_cleanup_outbox(未登记 tenant)` → 空 / 契约错误 — 不 internal.
- H9  反射断言无 SHA-existence 探测接口（`has_object / object_exists
  / check_sha / resolve_sha / grant_key_exists`）.

### I. 零漂移 + 完整回归（4/4 PASS）

- I1  `python3.11 -m unittest discover -s tests -p 'test_rt015_*.py'`
  → **124 OK, 0 failure, 0 error**（RT-015 定向 124 用例全通过，与
  交付报告 3.1 一致）。
- I2  `python3.11 -m unittest discover -s tests -p 'test_*.py'`
  → **817 OK, skipped=7, 0 failure, 0 error**（含 RT-001~014 全库
  回归；与交付报告 3.2 一致）。
- I3  `git diff HEAD~1..HEAD` 只新增 RT-015 独占文件（8 schema + 2
  script + 6 test + 7 RT/RT-015 文档 + `RT/index.yaml` 单行状态
  变更）。**未修改任何 RT-011/012/013/014 frozen 模块 / schema /
  security_defaults / verified_shared_extensions**。
- I4  `python3.11 -m compileall -q scripts tests` → exit 0；
  `git diff --check` → exit 0。

具体 §I3 校验命令：

```
for f in scripts/cwk_pr001_contracts.py scripts/cwk_pr001_probes.py \
         scripts/cwk_instance.py scripts/cwk_atomic_file.py \
         scripts/cwk_tenant_registry.py scripts/cwk_tenant_cli.py \
         scripts/cwk_agent_binding.py scripts/cwk_agent_context.py \
         scripts/cwk_credential_broker.py scripts/cwk_tenant_cmd_binding.py \
         scripts/cwk_shared_evidence.py scripts/cwk_collect_live.py \
         scripts/cwk_nightly_pipeline.py scripts/cwk_raw_store.py \
         scripts/cwk_wiki_query.py scripts/cwk_entity_catalog.py \
         scripts/cwk_wiki_search_index.py scripts/cwk_docdb_cloud.py \
         scripts/cwk_sync_mirror_to_docdb.py; do
    a=$(git rev-parse HEAD~1:$f); b=$(git rev-parse HEAD:$f)
    [ "$a" != "$b" ] && echo "DIFF $f"
done
```

输出 0 行 —— 全部 byte-identical。

对 frozen 目录：`git diff --stat HEAD~1..HEAD -- PR/.../contracts/schemas/
PR/.../contracts/rt012/ PR/.../contracts/rt013/ PR/.../contracts/rt014/
PR/.../contracts/security_defaults.json
PR/.../contracts/verified_shared_extensions_v1.json` 也返回 0 行。

## 4. 交付物字节清单（HEAD:PR/.../contracts/rt015/schemas/）

| Schema | git object SHA |
| --- | --- |
| `access_grant_record.schema.json` | `4a5dcd5b33711466cce3ef9bc22451db529d094c` |
| `access_tombstone.schema.json` | `63fc965044ccd37a4c5e5c30e5ef85c17bdc243f` |
| `authority_receipt.schema.json` | `bbd0a470a058ace57ce97cb78f0727765694a028` |
| `cleanup_outbox.schema.json` | `4f897e1907f85f0965e4702b4328266372d9ab52` |
| `revoke_intent.schema.json` | `9c33702d6da3406e9fe77e5c27236d29a1e39ca4` |
| `revoke_receipt.schema.json` | `152b21c1943928e6dd94cb68186d7f083364eed1` |
| `state_transition_event.schema.json` | `cc662fec69566b30cf979475e1e3c5d9c1620ff6` |
| `tenant_view_record.schema.json` | `79ca563e1973f67d94ca41ba5b810bbb6a819839` |

## 5. Blocker / Major / Minor

- **Blocker：0**
- **Major：0**
- **Minor：0**

未观察到实现层缺陷；本报告的所有 attack case 均按预期 fail-closed 或
返回契约错误码，`AccessDenied.reason` 稳定、`__str__` 完全 opaque，
`RecoveryReport` 幂等，撤权不可复活语义（tombstone 缺失 / receipt
缺失后依然拒绝查询）在 §3.E 全部复现。

## 6. 明确未覆盖 / 明确不宣称

RT-015 交付边界与本次验收范围之外的项目 —— **不视为缺陷，但必须列出**：

1. **真实 authority adapter 未接入**：默认 `_FailClosedAuthority` 已经
   verify，`FakeSigningAuthority` 只走 module-private
   `_TEST_AUTHORITY_TOKEN`；生产集成需要独立 RT。
2. **cleanup 消费者只交付 `tenant_view` (`purge_for_revoked_grant`)**；
   `space_index` / `cache` 消费者由 RT-021 / RT-022 后续实现；接口
   `iter_cleanup_outbox` + `ack_cleanup_task` 已冻结。
3. **Broker / Scheduler / Space Projector / Legacy Timeline payload
   迁移** 全部不属于 RT-015 范围。
4. **真实 Gateway 身份 / OpenClaw Tool / UDS peer credential 通道**：
   RT-013 的 `AgentContextSnapshot.from_trusted(...)` 只在测试层
   验证；生产 transport 由 RT-023 / VG-D / G5 判定。本报告
   **不宣称** 生产就绪。
5. **G3 / VG-A / M3 / 生产切换 / 试点放量**：不由本次 RT-015 独立
   验收单独关闭；VG-A 与 G3 需要 RT-016 就位、集成层 verifier
   在集成环境中确认。
6. **性能 / RTO / RPO / SLA 数字化承诺**：RT-011 已 conservative_unknown；
   RT-024/025 才真正测量。本报告不给出任何 P50/P95 数字。

## 7. 剩余风险（次序：安全 → 隔离 → 可用）

- R1 (Info) `RecoveryReport` 在 tombstone / receipt 已存在的场景不会
  “重建” 缺失文件——本报告 §3.E5/E6 已确认 grant 查询仍持续 deny，因此
  安全属性成立；但如果运维在没有 backup 的情况下手删 receipt/tombstone，
  后续 audit 追溯会需要额外的运维 runbook。设计意图明确，不算缺陷；
  仅记录以便 RT-024 audit 消费。
- R2 (Info) `AccessDenied.reason` 为 module-internal 诊断字段——白盒
  测试可通过 `.reason` 触及；本次验收依赖它做诊断断言，但生产日志/审计
  只序列化 `code`+`grant_key`，因此不影响 opacity。RT-024 需要在
  audit 输出里显式加白名单，避免 `reason` 意外落盘。
- R3 (Info) RT-015 未新增任何 CLI / HTTP / cron / installer 钩子，
  legacy pipeline 与 nightly 未受影响 —— 与实现记录 / rt-intake 一致。

无 Blocker / Major / Minor。

## 8. 用户注意事项 / 复现清单

```
# 1. baseline commits
git rev-parse HEAD            # 9b115ed... (RT-015 impl)
git rev-parse HEAD~1          # e483f67... (RT-014 独立验收)
python3.11 --version          # Python 3.11.14

# 2. RT-011~014 frozen byte-identical
git ls-tree -r HEAD~1 -- PR/.../contracts/schemas/ vs HEAD → identical
for f in <RT-011~014 & legacy modules>; do
    diff between HEAD~1:$f and HEAD:$f (see §I3 script) → all identical
done

# 3. Regression & compile
python3.11 -m compileall -q scripts tests                      # exit 0
git diff --check                                                # exit 0
python3.11 -m unittest discover -s tests -p 'test_rt015_*.py'  # 124 OK
python3.11 -m unittest discover -s tests -p 'test_*.py'        # 817 OK skipped=7

# 4. Black-box attack matrix (all in /tmp/rt015-verify.N5LiU2/)
python3.11 attack_A_input_contract.py     # 15/15 PASS
python3.11 attack_B_authority.py          # 13/13 PASS
python3.11 attack_C_epoch_cache.py        # 7/7   PASS
python3.11 attack_D_isolation.py          # 9/9   PASS
python3.11 attack_E_crash.py              # 12/12 PASS
python3.11 attack_F_view_overlay.py       # 12/12 PASS
python3.11 attack_G_fs_and_logs.py        # 16/16 PASS
python3.11 attack_H_enum.py               # 9/9   PASS
# I (zero-drift + regression) covered by §I1..I4 above.

# 5. Sensitive scan
grep -rIn -E 'CWORK_APP_KEY|AppKey|app_secret|APP_SECRET|Bearer ...|CWORK_TOKEN|token=[A-Za-z0-9]{20,}|(sk|pk)_[a-zA-Z0-9]{20,}' \
    scripts/cwk_access_ledger.py scripts/cwk_tenant_view.py \
    tests/test_rt015_*.py tests/_rt015_helpers.py \
    PR/.../contracts/rt015/schemas/ RT/RT-015/
# 命中项均为字段名字符串（deep-forbidden 列表）或文档字符串（"不接触
# CWORK_APP_KEY"），无明文密钥。

# 6. Cleanup
rm -rf /tmp/rt015-verify.N5LiU2 /tmp/rt015_scratch.txt
git status --short --untracked-files=all      # only this report
```

（本报告为唯一新增文件；未修改任何实现代码 / schema / fixture / 测试 /
PRD / DESIGN / 计划 / RT index / legacy 文件；已 `git diff --check`
通过；`git status --short --untracked-files=all` 只显示本报告。）

# RT-011 开发任务

- [x] T1：新建 `PR/PR-001-multitenant-knowledge-spaces/contracts/` 目录，落地 13 个 JSON schema：
      `report_key`、`canonical_report`、`tenant_view`、`access_observation`、`access_grant`（含 `allowedTransitions`）、
      `knowledge_profile`（含 `allowedTransitions` 与 `forbiddenProperties`）、`profile_pointer_rollback`、
      `route_decision`、`query_request`、`sample_manifest`、`verified_shared_extensions`、`capability_probe`、
      `security_defaults`。
- [x] T2：落地机器可读安全默认 `security_defaults.json` 与 bootstrap `verified_shared_extensions_v1.json`；
      SLA 数值一律 `null`，标注 `sla_measurement_owner = RT-024`。
- [x] T3：撰写 `adr/ADR-0001-transport-and-identity.md`：OpenClaw Tool 优先、UDS peercred 后备、
      loopback HTTP + 自报 `agent_id` 永禁；表格给出每个 probe 的升级条件。
- [x] T4：实现 `scripts/cwk_pr001_contracts.py`：
    - `new_object_id / validate_object_id`（`o_` + base32 128 bit）；
    - `compose_report_key / parse_report_key` + `ReportKey` dataclass；
    - `nfc_normalize`、`_jcs_string/_jcs_number/_jcs_serialize`、`canonical_json_bytes`、`canonical_sha256`；
    - `compute_profile_sha256`（domain separator + 0x00 分隔）；
    - 13 个 `validate_*` 函数 + `validate` 分发；
    - `ACCESS_GRANT_ALLOWED_TRANSITIONS / KNOWLEDGE_PROFILE_ALLOWED_TRANSITIONS` 状态机；
    - `load_security_defaults / load_verified_shared_extensions_v1`。
- [x] T5：实现 `scripts/cwk_pr001_probes.py`：
    - 9 个 probe_id、`POLICY_FORBIDDEN_UPGRADE`；
    - `EvidenceBundle.is_authoritative`：只有 `controlled_environment_receipt` 且 refs 非 `fixture://` 才可能 verified；
    - `run_default_matrix` 与 `aggregate`；
    - fixture / mock 证据永不升级；`verified_shared_extensions_dual_user_sample` 要求 `unique_report_key_pairs ≥ 50`。
- [x] T6：实现 `scripts/cwk_pr001_view_compare.py`：
    - 拒绝 AppKey / credential / cookie / session 字段；
    - `TenantEnvelopeSet` 强制 `validate_tenant_view`；
    - `compare(a, b)` 输出 sample_sizes / only_in_* / canonical mismatches / 逐字段一致率 /
      reply/node/attachment 差异 / URL 是否出现 / shared/overlay 建议；
    - `common < 50` 或 URL/身份字段 → 强制 `keep_in_tenant_overlay`。
- [x] T7：实现 `scripts/cwk_pr001_cli.py`：
    - 子命令 `validate / canonicalize / sha256 / profile-hash / compare-user-views / probe run|aggregate / conformance`；
    - 退出码 0/2/3/4/5，无 traceback，无绝对路径回显；
    - `conformance` 与 `security_defaults.json` 深度比对。
- [x] T8：生成 `tests/fixtures/pr001/`：
    - 良性双租户 envelope 集（common ≥55 → 支持比较器测试 ≥50 阈值）；
    - 12 个 malicious_*（跨 namespace 冲突、path 注入、slug、身份注入、rolled_back、错误 grant/transition、
      holdout overlap、临时 URL 升级、伪造 verified probe、AppKey 输入等）；
    - `manifest.json` 附 canary `RT-011-CANARY-2026-08-19`，每条含 SHA/bytes/expected_result。
- [x] T9：编写测试：
    - `tests/test_pr001_contracts.py`（字节契约 + 九类 schema 正反例 + 转移表）；
    - `tests/test_pr001_probes.py`（默认全 conservative、controlled receipt 升级、fixture/mock 不升级、
       forbidden probe 永不升级、`≥50` 要求、聚合拒绝伪造）；
    - `tests/test_pr001_view_compare.py`（AppKey 拒收、常规比较、`<50` 不升级、canonical mismatch 检测）；
    - `tests/test_pr001_malicious_fixtures.py`（canary、SHA/bytes、每 malicious 被拒）；
    - `tests/test_pr001_cli.py`（CLI 稳定退出码、无 traceback、无绝对路径、conformance drift 检测）。
- [x] T10：跑 `python3.11 -m unittest tests.test_pr001_*` 全绿；再跑现有回归 `python3.11 -m unittest discover -s tests -q`
      与 `python3.11 -m py_compile scripts/*.py`；记录到实现记录与交付验证报告。
- [x] T11：撰写 `RT/RT-011/rt-intake.md` / `specs/需求契约.md` / `specs/技术方案.md` / `tasks/开发任务.md` /
      `reports/实现记录.md`；`reports/交付验证报告.md` 只填客观 receipt，独立验收报告由 `agent-rt011-verify` 补齐。
- [x] T12：更新 `RT/index.yaml` 追加 RT-011 条目；不改 `CHANGELOG.md`、`Makefile`、`.env.example`、`README.md`、
      nightly、cron、生产镜像；不触碰 `.serena/`、`.spec-workflow/`。
- [x] T13：在 worktree 提交（`git add` 显式列文件），提交信息说明 RT-011 交付边界。

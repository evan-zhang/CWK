# RT-011 独立验收报告 r2（agent-rt011-verify-r3-opus）

> Verdict：**PASS**
> 是否允许进入 RT-012：**是**（受下文 Minor 提醒约束）
> 是否允许关闭 SG-02 / SG-07 / G5 或批准 verified_shared 升级：**否**（RT-011 只冻结政策/契约/探针，真实运行时验收留给 RT-023 / VG-D / G5；九项 conservative_unknown 仍未升级）
> 验收 Agent：`agent-rt011-verify-r3-opus`（独立、未参与实现，未修改任何实现代码 / schema / fixture / 测试 / PRD / DESIGN / 计划）
> 分支：`agent/cwk-rt011-verify-r3-opus`
> 唯一新增 / 修改文件：`RT/RT-011/reports/独立验收报告-r2.md`

## 1. 基线与环境

| 项 | 值 |
| --- | --- |
| 验收 worktree | `/Users/evan/.openclaw/gateways/life/state/workspace-life/projects/CWK/.worktrees/openclaw-worktree-cwk-rt011-verify-r3-opus` |
| 基线 commit | `eba42ccd34378e3d295e70e447365520952ae6c0`（“RT-011 remediation: strict Draft-2020-12 engine, signed probe receipts, comparator allowlist, JCS/NFC hardening”） |
| 上一次独立验收 | `8d547e7` （FAIL，Blocker-A/B/C + 6 项 Minor） |
| Python | `Python 3.11.14`（`/opt/homebrew/opt/python@3.11/bin/python3.11`） |
| Platform | `darwin arm64` |
| 依赖 | 仅 stdlib（`base64/hashlib/json/math/re/secrets/unicodedata/argparse/dataclasses/pathlib/typing/datetime`）；**零第三方 pip 依赖** |
| 提交范围 | `6636a33..eba42cc` diff（19 646 行新增，实现方 remediation 的 61 个文件） |

写入策略：**仅新增本报告 `RT/RT-011/reports/独立验收报告-r2.md`；未编辑任何实现代码 / schema / fixture / 测试 / 文档 / 计划**。

## 2. 命令与实际输出（可复现）

### 2.1 RT-011 专项测试

```
python3.11 -m unittest tests.test_pr001_contracts tests.test_pr001_probes \
  tests.test_pr001_view_compare tests.test_pr001_malicious_fixtures tests.test_pr001_cli
```

- 结果：`Ran 124 tests in 1.040s — OK`。
- 与实现方 `交付验证报告.md` §1.3 对齐（124 tests；r1 时为 77，此次增补引擎/receipt/comparator/malicious/CLI 共 47 项）。

### 2.2 全量回归

```
python3.11 -m unittest discover -s tests -p 'test_*.py'
```

- 结果：`Ran 393 tests in 2.057s — OK (skipped=7)`。
- 393 = 269（历史 RT-001..RT-010）+ 124（RT-011）；7 skipped 属既有条件式（可选 fixture 缺失），与 RT-011 无关。

### 2.3 编译 / 空白

```
python3.11 -m compileall -q scripts tests   # exit 0
git diff --check                             # exit 0
```

### 2.4 CLI 冷启动黑盒探测（独立构造）

| 命令 | 观察到的行为 |
| --- | --- |
| `cwk_pr001_cli.py --help` | exit 0，7 子命令齐全，无 traceback |
| `cwk_pr001_cli.py`（无子命令） | exit 4，打印 help（契约合规） |
| `cwk_pr001_cli.py foo` | exit 2（argparse invalid choice） |
| `cwk_pr001_cli.py validate` | exit 2（argparse missing --file） |
| `cwk_pr001_cli.py validate --file /Users/evan/secret_dir/rt011/nonexistent.json` | exit 5；stderr `error: input not found: rt011/nonexistent.json` — 仅保留尾部两段，不打印宿主机绝对路径 |
| `cwk_pr001_cli.py validate --file /tmp/rt011verify_r2/bad.json`（半截 JSON） | exit 2；stderr `error: invalid JSON in rt011verify_r2/bad.json: Expecting value` |
| `cwk_pr001_cli.py validate --file /tmp/rt011verify_r2/dupkey.json`（`{"a":1,"a":2}`） | exit 2；stderr `contract-error: rt011verify_r2/dupkey.json: duplicate JSON key 'a'` |
| `cwk_pr001_cli.py sha256 --file …` × 2（两次子进程） | 相同 stdout：`de32895525fd0a11049aecd96d43ec0ed4ea3c2d64e58ce519d1ed3451628100`；跨进程稳定 |
| `cwk_pr001_cli.py conformance --file .../security_defaults.json` | exit 0 → `{ "conforms": true, "schema": "cwk.pr001.conformance_receipt.v1" }` |
| `cwk_pr001_cli.py conformance --file tests/fixtures/pr001/malicious_security_defaults_loopback_allowed.json` | exit 2；stderr `contract-error: $: additional property '__fixture_note' not permitted` |
| `cwk_pr001_cli.py probe run` | exit 0；9 个 probe 全部 `conservative_unknown`，无 `notes`/`evidence_refs` 字段 |
| `cwk_pr001_cli.py probe aggregate /tmp/rt011verify_r2/forged.json`（含 `evidence_refs`/`notes`） | exit 2；stderr `contract-error: $: additional property 'evidence_refs' not permitted` |
| `cwk_pr001_cli.py probe aggregate /tmp/rt011verify_r2/forged2.json`（`result=verified`, `receipt=null`） | exit 2；stderr `$.receipt: verified capability probes require a signed receipt envelope` |
| `cwk_pr001_cli.py probe aggregate /tmp/rt011verify_r2/forged3.json`（伪造 signer `attacker-Inc` + 假 sha） | exit 2；stderr `$.receipt.signer: receipt.signer is not on the trusted signer allowlist` |
| `cwk_pr001_cli.py probe`（无 probe 子命令） | exit **0**（应为 4）— **Minor-1，未修复** |

## 3. 验收矩阵 V-00 … V-12

Legend：PASS / FAIL / PARTIAL。矩阵内所有 PASS 均由本验收 Agent 独立复现攻击，未依赖实现方的自证测试。

### V-00 公开 CLI/进程契约 — **PARTIAL（Minor-1，仍存在）**

- `--help` / 正例 / 坏 JSON / 缺文件 / 未知命令 全部非零或 0（按契约），无 traceback。
- `_safe_path` 只保留尾部两段目录名（`rt011/nonexistent.json`），不打印宿主机绝对路径。
- 与 r1 报告一致：**`probe`（无子命令）返回 exit 0** 而非 4。argparse `--help` re-invocation 覆盖了 `return EXIT_USAGE`。CLI 契约“使用错误 exit 4”未闭合。**非安全阻塞**，只影响 CI/门禁的机器可区分性。
- CLI 内部异常路径 `except Exception` 会输出 `error: internal failure (<Exception>)`，不打印 traceback；`_read_json` 的 `OSError` / `ContractError` / `JSONDecodeError` / `FileNotFoundError` 分别对应稳定退出码。

### V-01 v1 schema/manifest 正/负例（Draft 2020-12 subset） — **PASS**

独立复现的攻击（本次全部被拒）：

- Canonical envelope 顶层加 `attacker_field`（并重算 `canonical_sha256`）：`ContractError $: additional property 'attacker_field' not permitted`。
- Canonical envelope 内 `author.tenant_id`：`ContractError $.author: additional property 'tenant_id' not permitted`。
- QueryRequest 加嵌套 `meta:{agent_id:'root', credential_ref:'secret://x', path:'/etc/passwd'}`：`$: additional property 'meta' not permitted`。
- QueryRequest 顶层 `extra_field`：`$: additional property 'extra_field' not permitted`。
- TenantView 顶层 `extra`：`$: additional property 'extra' not permitted`。
- access_grant.roles `["receiver","receiver"]`：`$.roles[1]: array items must be unique`（`uniqueItems: true`）。
- sample_manifest.samples 复制同一 `report_key`：`$.samples: samples must have unique report_key values`。
- KnowledgeProfile 修改 `routing_rules` 但保留旧 `profile_sha256`：`$.profile_sha256: profile_sha256 mismatch`。
- Canonical envelope 手工填 `canonical_sha256='f'*64`：`$.canonical_sha256: canonical_sha256 mismatch`。
- `sample_manifest.created_at='2026-08-19'`（date-only）与无时区：均 `$.created_at: datetime must include a timezone (Z or ±HH:MM)`。
- capability_probe 顶层加 `notes` / `evidence_refs`：`$: additional property … not permitted`。

Draft 2020-12 subset 引擎覆盖：`type`（string/integer/number/boolean/array/object/null）、`required`、`properties`、`additionalProperties:false`（含 `unevaluatedProperties:false`）、`items`、`minItems`/`maxItems`、`uniqueItems`、`pattern`、`minLength`/`maxLength`、`minimum`/`maximum`、`format:date-time`（强制时区）、`enum`/`const`、`$ref`（跨文件 + in-schema pointer）。custom 关键字 `deepForbiddenProperties`/`allowedTransitions`/`policy`/`forbiddenFieldPaths`/`recompute*Sha256` 由 `_CUSTOM_HANDLERS` 注册。

**r1 Blocker-A 已修复**（对每张 schema 增加 `additionalProperties:false` + `unevaluatedProperties:false`；`author` 严格 whitelist；`sample_manifest.samples` 去重；`access_grant.roles` `uniqueItems:true`；顶层 dispatch 通过 `_validate_schema` 而非“ad-hoc forbidden 列表”）。

### V-02 ReportKey namespace 隔离 & object_id 规则 — **PASS**

- `compose_report_key("cwork","5") = "cwork:5"`；`compose_report_key("third_party_wf","5") = "third_party_wf:5"`；不冲突（`malicious_report_key_namespace_collision.json` 对齐）。
- `SOURCE_NAMESPACE_REGEX / REPORT_ID_REGEX / REPORT_KEY_REGEX` 全部 `\A ... \Z` 严格锚定：`report_id='../../etc/passwd'` 与 `'abc\n'` 均拒（无法通过 regex）。
- `validate_object_id('o_' + 'a'*26)`（tail=`a`，值 0，2 位残余零）通过；`'a'*25 + 'b'`（tail=`b`，值 1，残余低位非零）被拒 `object_id last character is not a valid canonical base32 tail`；大写 / 太短 / 尾换行均拒。
- 不存在 `hash_exists(sha)` 之类的 API；SHA-256 只作完整性字段。

### V-03 canonical 与 tenant overlay 严格分离 — **PASS**

- Canonical envelope schema 顶层与 `author` 子对象均 `additionalProperties:false + unevaluatedProperties:false`；`_canonical_handler` 追加 `_iter_deep_forbidden` 扫描 32 项 `CANONICAL_FORBIDDEN_FIELDS`（包含 `tenant_id/agent_id/credential_ref/lane/read_status/todo_status/roles/reply/reply_overlay/node_overlay/attachment/attachment_permissions/temporary_url/preview_url/short_url/presign_url/download_url/collected_at/observed_at/path/mirror_root/auth_epoch/binding_epoch/cookie/session_token/authorization/…`）。
- `canonical_sha256` 强制在去除该字段后重算比对；任何顶层 / 嵌套 / 附件字段的 tamper 都会命中。
- 独立复现结果与 §V-01 一致：canonical 层不再可被 tenant/agent/credential/path 注入。

### V-04 Grant 七态 & Profile 六态 & rollback pointer-only — **PASS**

- `ACCESS_GRANT_STATES = ('discovered','granted','active','revalidation_due','revoked','purge_pending','purged')` — n=7；`ACCESS_GRANT_QUERY_ELIGIBLE == {'active'}`。
- `validate_access_grant_transition('purged','active')` → `illegal … ; allowed: []`；`revalidation_due→active` / `granted→active` 等只按 `ACCESS_GRANT_ALLOWED_TRANSITIONS` 允许。
- `access_observation.initial_status ∈ {discovered, granted}`；`active` 通过 `_access_observation_handler` 被拒（也被 schema 的 enum 拒）。
- `KNOWLEDGE_PROFILE_STATES` 六态；**不含 `rolled_back`**（schema enum 与 malicious fixture 双重锁定）。
- `profile_pointer_rollback` `from_version == to_version` 或 `auth_epoch_after <= auth_epoch_before` 均被 `_profile_rollback_handler` 拒。

### V-05 RouteDecision.space_ids & QueryRequest.space_selector opaque only — **PASS**

- RouteDecision `space_ids=['tbs']` → `$.space_ids[0]: value 'tbs' does not match '^sp_[a-z0-9]{10,32}$'`（schema pattern + `_route_decision_handler` 双重拦截）；`disposition='delete'` → `not in enum`。
- QueryRequest `space_selector=['tbs']` 同 pattern 拒；`time_filter: {credential_ref:…}` 被 `additionalProperties: false` 拒；顶层任意扩展字段（`meta / mirror_root / agent_id / …`）全部拒。

### V-06 sample/holdout 不重叠 & profile SHA 公式 — **PASS**

- 手工按公式 `sha256(b"cwk-profile-v1"+0x00+jcs(nfc(proposal))+0x00+sample_manifest_sha256_ascii+0x00+prompt_template_sha256_ascii+0x00+model_id_utf8)` 复算，与 `compute_profile_sha256(...)` 完全一致（`sample=a*64, prompt=b*64, model='newapi/BD-MiniMax'` → `b29439ddb66455fe4da3c6b09ff4396c24e1a4bdf102ae5fadf25b7559a7c51b`）。
- `_sample_manifest_handler` 五项聚合校验：samples 唯一、holdout 唯一、二者不重叠、`actual_sample_size == len(samples)` 且 100≤len(samples)≤200、20≤len(holdout)≤30、`chunk_layout` 精确覆盖 samples、`strata.picked` 求和等于 samples 长度。任一违反 → `ContractError`（`malicious_sample_manifest_*` 五份 fixture 复现）。

### V-07 verified_shared_extensions & 临时 URL 永不升级 — **PASS**

- Bootstrap manifest `verified_shared_extensions_v1.json` `entries=[]`，`manifest_sha256 = "0"*64`；`_verified_shared_handler` 对空 entries 允许 `computed` 或 `"0"*64` 占位。
- 非空 entries 时 `manifest_sha256` 必须精确等于对 payload（去掉该字段）的 `canonical_sha256` — 独立复现：将 `malicious_verified_extensions_temporary_url.json` 的 `manifest_sha256` 重算通过 sha 校验后，进入 `_is_forbidden_extension_path` 立刻命中 `attachments[*].temporary_url` → `field_path 'attachments[*].temporary_url' is on the never-shareable list`。
- 每个 entry `sample_ids` 长度 ≥50 且无重复；含 URL/token/身份 hint（`temporary_url / preview_url / presign / download / short_url / tenant_id / agent_id / credential / app_key / password / token / cookie / session_token / authorization / auth_epoch / binding_epoch / path / absolute_path / mirror_root / lane / read_status / todo_status / roles / allowed_actions / attachment_permissions / reply_overlay / node_overlay / visible_event_ids`）的 field_path 一律拒。

### V-08 compare-user-views 攻击矩阵 — **PASS**

- 输入 schema 由 r2 换为 `cwk.dual_user_observation.v1`：`canonical_fields`（唯一候选允许集：`title / body / author.source_user_id / author.display_name / created_at / source_updated_at`）与 `overlay_fields`（tenant 特有）显式分离。
- 独立复现：
  1. 55 篇 common，overlay 全一致（lane/read/todo/roles/allowed_actions/reply_overlay/node_overlay/attachment_permissions 全相同）→ 仅 6 项 `canonical_fields.*` 候选被建议为 `candidate_verified_shared`；**没有任何 overlay 字段被建议**（`_is_forbidden_extension_path` 与 `CANDIDATE_CANONICAL_FIELDS` 白名单双重锁定）。
  2. 49 common → `upgrade_block_reason='fewer than 50 unique common report_keys'`，无候选升级。
  3. `--threshold=0.0` / `-1.0` → `ContractError upgrade_threshold below floor 0.99`（`THRESHOLD_FLOOR=0.99` 硬地板）。
  4. 同一 `report_key` 在 A 或 B 内重复出现 → 集合构造期即 `ContractError duplicate report_key … in tenant observation set`（不再 last-wins）。
  5. 同一 `report_key` 在 A、B 间 `canonical_sha256` 不一致 → `upgrade_block_reason='canonical_sha256_mismatch_between_tenants'`，所有候选转 overlay。
  6. `attachment_permissions[].temporary_url` / `reply_overlay[].download_url` / `node_overlay[].preview_url` 出现（即使只在一侧）→ 递归 `_detect_temporary_url` 命中 → `upgrade_block_reason='temporary_url_present_in_overlay'`。
  7. `TenantObservationSet` 顶层或 observation 中出现 `app_key/credential_ref/cookie/session_token/authorization/password/token/appKey/authorization_header` 之一 → `ContractError comparator refuses AppKey/credential/session/token fields: [...]`；相同 tenant_id 输入 → `comparator requires two distinct tenants`。
  8. 双租户 fixture `dualuser_clean_tenant_{a,b}.json`：`common=55`，无 block reason，6 个 canonical 候选 `match_rate=1.0`；`dualuser_with_tempurl_tenant_{a,b}.json`：同 55 篇 common，`upgrade_block_reason='temporary_url_present_in_overlay'`，6 个候选全部转 overlay。

**r1 Blocker-C 完全修复**：overlay-only 字段（lane/read/todo/roles/allowed_actions/reply/node/attachment/临时 URL）在 r2 架构下**没有语法路径**能被推荐为 `candidate_verified_shared`。

### V-09 九项能力探针 — **PASS**

- `run_default_matrix()` 返回 9 个 probe，全部 `conservative_unknown`；`aggregate()` `all_verified=False, complete=True, missing=[]`。
- `EvidenceBundle`/`refs=('urn:test',)`/`('data:whatever',)`/`('file:///…',)` 已不再是 r1 的“靠 refs startswith 判断”。r2 完全重写为 `ReceiptEnvelope`：
  - 必须有 `receipt` 子对象（schema `additionalProperties:false + required=['envelope_sha256','signer','signature','target','environment','not_before','not_after']`）。
  - `signer ∈ TRUSTED_PROBE_SIGNERS`（RT-011 冻结为**空集** `frozenset()`；测试通过 `_register_test_probe_signer` 临时注入并在 tear-down `_unregister_test_probe_signer`）。
  - `target == probe_id`（`receipt.target must equal probe_id`）。
  - `environment ∈ {gateway_production, gateway_control}`（schema enum）。
  - `signature = sha256(secret + envelope_sha256_ascii + target_ascii)`；constant-time 比对，任一 tamper 拒。
  - `not_before ≤ _now ≤ not_after`；否则 downgrade 到 `conservative_unknown`（不抛异常，防止敌意 payload 崩掉 CLI）。
  - Forbidden probe `sandbox_transport_loopback_http_self_reported`：schema `policy` 强制 `result=conservative_unknown`；`_capability_probe_handler` 二次判定；`run_probe` 忽略 receipt 直接返回 conservative。
- 独立复现攻击：
  - CLI `probe aggregate /tmp/…/forged.json`（含 `evidence_refs/notes`）→ exit 2（schema 拒）。
  - CLI `probe aggregate /tmp/…/forged2.json`（`result=verified, receipt=null`）→ exit 2 `verified capability probes require a signed receipt envelope`。
  - CLI `probe aggregate /tmp/…/forged3.json`（伪造 signer `attacker-Inc` + 假 sha）→ exit 2 `receipt.signer is not on the trusted signer allowlist`。
  - Python：`ReceiptEnvelope(signer='untrusted_signer', signature='0'*64, target=probe_id)` → `receipt.signer is not on the trusted signer allowlist`。
  - Python：注册 test-signer 后签一个合法 receipt → `result=verified`；换 `signature='deadbeef'*8` → `receipt signature does not verify`；换 `target=trusted_agent_identity_openclaw_tool` → `receipt.target must equal probe_id`；`not_after='2001-01-01T00:00:00Z'`（过期）→ `conservative_unknown`（downgrade）。
  - `aggregate` 要求所有 9 个 probe_id 出现且唯一；缺任一 → `all_verified=False + missing_probe_ids=[...]`；重复 → `duplicate probe_id … in aggregate`。
- CLI `_redact_probe` 把 `receipt.signature` 替换为 `<redacted>`，只保留 `envelope_sha256` hash；probe schema 已无 `notes / evidence_refs / refs` 字段，`file://` 或宿主机绝对路径无法从此路径回显。

**r1 Blocker-B 完全修复**。RT-011 的 `TRUSTED_PROBE_SIGNERS` 冻结为空集，任何调用者无法在 CLI 层伪造 verified；RT-023 才会加入真实 signer，届时再由独立审核批准。

### V-10 ADR / security_defaults 机器 conformance — **PASS**

- `security_defaults.json` 通过 `validate_security_defaults`；`transport_and_identity.preferred_transport = 'openclaw_controlled_tool'` / `fallback = 'unix_domain_socket_peercred'` / `forbidden = 'loopback_http_self_reported_agent_id'` / `identity_source = 'gateway_authenticated_context'` / `request_body_identity_fields_permitted = false`。
- `access_grant.grace_read_forbidden = true`；`revocation_ack_target_seconds = null`；`derived_cleanup_target_seconds = null`；`sla_measurement_owner = "RT-024"` / `sla_confirmation_owner = "RT-026"`（未虚构 SLA 数字）。
- `break_glass.enabled = false`；`cold_archive.archive_no_index_limit_ratio_max = 0.5`；`cache_key.required_components` 7 项；`logging.forbidden_content` 5 项。
- `_security_defaults_handler` 深度 `_iter_deep_dangerous` 扫描 dangerous keys `{loopback_http_self_reported_allowed, loopback_http_allowed, self_reported_agent_id_allowed, alternate_transport, alternate_enabled, break_glass_enabled, grace_read_allowed, allow_all, bypass_acl}`；任一命中即拒。
- CLI `conformance --file .../security_defaults.json` → exit 0；`--file .../malicious_security_defaults_loopback_allowed.json` / `--file .../malicious_security_defaults_break_glass_alt.json` → exit 2 且 stderr `contract-error: $: additional property '__fixture_note' not permitted`（去 `__fixture_note` 后再走一次，仍在 `transport_and_identity.loopback_http_self_reported_allowed` / `break_glass.alternate_enabled` 上被拒）。
- ADR-0001 冻结“政策 + 探针 + 默认”，明确 loopback HTTP + 请求体自报 `agent_id` 任何时候都不满足 G1；真实运行时合规由 RT-023 / VG-D / G5 验收。

### V-11 双租户恶意 fixture manifest / SHA / canary — **PASS**

- 独立按 `hashlib.sha256(open(path,'rb').read())` 复算 `tests/fixtures/pr001/manifest.json` 中 28 项 entries + `canary_file` 的 SHA-256、字节数，**全部匹配**；`canary_id = "RT-011-CANARY-2026-08-19"` 与磁盘 `__canary__.json` 内容对齐。
- 21 个 `malicious_*.json` fixture 全部被对应 validator 拒（15 项因 `__fixture_note`（additionalProperties:false）在第一层就 fail；剥离 `__fixture_note` 后深层攻击仍全部被拒——见 §V-01/V-07/V-09 复现表）。
- 附加：新增 `dualuser_clean_tenant_{a,b}.json`（各 55 篇 common，overlay 全 tenant 特有）、`dualuser_with_tempurl_tenant_{a,b}.json`（含 attachment/reply/node 内嵌 temporary URL）、`jcs_number_vectors.json`（独立 RFC 8785 / ECMA-262 数值 oracle，含 `-9007199254740992` reject 案例）。
- fixture 中 canary 只作为磁盘文件与 manifest 元数据存在；CLI 运行输出、错误信息、临时文件、日志中均不出现 canary 字符串。

### V-12 标准 RT 包 / 范围 / 回滚 / unknown 显示 — **PASS**

- `RT/RT-011/{rt-intake.md, specs/需求契约.md, specs/技术方案.md, tasks/开发任务.md, reports/实现记录.md, reports/交付验证报告.md}` 全部到位并已更新 r1 整改说明；`RT/RT-011/reports/独立验收报告.md`（r1 FAIL）保留、未被修改；本报告为唯一新增文件。
- `git diff --stat 6636a33..eba42cc`：61 文件、19 646 行新增、6 647 行删除，**全部集中在 RT-011 边界**：`PR/PR-001-…/contracts/schemas/*.schema.json`（14 张）、`security_defaults.json`、`verified_shared_extensions_v1.json`、`ADR-0001`、`scripts/cwk_pr001_*.py`（4 个）、`tests/test_pr001_*.py`（5 个）、`tests/fixtures/pr001/**`（28 JSON + manifest + canary）；**未触碰**任何 legacy 代码 / cron / `.env.example` / CHANGELOG / RT-001..RT-010 / `.serena/` / `.spec-workflow/` / `knowledge/`。
- 未新增 runtime / registry / credential broker / canonical store / ACL / collector / scheduler / router / projector / query broker。
- 未读取 `CWORK_APP_KEY`；未发起真实 CWork 请求；无第三方 pip 依赖（`grep -R "^import" scripts/cwk_pr001_*.py` 只见 stdlib）。
- 九项 `unknown` 事实在 `security_defaults.json`、`交付验证报告.md`、`实现记录.md`、`rt-intake.md`、本报告 §7 一致列出；`conservative_unknown` 未被错误升级。

## 4. 五类外部事实 fact_status

| 事实 | fact_status | 依据 |
| --- | --- | --- |
| `report_id_global_uniqueness` | `conservative_unknown` | 无跨源实测；默认 `source_namespace + ':' + report_id`；schema/regex 双重锁定；`security_defaults.report_key_composition.downgrade_forbidden=true` |
| 双身份可见样本差异（reply/node/attachment/临时 URL） | `conservative_unknown` | 双租户 fixture 是合成 55 条，非真实双 AppKey 采集；`verified_shared_extensions_v1.json` 保持空 entries；升级路径依赖 RT-016+RT-019 及真实差异证据 |
| 可信 Gateway Agent 身份（OpenClaw Tool / UDS peercred） | `conservative_unknown` | ADR-0001 只冻结政策；`TRUSTED_PROBE_SIGNERS = frozenset()`；RT-023 / VG-D / G5 未执行 |
| 权限撤权来源（events / API） | `conservative_unknown` | 未接工作协同源；`revocation_ack_target_seconds/derived_cleanup_target_seconds = null`；`sla_measurement_owner=RT-024` |
| 沙箱通信通道（Tool / UDS） | `conservative_unknown`；`loopback_http_self_reported` **政策禁止升级，永远保守**（schema `policy` + `_capability_probe_handler` 强制） | 未执行真实沙箱验收 |

**关键结论**：在当前 remediation 后的 probe 机制下，任何调用者**无法**通过手写 JSON 把上述 4 项 `conservative_unknown` 事实伪造为 `verified` —— 需要注册可信 signer + 合法签名 + `target=probe_id` + 有效时间窗 + `environment ∈ {gateway_production, gateway_control}` 五项条件同时满足；RT-011 的 `TRUSTED_PROBE_SIGNERS` 为空集，不存在可用生产 signer。**RT-011 结束不允许关闭 SG-02 / SG-07 / G5 / G7；不允许批准任何 `verified_shared_extensions_vN` 升级**。

## 5. Blocker / Major / Minor 汇总

### Blocker — 无

**r1 三个 Blocker 全部闭合**：

- **Blocker-A（Python validator 不闭合 `additionalProperties: false`）**：新增 Draft 2020-12 subset 引擎，14 张 schema 全部 `additionalProperties:false + unevaluatedProperties:false`，`author` / `time_filter` / `attachment_permissions[]` / `reply_overlay[]` / `node_overlay[]` 每层 whitelist；`_iter_deep_forbidden` 在 canonical / query / profile 三处叠加 deep 扫描。`sample_manifest.samples` 去重，`access_grant.roles` `uniqueItems:true`。独立黑盒复现（§V-01）确认不再可绕过。
- **Blocker-B（探针任意 caller 可伪造 verified）**：受 r2 `ReceiptEnvelope` + 冻结空 `TRUSTED_PROBE_SIGNERS` + signer/target/environment/时间窗/签名五项校验闭合；CLI `_redact_probe` 抹掉 `signature`；probe schema 无 `notes/evidence_refs`。独立复现（§V-09）确认 CLI 无法产出 `all_verified=true`。
- **Blocker-C（comparator 建议 tenant overlay 字段）**：受 `cwk.dual_user_observation.v1` schema 输入 + `CANDIDATE_CANONICAL_FIELDS` 白名单 + `THRESHOLD_FLOOR=0.99` + 每字段 100% 覆盖 ≥50 unique + duplicate report_key 拒 + canonical_sha256 mismatch block + 递归 `_detect_temporary_url` 六重闭合。独立复现（§V-08）确认 overlay 字段无语法路径可被建议。

### Major — 无

### Minor（不阻塞 RT-012；建议在 RT-012 或后续 RT 内顺手处理）

- **Minor-1（未修复自 r1）**：`cwk_pr001_cli.py probe`（无子命令）返回 exit **0** 而非 4；`main()` 内 `parser.parse_args([args.command, "--help"])` 隐式 `sys.exit(0)` 覆盖了 `return EXIT_USAGE`。修法：改写为 `p_probe.print_help(sys.stderr); return EXIT_USAGE`。**非安全阻塞**，只影响 CI 门禁的机器可区分性。
- **Minor-2（新观察）**：`_read_json` 在 JSON payload 包含 lone-surrogate（例如 `{"x":"\uD83D"}`）时，`canonical_json_bytes` → `UTF-8 encode` 抛 `UnicodeEncodeError`，被 CLI `except Exception` 兜底为 `error: internal failure (UnicodeEncodeError)` + exit 5。行为安全（无 traceback、无路径回显），但严格来讲这是“契约错误”，宜转为 `ContractError`（在 `_jcs_string` 或 `nfc_normalize` 前显式 reject lone surrogate）以走 exit 2。
- **Minor-3（架构备忘）**：`compute_profile_sha256` 是 RT-011 硬编码；`security_defaults.profile_sha256_recipe.byte_formula` 只记录字符串，`cwk pr001 conformance` 覆盖不到下游 RT-014/019 是否复用同一实现。建议 RT-019 在 CI 里额外做一次 cross-implementation SHA vector 对齐（用 RT-011 的 `jcs_number_vectors.json` + 一组 profile vectors）。
- **Minor-4（架构备忘）**：`REPORT_ID_REGEX = ^[A-Za-z0-9][A-Za-z0-9_\-.]{0,127}$` 已比 r1 的 `^[^\s]{1,256}$` 严格得多（拒 `/ : \ ..`），路径穿越攻击已在 RT-011 边界内闭合；但下游 RT-014 落地 `object_path(object_id)`/`report_versions/<source_namespace>/<report_id>/catalog.jsonl` 时仍需二次做 `Layout Resolver` 的 containment 校验（DESIGN §5 C-01 明示），不是 RT-011 遗漏。

## 6. Scope diff（与开发计划 §3 严格边界的对齐）

- 未新增 tenant runtime / registry / credential broker / canonical store / ACL / collector / scheduler / router / projector / query broker。
- 未修改：`cwk_collect_live.py` / `cwk_nightly_pipeline.py` / `cwk_wiki_query.py` / `cwk_wiki_search_index.py` / `cwk_entity_catalog.py` / `cwk_sync_mirror_to_docdb.py` / `Makefile` / `.env.example` / `CHANGELOG.md` / `RT/RT-001..RT-010` / `config/` / `docs/` / `knowledge/`。
- 未新增 cron / feature flag / 环境变量 / 生产开关；未读取真实 `CWORK_APP_KEY`；未接入 Cloud/DocDB/生产镜像。
- 未触碰 `.serena/`、`.spec-workflow/`。
- 无第三方 pip 依赖；仅 stdlib（`base64/hashlib/json/math/re/secrets/unicodedata/dataclasses/pathlib/typing/datetime/argparse`）。
- fixture SHA/bytes/canary 全部与 `manifest.json` 匹配（独立 oracle 复算）；`__canary__` 只在磁盘 + manifest 中出现，不进入运行时输出。

## 7. 残余 conservative_unknown（禁止升级）

以下事实保持 `conservative_unknown`，**任何一个都不允许通过当前 probe 机制升级**（RT-011 的 signer allowlist 为空）：

- `report_id_global_uniqueness`
- `permission_authoritative_events` / `permission_authoritative_api`
- `trusted_agent_identity_openclaw_tool` / `trusted_agent_identity_uds_peercred`
- `sandbox_transport_openclaw_tool` / `sandbox_transport_uds`
- `verified_shared_extensions_dual_user_sample`
- `sandbox_transport_loopback_http_self_reported`（**政策禁止升级，永远保守** —— schema `policy` + `_capability_probe_handler` 强制）

## 8. 与 r1 独立验收 Findings 的复核

| r1 Finding | 结论 | 依据 |
| --- | --- | --- |
| Blocker-A：validator 只做浅层禁止词 | **CLOSED** | Draft 2020-12 引擎 + `additionalProperties:false` + custom handlers；独立复现 §V-01。 |
| Blocker-B：探针任意 caller 可伪造 verified | **CLOSED** | `ReceiptEnvelope` + `TRUSTED_PROBE_SIGNERS=frozenset()` + signer/target/env/time-window/signature 五项检查；独立复现 §V-09。 |
| Blocker-C：comparator 建议 overlay-only 字段 | **CLOSED** | `cwk.dual_user_observation.v1` schema + `CANDIDATE_CANONICAL_FIELDS` 白名单 + 100% coverage + threshold 硬地板 + 递归 tempURL 检测 + duplicate/mismatch block；独立复现 §V-08。 |
| Minor-1：`probe` 无子命令 exit=0 | **未修复** | `parser.parse_args([args.command,'--help'])` 仍在 CLI 内。非安全阻塞。 |
| Minor-2：`profile_sha256_recipe` 无跨 RT conformance | 记录为架构备忘 | 需 RT-019 引入 cross-implementation SHA vectors。 |
| Minor-3：`交付验证报告.md` §5 wiki_smoke 标注 | r2 交付报告已改为“不出具 PASS”，不再包含误导性 §5 | 交付报告 §6 明确“独立验收由本 Agent 出具”。 |
| Minor-4：`_safe_path` 保留尾部两段 | 不改 | 属可接受权衡：完整绝对路径不回显；截断颗粒度不足以泄漏 tenant。 |
| Minor-5：JCS 数字边界 | **CLOSED** | `_js_number_string` 按 ECMA-262 Number.prototype.toString 实现，`1e-6`→`0.000001`、`1e-7`→`1e-7`、`1e21`→`1e+21`、`1e20`→`100000000000000000000`、`2^53-1`→`9007199254740991`、`2**60`→`ContractError`。独立向量 fixture `jcs_number_vectors.json`。 |
| Minor-6：NFC 键碰撞 | **CLOSED** | `nfc_normalize` 检测键碰撞并 `raise ContractError`；`strict_json_loads` 拒 JSON 重复键。独立复现 §V-01 / JCS 段。 |

## 9. 最终 Verdict

**PASS。**

- RT-011 交付的 v1 契约在 Draft 2020-12 subset 引擎、能力探针签名 receipt 与双用户比较器三个安全维度上，独立黑盒验证**全部通过**，与 PRD FR-06/07/17、DESIGN §5 C-06/C-13/§11、`安全威胁模型.md` T-01/T-07/T-09/T-11、以及 rt-intake.md 冻结口径一致。
- Blocker-A/B/C 全部闭合；未发现 Major；仅剩一个 r1 遗留 Minor-1（CLI `probe` 无子命令 exit=0，非安全阻塞）+ 一个新观察 Minor-2（lone-surrogate 走 exit 5 而非 exit 2，非安全阻塞）+ 两条架构备忘。
- 九项 conservative_unknown 全部保持保守；RT-011 未升级任何外部事实；`sandbox_transport_loopback_http_self_reported` 永久锁死。
- `TRUSTED_PROBE_SIGNERS = frozenset()`：任何调用者无法在 RT-011 边界内产出 `all_verified=true`；真实 signer 由 RT-023 引入并需独立审核。

**允许启动 RT-012**（Instance Layout / Tenant Registry），但仍需遵守：

- **禁止**关闭 SG-02 / SG-07 / G5 / G7；
- **禁止**批准 `verified_shared_extensions_vN` 升级（除非独立审核 + ≥50 真实双身份样本 + 递归 tempURL 检查全部通过）；
- **禁止**在 RT-012~RT-026 内私自修改 RT-011 冻结的 14 张 v1 schema / `security_defaults.json` / `verified_shared_extensions_v1.json` / `ADR-0001`；
- RT-023 引入首个 `TRUSTED_PROBE_SIGNERS` 条目时，必须走独立 PR / 独立审核 / 增加 signature vector 测试；
- 建议 RT-012 或后续 RT 在自己的 CLI 修复同类 Minor-1（`--help` re-invocation 覆盖 return code）。

**结论**：
- **contract_conformance = PASS**（Blocker-A/B/C 均已闭合）。
- **每类外部事实 fact_status = conservative_unknown**（不允许被计为 verified）。
- **允许进入 RT-012**；**不允许**关闭 SG-02 / SG-07 / G5 / G7；**不允许**批准任何 `verified_shared_extensions_vN` 升级。

## 10. 附录：本 Agent 使用的独立命令一览

```
# 1. baseline
git rev-parse HEAD                                        # eba42cc
git log --oneline -3                                      # eba42cc vs 8d547e7 vs 6636a33
python3.11 --version                                      # 3.11.14

# 2. RT-011 tests
python3.11 -m unittest tests.test_pr001_contracts tests.test_pr001_probes \
    tests.test_pr001_view_compare tests.test_pr001_malicious_fixtures tests.test_pr001_cli
# → 124 tests OK

# 3. Full regression
python3.11 -m unittest discover -s tests -p 'test_*.py'
# → 393 tests OK (7 skipped)

# 4. compile / diff
python3.11 -m compileall -q scripts tests                 # exit 0
git diff --check                                          # exit 0

# 5. CLI probes
python3.11 scripts/cwk_pr001_cli.py --help                # exit 0
python3.11 scripts/cwk_pr001_cli.py                       # exit 4
python3.11 scripts/cwk_pr001_cli.py foo                   # exit 2 (argparse)
python3.11 scripts/cwk_pr001_cli.py validate              # exit 2 (argparse)
python3.11 scripts/cwk_pr001_cli.py validate --file /Users/evan/secret_dir/rt011/nonexistent.json
                                                          # exit 5, "rt011/nonexistent.json"
echo '{"broken":' > /tmp/rt011verify_r2/bad.json
python3.11 scripts/cwk_pr001_cli.py validate --file /tmp/rt011verify_r2/bad.json
                                                          # exit 2, "invalid JSON"
echo '{"a":1,"a":2}' > /tmp/rt011verify_r2/dupkey.json
python3.11 scripts/cwk_pr001_cli.py validate --file /tmp/rt011verify_r2/dupkey.json
                                                          # exit 2, "duplicate JSON key"
python3.11 scripts/cwk_pr001_cli.py conformance --file PR/PR-001-multitenant-knowledge-spaces/contracts/security_defaults.json
                                                          # exit 0
python3.11 scripts/cwk_pr001_cli.py conformance --file tests/fixtures/pr001/malicious_security_defaults_loopback_allowed.json
                                                          # exit 2
python3.11 scripts/cwk_pr001_cli.py conformance --file tests/fixtures/pr001/malicious_security_defaults_break_glass_alt.json
                                                          # exit 2
python3.11 scripts/cwk_pr001_cli.py probe run             # exit 0 (9 conservative_unknown, no notes/refs)
python3.11 scripts/cwk_pr001_cli.py probe                 # exit 0 ← Minor-1

# 6. Schema strict engine attacks (Blocker-A retest)
python3.11 -c "…"   # canonical + attacker_field/author.tenant_id → all rejected
python3.11 -c "…"   # query_request + meta{agent_id,credential_ref,path} → rejected
python3.11 -c "…"   # tenant_view + extra → rejected
python3.11 -c "…"   # access_grant.roles dup → rejected
python3.11 -c "…"   # sample_manifest.samples dup report_key → rejected
python3.11 -c "…"   # knowledge_profile edit routing_rules keep sha → rejected

# 7. Probe attacks (Blocker-B retest)
python3.11 -c "…"   # ReceiptEnvelope untrusted signer → rejected
python3.11 -c "…"   # register test signer → verified; tamper signature/target → rejected
python3.11 -c "…"   # expired receipt → downgrade to conservative
python3.11 -c "…"   # forbidden probe + valid receipt → conservative
python3.11 -c "…"   # aggregate partial subset → all_verified=False
python3.11 scripts/cwk_pr001_cli.py probe aggregate /tmp/rt011verify_r2/forged{,2,3}.json
                                                          # all rejected

# 8. Comparator attacks (Blocker-C retest)
python3.11 -c "…"   # 55 common overlay-only lane/read/todo/roles/allowed_actions/reply/node/attachment
                    # → only canonical_fields.* suggested; no overlay suggested
python3.11 -c "…"   # threshold 0.0 / -1.0 → rejected
python3.11 -c "…"   # 49 common → block reason 'fewer than 50 unique common report_keys'
python3.11 -c "…"   # canonical_sha256 mismatch → block reason 'canonical_sha256_mismatch_between_tenants'
python3.11 -c "…"   # nested temporary_url in attachment_permissions/reply_overlay/node_overlay → block
python3.11 -c "…"   # duplicate report_key within tenant set → rejected
python3.11 -c "…"   # same tenant compared → rejected

# 9. JCS / NFC / edge numbers
python3.11 -c "…"   # 0/-0/0.1/1e-6/1e-7/1e21/1e20/2^53-1 → ECMA-262 canonical strings
                    # inf/nan/bool/2**60 → rejected
python3.11 -c "…"   # NFC/NFD identical bytes; NFC key collision rejected; duplicate JSON key rejected
                    # cross-process JCS stable

# 10. Fixture manifest + canary + malicious matrix
python3.11 -c "…"   # 28 entries + canary file SHA/bytes all match; canary_id=RT-011-CANARY-2026-08-19
python3.11 -c "…"   # 21 malicious_*.json all rejected by appropriate validator
python3.11 -c "…"   # strip __fixture_note → deeper attack still rejected

# 11. Byte contracts
python3.11 -c "…"   # object_id valid tail 'a', invalid tail 'b'/uppercase/short/newline
python3.11 -c "…"   # ReportKey namespace isolation, ../ + \n rejected
python3.11 -c "…"   # profile_sha256 manual replay matches compute_profile_sha256
```

（所有命令均在本 worktree 内运行；未修改任何实现文件；本报告为唯一新增文件。）

# RT-011 交付验证报告（实现方）

> 状态：实现完成；**独立验收 PASS 由 `agent-rt011-verify` 单独出具**。
> 本文件只记录客观 receipt；不包含 “通过 / PASS” 结论。

## 命令与结果

### 1. `python3.11 -m compileall -q scripts`

- 结果：无输出（成功）。

### 2. `python3.11 -m py_compile scripts/cwk_pr001_*.py`

- 结果：`compile ok`。

### 3. RT-011 专项测试

```
python3.11 -m unittest \
  tests.test_pr001_contracts \
  tests.test_pr001_probes \
  tests.test_pr001_view_compare \
  tests.test_pr001_malicious_fixtures \
  tests.test_pr001_cli
```

- 结果：`Ran 77 tests in 0.604s — OK`。

### 4. 现有全量回归

```
python3.11 -m unittest discover -s tests -p 'test_*.py'
```

- 结果：`Ran 346 tests in 1.174s — OK (skipped=7)`。
- 说明：346 = 269（历史 RT-001..RT-010）+ 77（RT-011 新增）。7 个 skipped 均属于既有条件式测试（例如可选 fixture 不存在），
  与 RT-011 变更无关。

### 5. `python3.11 scripts/cwk_wiki_smoke_test.py`

- 结果：`overall=FAIL failed=9/9`。
- 说明：worktree 隔离副本没有携带 `knowledge/工作协同镜像/wiki/`，因此 wiki_smoke 全部报缺失文件；
  与 RT-011 修改无关。在带完整 mirror 的主工作树复跑即可恢复；本 RT 未修改任何 wiki 相关代码。

### 6. `python3.11 scripts/cwk_pr001_cli.py --help`

- 退出码：0；输出列出 7 个子命令；无 traceback；无绝对路径。

### 7. `python3.11 scripts/cwk_pr001_cli.py probe run | ... probe aggregate ...`

- 默认矩阵 → 全部 `conservative_unknown` → 聚合退出码 3；
- 附上 `controlled_environment_receipt` 证据（≥50 unique pairs）→ 聚合退出码 0；
- 附上 `fixture://` 证据 → 仍然 conservative → 退出码 3；
- `sandbox_transport_loopback_http_self_reported` 无论证据如何都 conservative。

### 8. `python3.11 scripts/cwk_pr001_cli.py conformance --file PR/PR-001-multitenant-knowledge-spaces/contracts/security_defaults.json`

- 退出码 0（自对齐）。
- 手动修改 `transport_and_identity.forbidden_transport` 值 → 退出码 2 并输出 `forbidden_transport` 差异。

## 交付文件（本 RT 新增）

- `PR/PR-001-multitenant-knowledge-spaces/contracts/schemas/*.schema.json` × 13
- `PR/PR-001-multitenant-knowledge-spaces/contracts/security_defaults.json`
- `PR/PR-001-multitenant-knowledge-spaces/contracts/verified_shared_extensions_v1.json`
- `PR/PR-001-multitenant-knowledge-spaces/contracts/adr/ADR-0001-transport-and-identity.md`
- `scripts/cwk_pr001_contracts.py`
- `scripts/cwk_pr001_probes.py`
- `scripts/cwk_pr001_view_compare.py`
- `scripts/cwk_pr001_cli.py`
- `tests/test_pr001_contracts.py`
- `tests/test_pr001_probes.py`
- `tests/test_pr001_view_compare.py`
- `tests/test_pr001_malicious_fixtures.py`
- `tests/test_pr001_cli.py`
- `tests/fixtures/pr001/*.json` × 16（含 `manifest.json` with canary）
- `RT/RT-011/rt-intake.md`
- `RT/RT-011/specs/需求契约.md`
- `RT/RT-011/specs/技术方案.md`
- `RT/RT-011/tasks/开发任务.md`
- `RT/RT-011/reports/实现记录.md`
- `RT/RT-011/reports/交付验证报告.md`
- `RT/index.yaml`（追加 RT-011 条目）

## 残余 conservative_unknown

以下事实在本 RT 结束时仍为 `conservative_unknown`，等待 RT-023 / VG-D / G5 用真实运行时数据升级：

- `report_id_global_uniqueness`
- `permission_authoritative_events`
- `permission_authoritative_api`
- `trusted_agent_identity_openclaw_tool`
- `trusted_agent_identity_uds_peercred`
- `sandbox_transport_openclaw_tool`
- `sandbox_transport_uds`
- `verified_shared_extensions_dual_user_sample`
- `sandbox_transport_loopback_http_self_reported`（**政策禁止升级，永远保守**）

## 回滚

删除本 RT 新增的目录 / 文件即可完全回滚；本 RT 未修改任何 legacy 代码，
未新增 cron / feature flag / 环境变量 / 生产开关。

## 独立验收

- 本报告不出具 PASS。
- `agent-rt011-verify` 将以黑盒方式复跑上述命令，独立生成 `RT/RT-011/reports/独立验收报告.md`。
- 提交 SHA 与最终修改文件清单见 git commit。

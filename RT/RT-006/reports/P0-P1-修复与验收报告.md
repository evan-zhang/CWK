# CWK Cloud-First P0/P1 修复与验收报告

> 状态：完成。Opus 5 最终只读评审裁决 PASS；本轮未删除本地 raw/wiki。

## 1. 交付范围

本轮关闭 Cloud-First v2 的数据完整性、证据可信、云端提交、恢复、性能和隐私边界 P0/P1：

- 修复 manifest source/compiled/quality 漂移；
- raw 默认拒绝上传，仅允许显式个人私有 DocDB Cloud-First 路径；
- 建立版本化持久 search index 和 cloud/shadow 查询；
- 建立 summary→raw 的 report ID、SHA、cloud file ID 稳定绑定；
- 建立逐对象云端字节审计、远端 active tree 孤儿/重复检查；
- 建立 retry queue、个人项目断言和读己之写门禁；
- 建立大 raw 内容寻址 gzip 分片与逻辑 SHA 重组；
- 建立空目录全量恢复、lint、smoke 验收；
- 防止空本地镜像或大比例裁剪覆盖云端 catalog；
- 将 catalog 发布改为 live pre-commit 审计通过后提交。

## 2. 最终云端状态

- 云端提交索引：v10。
- active tree 逻辑对象：3,452。
- mapped/hash match/live verified：3,452/3,452。
- missing、extra、missing checksum、hash mismatch、live error：均为 0。
- remote missing、remote extra、remote duplicate、retained legacy/index：均为 0。
- `project_id == personal_project_id`，`private_target_verified=true`。
- 默认 `runs/docdb-sync-retry-queue.json`：0 条待重试。

证据：`runs/rt006-cloud-coverage-v10-final.json`。

## 3. 查询与性能

- 固定 20 问 live scan vs persistent index Top-8：
  - 聚合重合率 1.0；
  - 精确排序率 1.0；
  - 契约门槛 ≥0.99，通过。
- 固定 5 问 × 2 轮：
  - local persistent index P50 0.0121 秒；
  - cloud-first P95 3.4938 秒；
  - error rate 0。
- v10 云端查询返回 verified evidence、cloud file ID 与预览链接，preview error 0。

证据：

- `runs/rt006-shadow-consistency-v10.json`
- `runs/rt006-benchmark-v10-cache-final.json`
- `runs/rt006-cloud-query-v10.json`

## 4. 恢复与回滚

- v9 两次不同空目录全量恢复：各 3,451/3,451、失败 0；lint 与 18/18 smoke 通过。
- v10 提交后再次从空目录全量恢复：3,452/3,452、失败 0；lint 与 18/18 smoke 通过。
- 65MB raw 的分片重组与逻辑 SHA 已单独验证。
- 旧索引、错误大 raw 与孤儿对象仅移动到云端 `_archive/rt006-orphans-20260804`，操作可恢复。

证据：

- `runs/rt006-full-restore-a.json`
- `runs/rt006-full-restore-b.json`
- `runs/rt006-full-restore-v10.json`
- `runs/rt006-targeted-large-restore.json`

## 5. 测试与质量门禁

- Python 编译检查：PASS。
- 单元测试：89/89 PASS。
- Wiki lint：PASS；1,450 summaries、missing raw 0、invalid evidence quote 0。
- Wiki smoke：18/18 PASS。
- `git diff --check`：PASS。

## 6. Opus 5 独立评审

- 初审：FAIL，识别云端自证、索引发布、raw 版本绑定、恢复、verified 语义等 P0/P1。
- 二审：CONDITIONAL PASS，确认 P0 全部关闭，提出 live 默认、个人项目持续断言、retry/Top-8、catalog 裁剪 4 组 P1。
- 终审：PASS，确认 P1-A/B/C/D 全部关闭，未发现新的阻断级 P0/P1。

证据：

- `runs/rt006-opus5-final-review.md`
- `runs/rt006-opus5-final-review-v2.md`
- `runs/rt006-opus5-final-review-v3.md`

## 7. 结论与后续边界

Cloud-First 的正确性、证据、提交和恢复底座达到本 RT 验收标准，RT-006 完成。

本轮明确不删除本地 raw/wiki。当前 nightly 仍使用完整本地镜像作为运行期候选编译基线；若直接删除，本地空目录保护会让任务安全失败并持续红，不会损坏云端。要实现“云端唯一持久主库且本地无持久镜像”的日常运行，还需单独建设 cloud-only 增量编译/门禁路径。

legacy raw 的 `isSensitive=0` 是非阻断纵深防御整改项；现有访问控制由个人私有项目承担，新建 raw/分片已强制 `isSensitive=1`。若决定统一历史标签，应在删除本地副本之前完成。

# RT-064 复盘

## 目标达成了吗

达成。RT-055 台账族在 CI 上的假红已收口：缺第三方校验库或非苹果沙箱时改为跳过，
不再 ERROR/FAIL。本机有依赖时原用例仍全绿。

## 做对了什么

- 对齐仓库里已有写法（缺库 skip、非 darwin skip），不引入新依赖。
- 用「屏蔽 jsonschema 的模拟解释器」复现 CI 条件：130 条 OK、26 skip、0 红。
- 聚合退出码 3 确认为缺库连锁，对相关用例一并跳过，无需改产品代码。

## 踩到什么

- 模块顶层 `import jsonschema` 会让整文件加载失败（显示成 `_FailedTest`），
  必须先改成可缺失导入，行内 skip 才够。

## 留给下一次

- 访问审计（时间/令牌/库/结果码）与同事安全报到，按产品主线继续，不在本 RT。

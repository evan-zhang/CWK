# RT-023 独立验收报告

> 状态：NOT RUN
> 结论：未验收（RT-023 独立验收本身尚未执行）。
>
> **本报告的结论不依赖 VG-D，也不得引用 VG-D 的状态。** 顺序恒为
> T023-11 独立验收 PASS → 激活 receipt → T023-12a 的 VG-D → RT-026；把 VG-D
> 写进本报告的判定条件会让 VG-D 既在这份报告之后、又是它的前置，构成环。
> VG-D 是 RT-026 的**同级独立输入**，不是 RT-023 独立验收的入口条件。

独立验收尚未执行。最终报告必须证明测试发生在真实 Gateway 控制环境与真实
沙箱身份链，列出实际 transport、build、trust receipt、攻击命令摘要与结果。
以下任何证据均不能单独构成 PASS：mock metadata、fake AgentContext、进程内
HMAC signer、fake SpaceIndexProvider、宿主机直接调用 Broker。

验收必须逐条映射 AC-023-01～09 及 09a，并明确 non-production admission adapter 不被
用于 VG-D/G5 production claim，以及是否允许进入 RT-024/G5。

## 本验收 Agent 必须签发的 PR-001 receipt（当前均未生成）

本报告的验收 Agent（T023-11），而非实现 Agent，是以下两份 receipt 的签发者。二者
不是 RT-023 的完成条件，而是本报告判定 PASS **之后**的产出物。

**本文件被激活 receipt 按 sha256 绑定（`independent_acceptance_report` 角色），
因此它必须在 receipt 签发之前冻结，之后永不再编辑。** 事后补写会改变它的 sha256
并使已签发的 receipt 失配；需要在 receipt 之后记录的内容一律写进《交付验证报告》，
那是一份不被任何 receipt 绑定的状态产物。本报告因此**不引用** `receipt_sha256`。

| receipt | schema | 路径 | 任务 | 状态 |
| --- | --- | --- | --- | --- |
| `gateway-identity-transport` 能力激活 | `cwk.pr001.capability_activation_receipt.v1` | `PR/PR-001-multitenant-knowledge-spaces/capability-receipts/gateway-identity-transport/receipt.json` | T023-13 | **NOT GENERATED** |
| RT-023 安全门 | `cwk.pr001.security_gate_receipt.v1` | `PR/PR-001-multitenant-knowledge-spaces/security-receipts/RT-023/receipt.json` | T023-14 | **NOT GENERATED** |

独立验证义务：

- 上表两份是**仓库级 PR-001 证据**，与 §5 的运行时凭据
  `cwk.rt023.capability_receipt.v1` 是不同对象，domain separator 与路径均不同，
  验收时不得互相代替或混引。
- `verifier` 填本验收 Agent，且**严格不等于** `producer`。
- 激活 receipt 的 `evidence_refs` 必须精确覆盖 5 个角色
  （`controlled_transport_end_to_end`、`identity_spoof_rejection_matrix`、
  `production_trust_store_verifier`、`sandbox_isolation_scan`、
  `independent_acceptance_report`），每条为仓库相对普通文件并附匹配 sha256；
  **任何角色都不得指向 VG-D receipt**。
- 安全门 receipt 的 `claims` 必须与 registry 中 RT-023 的集合逐字相等
  （SGC-023-01/SG-00、-02/SG-02、-03/SG-03、-04/SG-07），
  `filesystem_policy.mode = applicable` 故**无 N/A**；SG-03 须逐类给出
  `path_traversal`、`symlink_component`、`symlink_leaf`、`hardlink`、`toctou`、
  `special_file` 的真实沙箱运行面与可执行用例引用。
- `tests_run > 0`、`tests_failed = 0`、`artifacts` 非空且 sha256 逐条匹配；
  `tested_subject_commit` 为被测候选 commit（evidence-only commit 的祖先），
  **不要求**等于 RT-026 最终 HEAD。
- 激活 receipt 有效期须满足 `0 < expires_at - created_at ≤ 2592000s`（30 天）。
- **blocked 时的正确产出是不生成 receipt**；届时 RT-026 判 NOT_RUN / NO_GO。

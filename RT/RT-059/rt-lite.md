# RT-Lite: RT-059 - 知识库服务官网 v1（产品站 + 站内控制台入口）

> profile: Spec-Lite | execution_mode: collaborative

## 方案（给人看）

- 做什么：把 RT-058 那个单页门户升级成一个像样的产品官网——讲清楚这个服务是什么、
  现在有哪几个库、能做什么、怎么接入、常见问题，并在站内保留控制台入口
  （点开跳到控制台，输 `12345678` 解锁）。
- 为什么：现在的门户只有一屏——三张库卡片、三步接入、一个按钮。它能证明"通了"，
  但拿给同事看不足以让人明白这东西是干什么的、值不值得接。
- 代价：接入手册进了页面，页面和 `docs/cwk-kb-access-setup.md` 从此有两处要同步。
- 这次故意不做什么：不做真实建库、摄取、用户管理和授权签发（那是后面的阶段）；
  不做公网暴露；不引入前端框架或构建步骤，仍是零依赖的服务端渲染 + 原生 JS。
- 用户怎样算成功：从局域网打开官网，不看技术细节也能说清楚"这是什么、我要不要接"；
  一个没用过的同事能照着页面把安装和配置全部做完；点控制台入口能进去并用 `12345678` 解锁；
  页面上不出现任何库内数据（那条负向判据红得起来）。
- 建议：定论——控制台保持独立进程，站内入口是链接而不是内嵌。理由见"控制台入口"。

## 站点结构

单页长滚动，锚点导航：

1. **首屏**：一句话讲清它是什么，两个入口（开始接入 / 管理控制台）。
2. **能解决什么**：三个具体场景，让人对号入座。
3. **四项能力**：检索 / 问答 / 核验 / 授权，每项都写清楚它不做什么——后半句更重要。
4. **可接入的知识库**：库名与一句话说明，来自配置，无任何统计。
5. **怎么接入**：完整五步，每步带可复制命令、期望输出和失败处理；末尾附提问示例。
6. **边界与承诺**：只读、找不到就说找不到、可追溯、快照、库间隔离、令牌按实例。
7. **常见问题**：六条，覆盖零命中、401/403、耗时、令牌共用、数据是否外发。
8. **管理员入口**：控制台链接 + 需要密码的说明。

## 不显示任何库内数据（2026-09-16 修正）

初版方案打算在官网上显示三个库的实况文档数，并为此在控制台加一个公开摘要端点。
用户当场纠正：官网是**产品介绍页**，给还没用过的人看功能和用法，不是数据看板，
不显示真实库里的东西。

于是公开端点不做了，门户保持纯静态。好处是隔离这件事不需要额外判据去守——
它本来就什么都不读。判据改为反向断言：页面上不出现统计字段、不出现任何取数调用
（`fetch(`、`XMLHttpRequest`、`/api/*`、管理密钥头），库卡片的文字必须**恰好**等于
配置里的名字和说明，多一个字都说明有人往上加了东西。

## 控制台入口：为什么是链接不是内嵌

两种做法：

- **A（采纳）**：官网与控制台仍是两个进程，站内入口是一个链接，点开跳到控制台输密码。
- B：合成一个地址，控制台成为官网里的一个区域。

B 换来的只是少一个端口号，代价是官网进程必须持有登记表和审计路径——
"公开面零数据访问"这条唯一的结构性保证就没了。不值得。

## 假设与现状

- 关键假设：库名与说明本就写在 `docs/cwk-kb-access-setup.md` 和 `cwk-kb-query` skill 里，
  放上官网不引入新的信息类别；统计数字才是新类别，因此不放。
- 现状依据：`docs/cwk-kb-access-setup.md` 的阶段 A/B 全文（页面五步即由它改写而来）；
  `scripts/kb_portal.py` 的 `PortalApp.page()`；`tests/test_rt058_kb_console.py` 的
  `PortalIsolationTests`（导入面隔离判据）。

## 实现备注

- 只改 `scripts/kb_portal.py`（页面重写）并新增 `tests/test_rt059_kb_website.py`；
  `kb_admin.py` 一行未动。新增三个配置：检索地址、问答地址、仓库地址。
- 不能破坏的约定：门户不导入任何数据模块；控制台默认关闭与写动词占位不变；
  8787/8790 不受影响。
- 内部阶段：页面 + 判据 → 本机真实浏览器点一遍 → 部署 OPS → 局域网复验。

## 验证

- 新增判据（`tests/test_rt059_kb_website.py`，14 条）：五个步骤与关键命令在页面上、
  登记模板逐库带勾选框、服务地址来自配置、401/403 语义有解释、复制走非安全上下文兜底；
  反向面：无统计字段、无取数调用、库卡片文字与配置严格相等。
- 真实浏览器：本机起一份指向生产地址的官网，整页截图，实测复制按钮返回"已复制"，
  并逐块量过代码首行没有被复制按钮遮住（第一次量到遮挡是浏览器缓存，加参数重取后全清）。
- 部署后从局域网复验：官网可达、控制台入口可达并能用 `12345678` 解锁。

## 变更记录

- 用户能感到什么变化：有了一个可以直接发给同事的产品站——看完知道这东西能干什么，
  并且能照着把自己的安装配置全做完；管理员从同一个站点进控制台。

## 遗留事项

- 页面五步与 `docs/cwk-kb-access-setup.md` 是同一套流程的两份表述，改一处要同步另一处；
  长期应择一为准，另一处引用它。
- 页面无法说明"语料多久更新一次"——索引里没有生成时间戳，需要摄取侧补元数据。
- 控制台的单一共享密钥、无每人身份、无失败锁定问题不在本 RT 范围，随用户管理那段解决。

# RT-026 开发任务：试点准备、影子切换与回滚工具

- [ ] T026-01 冻结 switch/transition/diff/policy/go-no-go/rollback schema 和
  machine-readable gate DAG。DAG 的 RT-026 入边集合**只允许** VG-A～VG-E 与
  G1～G5；G6/G7 必须建模为 RT-026 的出边（下游），并加自动化断言证明图中
  不存在 RT-026→G6→RT-026 环。
- [ ] T026-01b 以 `contracts/gates/release_gate_registry_v1.json` 为**唯一** release gate
  输入索引：按其 `receipt_path` 读取 `release-gate-receipts/G{1..5}/receipt.json`
  （schema `cwk.pr001.release_gate_receipt.v1`），**恰好五份**，校验 schema、
  `receipt_sha256`（domain separator 为 `cwk-release-gate-receipt-v1\0`，与 VG 族**不同**，
  不得互换）、`prerequisite_refs[].ref_id` 与 registry `required_prerequisite_ids`
  **精确相等**（少一个和多一个同样非法）、每个 `ref_sha256` 重算一致、
  `producer != verifier`、`verifier_role` 与 registry 的 `producer_role` 一致、
  `artifacts[].sha256` 与磁盘一致、`feeder_rts_independent_pass`、`status`、
  `synthetic` 与 `conclusion` 上限。硬约束：RT-026 **只消费，不创建、不补写、不猜测**
  任何 release receipt；`receipt_path` 不存在即该 gate 为 NOT_RUN 并 fail closed。
  **绝不读取 G6 与 G7**：`release-gate-receipts/G6/receipt.json` 与
  `release-gate-receipts/G7/authorization.json` 不在输入集内，读取即构成自依赖环；
  registry 中两者的 `direct_rt_consumers` 均为空数组。**该根当前不存在**，
  因此本项现在恒为 NO_GO，且实现工作不得把它创建出来。
- [ ] T026-01a 以 `contracts/gates/gate_registry_v1.json` 为**唯一** VG 输入索引：
  按 `receipt_path` 读取 `cwk.pr001.verification_gate_receipt.v1` JSON，校验
  schema、`receipt_sha256`（domain-separated 重算）、`artifacts[].sha256`、
  `feeder_rt_independent_pass`、`status`、`synthetic` 与 `conclusion` 上限。
  硬约束：RT-026 **只消费，不创建、不补写、不猜测**任何 VG receipt；
  `receipt_path` 不存在即该 gate 为 NOT RUN 并 fail closed，不得从 Markdown 叙述、
  registry 或任何其他来源推断状态。VG-A 的 receipt 已在 Wave-0 生成
  （`gate-receipts/VG-A/receipt.json`，synthetic PASS / `conservative_unknown`），
  RT-026 不得重新生成或升级其结论。
- [ ] T026-01c 实现 synthetic gate 缺口判定器：机读
  `contracts/gates/synthetic_closure_map_v1.json`，对每个 `synthetic=true` /
  `conclusion=conservative_unknown` 的 current VG receipt 解析 `closure_mode`。
  `capability_activation_receipts`（仅 VG-A）：按 `activation_receipt_path` 读取
  `cwk.pr001.capability_activation_receipt.v1`，以合取条件判定 CLOSED——
  `status=pass` ∧ `synthetic=false` ∧ `owner_rt_independent_pass=true` ∧
  `conclusion=capability_activated` ∧ `receipt_sha256`（`cwk-capability-activation-receipt-v1\0`
  域分隔重算）一致 ∧ 全部 `artifacts[].sha256` 与磁盘一致；任一不成立即 OPEN ⇒ NO_GO。
  `non_synthetic_rerun`（VG-B～VG-E）：只有 current receipt 本身 `synthetic=false`
  才 CLOSED，**不得**用任何 activation receipt 顶替。硬约束：RT-026 只消费——
  不创建、不补写、不猜测任何 activation receipt，不轮换或归档任何 gate receipt，
  不修改 closure map；缺口 CLOSED 也**不得**改写 VG-A receipt 的 `synthetic`/
  `conclusion`，它必须仍以 scoped evidence + 显式生产 caveat 列出。
- [ ] T026-02 完成 RT-012 CLI provider slot 显式兼容修订与独立零漂移审查；
  追加 tenant CLI ordinal 2 evolution receipt/migration note并绑定 RT-019 receipt；
  未通过不得继续 release CLI，且不得修改中央 policy/schema/helper/guard 或旧 SHA 表。
- [ ] T026-03 实现 default-off CAS switch、receipt verification 和 crash-safe
  rollback/recovery。
- [ ] T026-04 实现独立 shadow schedule/runner，证明不修改 production cron、
  legacy response/writer 或 tenant 状态。
- [ ] T026-05 实现 RT-016 crosswalk 校验与 legacy/new 六层 query diff。
- [ ] T026-06 实现四类允许差异 predicate、全部 blocker 和 independent review
  receipt 校验；禁止 force override。
- [ ] T026-07 实现 go/no-go DAG、前置 receipt（VG-A～VG-E、G1～G5）与 capability
  receipt 验证、外部批准 measured-threshold policy。输入必须是中央计划 §4.11 的
  **封闭 10 项清单**（多一项少一项都要先改中央计划），并含 SG-00～SG-10 crosswalk
  聚合器：逐条校验 §5.1 矩阵中每个 SG 的 owner 证据存在，且
  path/symlink/hardlink/TOCTOU 为 receipt 或显式 `N/A + 理由`；SG-03 另需证明
  RT-017～RT-026 十个包逐个有覆盖。**只聚合不制造**——RT-026 不得代写或推断任何
  SG owner 证据，漏项按 NO-GO 处理，且需负向测试证明删除任一 SG owner 证据
  都会使结论降级。并加负向测试证明注入或移除 G6 receipt 都不改变结论。另加负向测试：任一 VG `receipt_path` 缺失、
  `receipt_sha256` 不匹配、artifact 漂移、`feeder_rt_independent_pass=false`、
  `status != pass` 或 `synthetic=true` 时，go/no-go 必须 fail closed 或降级，
  绝不因为存在同名 Markdown 而放行。**VG-A 的 `synthetic=true` /
  `conclusion=conservative_unknown` 必须作为生产 blocker / scoped evidence 显式列出**，
  不得被 RT-026 结论掩盖——缺口闭合之后也仍须列出。
  同时接入 T026-01c 的缺口判定：**正向**负载测试证明两份 activation receipt 齐备且
  全部有效时 VG-A 缺口转 CLOSED、`READY_FOR_G7_REVIEW` 可达（证明规则不是永久死角）；
  **反向**证明缺失、只有一份、自身 `synthetic=true`、`owner_rt` 不符、
  `owner_rt_independent_pass=false`、`conclusion != capability_activated`、
  `receipt_sha256` 漂移、artifact 漂移八类情形都退回 NO_GO；再证明 activation receipt
  不能闭合 `non_synthetic_rerun` 的 VG-B～VG-E，且闭合前后 VG-A receipt 字节不变。
- [ ] T026-08 实现 release provider、doctor/install dry-run 和默认关闭的
  nightly/legacy compatibility adapter；owner 实现路径冻结为 installer=`install.sh`、
  doctor=`scripts/cwk_doctor.py`，不得用 wrapper、别名或 evidence 路径替代；为
  `cwk_nightly_pipeline.py` 追加预声明 evolution receipt/migration note。
- [ ] T026-08a 在 default-off composition root 通过显式对象注入装配
  `CollectorJobProviderV1` 与 `ProjectorJobProviderV1`；fixture 中验证可注册、
  可回滚且 flag/allowlist 默认关闭，真实启用仍需 G7。
- [ ] T026-08b 在 `scripts/cwk_release_switch.py` 实现 neutral
  `PilotAdmissionProviderV1` 的唯一 production adapter/factory，三个 purpose
  只能构造绑定。从 switch、外部验真 G7、target environment/instance、
  component flags 与 tenant allowlist 派生严格九字段、TTL<=300s 快照；
  实现 persistent CAS revision high-water。off/shadow、坏配置、test signer、错 target/
  tenant/purpose、G7 过期/撤销/重放、flag 关闭和 rollback 后都 fail closed。
  adapter 不得写 G7/真实 credential/租户正文，也不得加入 T026-07 输入集。
- [ ] T026-09 实现合成双租户全链 E2E 与 restore 后 E2E，覆盖 cold reroute、
  revoke、fault isolation、backup、diff、rollback。
- [ ] T026-10 在 switch/rollback 各 crash 点做 recovery，复验 legacy read/nightly。
- [ ] T026-11 运行全库 unittest、Wiki smoke、nightly fixture、secret scan、
  production-state/no-cron/no-AppKey/no-Cloud/DocDB 变更检查；加入 PilotAdmission
  三 purpose、九字段/hash/TTL、revision high-water、default-off、G7 target/
  signer/revoke/expiry/replay、allowlist/flag、rollback 与 production-rejects-fixture 矩阵。
- [ ] T026-12 独立 Agent 复核所有前置 gate（VG-A～VG-E、G1～G5）、diff、policy、
  CLI 与 rollback，出具 **RT-026 自身**的 PASS/FAIL 独立验收报告；go/no-go 结论
  上限为 `READY_FOR_G7_REVIEW`。本任务**不签发 G6**。
- [ ] T026-13（RT-026 之外，PASS 后触发）由**新鲜**最终验收者消费 RT-026 的
  go/no-go 报告 + 独立验收 PASS + 全部前置 receipt，签发 G6 最终验收报告；G7
  release readiness 授权在 G6 之后由业务/安全批准人执行。

- [ ] T026-14 **RT-026 预检安全门 receipt（SG registry 声明的生产任务，`preflight-security`）。**
  由**独立预检验证者**（`independent_preflight_verifier`，既不是 RT-026 的实现者，
  也不是 T026-07 的 go/no-go 评估者）在实现候选 commit **冻结之后、只读 go/no-go
  评估器运行之前**的 `preflight_after_candidate_freeze` 阶段，按
  `cwk.pr001.security_gate_receipt.v1` 生成
  `PR/PR-001-multitenant-knowledge-spaces/security-receipts/RT-026/receipt.json`，
  consumer 恰为 `RT-026`（go/no-go）与 `G6`（final acceptance）。
  - **反自依赖（关键；接口级署名排除，不是 OS 级写拒绝）**：本 receipt 由 T026-07 的
    评估器**消费**；该评估器在**声明阶段**上不属于写阶段
    （`go_no_go_evaluation` 不在 `write_phase_allowlist` 内），且**不得是本 receipt 的
    署名作者**（`producer` 与 `verifier` 都不得等于注入的
    `go_no_go_evaluator_identity`）。它不得以 RT-026 最终 PASS、G6 或 G7 为前置，
    `independent_security_verification_pass` 只表示独立安全/预检结论，
    **不表示 RT-026 已 PASS**。相应地必须置 `evaluator_identity_excluded = true`——
    它是一条**必须与校验方重算结果一致的声明，本身不是证明**：校验方注入评估器身份
    重新计算排除性，声明与重算不一致即 fail closed。阶段序为
    候选冻结 → 预检签发 → 只读评估 → RT-026 独立验收 → G6，全程无环。
    以上全部为**接口级**结论；OS 级不可变执行（写拒绝证据）归口 **AC-026-11**
    （T026-15a～d），Wave-0 未证明。
  - `claims` 必须与 registry 中 RT-026 的集合逐字相等：`SGC-026-01`/SG-03、
    `SGC-026-02`/SG-10。
  - `filesystem_policy.mode = applicable`，因此**没有任何 claim 可以标 N/A**；
    SG-03 覆盖必须绑定 RT-026 自身的真实运行面（switch/rollback/diff/receipt 读写
    路径）与可执行用例引用，逐类给出 `path_traversal`、`symlink_component`、
    `symlink_leaf`、`hardlink`、`toctou`、`special_file` 六类证据，泛泛散文不算覆盖。
  - `verifier` 必须存在且不等于 `producer`；`tests_run > 0`、
    `tests_failed = 0`、`artifacts` 非空且逐条 sha256 匹配；
    `tested_subject_commit` 为冻结的候选 commit（evidence-only commit 的祖先），
    **不要求**等于 RT-026 最终 HEAD。该 tested subject 必须已经包含三个权威 owner
    普通文件：`install.sh`、`scripts/cwk_doctor.py` 与计划新增的
    `scripts/cwk_go_no_go_launcher.py`；任一缺失或路径被替代都不得签发该 receipt。
  - 未生成该 receipt 时，RT-026 在 SG 视角下为 NOT_RUN，go/no-go 只能给 NO_GO。
- [ ] T026-15 **go/no-go 的 SG 聚合器必须是只读的。** 它只按
  `contracts/security/security_gate_registry_v1.json` 声明的 `receipt_path` 读取
  RT-017～RT-026 十份安全门 receipt，**不得创建、补写、推断或修改任何一份**
  （包括 RT-026 自己的 `preflight-security` receipt）。
  - registry 是 config-not-state：其中不存在任何 `status`/`conclusion`/`passed`
    等可变字段，状态只能由 receipt 是否存在及 receipt 自身的
    `status`/`conclusion` 推导。
  - 加负向测试：删除任一 receipt ⇒ 该 RT 判 NOT_RUN 且结论降为 NO_GO；出现
    registry 未声明的额外 receipt、owner/path/claim 集合不符、覆盖缺任一
    filesystem 类、`tests_run = 0`、`artifacts` 为空、sha256 漂移，全部 fail closed。
  - **Wave-0 已证明的是接口级只读，不是 OS 级只读。** 现在成立的是：评估器把
    自己的 `go_no_go_evaluator_identity` **注入**校验，任何 `producer` 或
    `verifier` 等于该身份的 receipt 一律判 INVALID；`evaluator_identity_excluded`
    只是一个必须与该重算结果**一致**的声明，不再是证据本身；`go_no_go_evaluation`
    不在 `write_phase_allowlist` 内。这证明评估器不能是它所消费证据的**署名作者**，
    **不**证明该进程在操作系统层面无法写入仓库。任何文档、receipt 或报告都不得
    把前者说成后者。
  - [ ] T026-15a **可信启动器（trusted launcher）**：权威 owner 实现路径冻结为
    `scripts/cwk_go_no_go_launcher.py`（计划新增）；go/no-go 评估器只能由其安装到固定
    root-owned 执行路径的受信实例拉起。启动器负责施加写拒绝（只读挂载 / 只读 bind
    mount / seccomp 或等价沙箱能力），评估器自身**无权**解除该限制。仓库 owner 路径、
    运行时执行路径、版本与二进制 sha256 必须记录在运行证明里。启动器创建 OS-control
    evidence，**不是**读取封闭输入并产出 go/no-go 结论的只读评估器。
  - [ ] T026-15b **运行证明（run attestation）**：每次评估产出一份运行证明，
    记录启动器身份与 sha256、被施加的写拒绝机制、评估器身份、被评估的候选 commit、
    环境指纹与起止时刻。运行证明由启动器（而非评估器）签发，评估器不得自证。
  - [ ] T026-15c **前后精确清单（pre/post exact manifest）**：启动器在评估**前后**
    各对 `security-receipts/`、`gate-receipts/`、`capability-receipts/` 取一次
    **精确成员清单**（每个条目的相对路径 + sha256 + 类型；拒绝符号链接、硬链接、
    特殊文件、点文件与嵌套杂项）。两份清单必须**逐条相等**；任何新增、删除或字节
    漂移都判 NO_GO，而不是只判"疑似"。
  - [ ] T026-15d **写拒绝证据（write-denial evidence）**：在受限运行内实际尝试
    创建、改写、重命名与删除各一次，证明四种写操作都被内核/沙箱拒绝（EROFS/EPERM
    或等价），并把这些尝试与其错误码记入运行证明。"没有观察到写入"不算证据，
    必须是"写入被尝试且被拒绝"。
  - **owner-scope v2 任务映射**：`install.sh` 与 `scripts/cwk_doctor.py` 归 T026-08；
    `scripts/cwk_go_no_go_launcher.py` 归 T026-15a～d；三者的 tested-subject 存在性
    归 T026-14，并统一在 AC-026-11 的 OS-control 验收中核对。本映射不新增 AC、
    不签发 receipt，也不表示任何实现任务或 RT-026 已 PASS。
  - 上述 T026-15a～d 的验收归口到 **AC-026-11**（《需求契约》正式验收项为
    AC-026-01～11，不另设 AC 编号），是 RT-026 实现与独立验收的验收条件；
    在它们落地之前，Wave-0 的正确表述是"接口级署名排除已证明；OS 级不可变执行
    尚未证明"。

## 完成定义

只有 AC-026-01～11 全部有可复现证据、独立验收 PASS 且结论不越过
`READY_FOR_G7_REVIEW`，RT-026 才 completed。G6 签发在 RT-026 completed **之后**
由新鲜最终验收者执行，不是 RT-026 的完成前置；真实试点/G7/M4/M5 不在本任务内。

# RT-014 独立验收报告（agent-rt014-verify-opus）

> Verdict：**PASS**（含 3 项 Minor 观察，均非安全阻塞）
> 是否允许启动 **RT-015 / RT-016** 与开启 **VG-A**：**是**
>   （RT-014 依赖 RT-011+RT-012 已就绪，冻结公开面稳定；RT-015 的 grant/view/撤权
>   与 RT-016 的 legacy 影子迁移可基于 `SharedEvidenceStore` 公开 API 构建）
> 是否允许宣称 **G3 / VG-A / M2 / 生产迁移** 已完成：**否**
>   （G3 要求 RT-014+015+016 全部通过独立验收；VG-A 要求同租户 A/B canonical 共享 +
>   view 隔离 + 撤权即生效 receipt；M2 需要真正的 legacy raw 影子导入。本 RT 仅完成
>   共享不可变证据存储的 store 部分，不涵盖 grant/view/legacy）
> 是否允许升级 `verified_shared_extensions_vN`：**否**（属 RT-023 / VG-D / G5）
> 是否允许修改 RT-011 / RT-012 / RT-013 冻结产物：**否**
>   （14 张 RT-011 v1 schema、`security_defaults.json`、
>   `verified_shared_extensions_v1.json`、`ADR-0001`、`cwk_pr001_*.py`；
>   RT-012 `cwk_atomic_file.py` / `cwk_instance.py` / `cwk_tenant_registry.py` /
>   `cwk_tenant_cli.py` / `cwk_tenant_cli_api.py` / `cwk_tenant_cmd_core.py`；
>   RT-013 `cwk_agent_binding.py` / `cwk_agent_context.py` /
>   `cwk_credential_broker.py` / `cwk_tenant_cmd_binding.py` 与 rt013 schemas
>   全部保持字节漂移 = 0）
> 验收 Agent：`agent-rt014-verify-opus`（独立、未参与实现；仅新增本报告一个文件）
> 分支：`agent/cwk-rt014-verify-opus`（git worktree）
> 唯一新增文件：`RT/RT-014/reports/独立验收报告.md`

## 1. 基线与环境

| 项 | 值 |
| --- | --- |
| 验收 worktree | `/Users/evan/.openclaw/gateways/life/state/workspace-life/projects/CWK/.worktrees/openclaw-worktree-cwk-rt014-verify-opus` |
| RT-014 impl HEAD | `d658b05a6886ca71b344147b28aec4857d874bed`（"RT-014: shared immutable Canonical Evidence Store"，唯一 RT-014 commit） |
| RT-014 前基线 | `afdc37e9bf52984976cc6536526f8301b706f2f8`（"RT-013: record independent acceptance"） |
| RT-013 独立 PASS | `95e84cc` + `19f2e37` + `afdc37e` |
| RT-012 独立 PASS | `1894576` |
| RT-011 独立 PASS r2 | `e3aa3e0`（含 remediation `eba42cc`） |
| Python | `Python 3.11.14`（`/opt/homebrew/bin/python3.11`） |
| Platform | `Darwin arm64 25.5.0` |
| 依赖 | 仅 stdlib（`base64/errno/hashlib/json/os/re/stat/datetime/dataclasses/typing/contextlib`）；RT-014 内部只从 `cwk_pr001_contracts` / `cwk_atomic_file` / `cwk_instance` import 公开面。`grep` 已确认无第三方 pip 依赖，未接 Cloud/DocDB/工作协同/cron/真实 Gateway。 |

写入策略：**仅新增本报告**；未编辑任何 RT-011 / RT-012 / RT-013 / RT-014 的
实现代码、schema、fixture、测试、PRD、DESIGN、plans、RT index、legacy 或
`.serena/`/`.spec-workflow/` 文件。所有攻击夹具均由本 Agent 在 `mktemp -d`
临时目录（`/tmp/rt014-verify.CnPlCr/`）里构造，验收结束后 `rm -rf` 清理。全程
**未读取真实 `CWORK_APP_KEY`**、未读 `.env`、未打开 `registry/credentials/`
或 `registry/agent-bindings/`、未访问 `tenants/`（RT-014 边界不涉及）。

## 2. Scope / 契约漂移核验

### 2.1 RT-011 / RT-012 / RT-013 冻结产物零字节漂移

`git rev-parse afdc37e:<path>` vs `git rev-parse d658b05:<path>` 对以下 14 个
Python 模块 blob 全部命中相同 object SHA：

- **RT-011**：`cwk_pr001_contracts.py`、`cwk_pr001_probes.py`、
  `cwk_pr001_view_compare.py`、`cwk_pr001_cli.py`
- **RT-012**：`cwk_atomic_file.py`、`cwk_instance.py`、`cwk_tenant_registry.py`、
  `cwk_tenant_cli.py`、`cwk_tenant_cli_api.py`、`cwk_tenant_cmd_core.py`
- **RT-013**：`cwk_agent_binding.py`、`cwk_agent_context.py`、
  `cwk_credential_broker.py`、`cwk_tenant_cmd_binding.py`

`git ls-tree -r afdc37e -- <dir>` vs `git ls-tree -r d658b05 -- <dir>` 对以下
schema/ADR/manifest 目录整目录零漂移：

- `PR/PR-001-multitenant-knowledge-spaces/contracts/schemas/`（14 张 RT-011 v1 schema）
- `PR/PR-001-multitenant-knowledge-spaces/contracts/rt012/`（5 张 RT-012 v1 schema）
- `PR/PR-001-multitenant-knowledge-spaces/contracts/rt013/`（5 张 RT-013 v1 schema）
- `PR/PR-001-multitenant-knowledge-spaces/contracts/adr/`（`ADR-0001-*`）
- `PR/PR-001-multitenant-knowledge-spaces/contracts/security_defaults.json`
- `PR/PR-001-multitenant-knowledge-spaces/contracts/verified_shared_extensions_v1.json`

（验收命令：`test_A8_frozen_rt011_012_013_zero_drift`、`test_A8_2_frozen_schemas_zero_drift`，均 PASS）

### 2.2 RT-014 impl 变更清单（`afdc37e..d658b05`）

```
scripts/cwk_shared_evidence.py                                                       1196 lines (new)
PR/PR-001-multitenant-knowledge-spaces/contracts/rt014/schemas/catalog_head.schema.json    48 lines (new)
PR/PR-001-multitenant-knowledge-spaces/contracts/rt014/schemas/publish_receipt.schema.json 42 lines (new)
PR/PR-001-multitenant-knowledge-spaces/contracts/rt014/schemas/recovery_report.schema.json 51 lines (new)
PR/PR-001-multitenant-knowledge-spaces/contracts/rt014/schemas/report_version.schema.json  44 lines (new)
tests/test_rt014_shared_evidence.py                                                  517 lines (new)
tests/test_rt014_attacks.py                                                          496 lines (new)
tests/test_rt014_concurrency.py                                                      317 lines (new)
RT/RT-014/rt-intake.md                                                               164 lines (new)
RT/RT-014/specs/需求契约.md                                                          102 lines (new)
RT/RT-014/specs/技术方案.md                                                          267 lines (new)
RT/RT-014/tasks/开发任务.md                                                           17 lines (new)
RT/RT-014/reports/实现记录.md                                                        104 lines (new)
RT/RT-014/reports/交付验证报告.md                                                     93 lines (new)
RT/index.yaml                                                                         +1 line
```

`git diff --stat afdc37e..d658b05 -- Makefile install.sh .env.example config/
knowledge/ .serena/ .spec-workflow/ RT/RT-001..RT-013 PR/PR-001-multitenant-knowledge-spaces/PRD.md
PR/PR-001-multitenant-knowledge-spaces/DESIGN.md PR/PR-001-multitenant-knowledge-spaces/plans/开发计划.md`
→ 空。

`git diff afdc37e..d658b05 -- RT/index.yaml` 仅新增一行：
```
+| RT-014 | PR-001 共享不可变 Canonical Evidence Store (G3) | implementation_done | Spec-Standard | 2026-08-19 |
```
未把 RT-014 标记为 completed，与"不自我验收"契约一致。

### 2.3 未触碰 legacy / 生产 / 密钥 / RT-013 面

`git diff --stat afdc37e..d658b05` 显示的所有新文件均位于 RT-014 边界内：

- **未修改** `scripts/cwk_collect_live.py` / `cwk_nightly_pipeline.py` /
  `cwk_wiki_query.py` / `cwk_wiki_search_index.py` / `cwk_entity_catalog.py` /
  `cwk_sync_mirror_to_docdb.py` / `cwk_raw_store.py` / `cwk_collection_state.py` /
  `cwk_thread_timeline.py` / `Makefile` / `install.sh` / `.env.example` /
  `config/` / `knowledge/`（`test_A8_3_legacy_files_untouched` 全项 PASS）。
- **未修改** RT-013 dispatcher slot 或任何 RT-013 provider：
  `git diff afdc37e..d658b05 -- scripts/cwk_tenant_cli.py scripts/cwk_tenant_cmd_binding.py scripts/cwk_agent_binding.py scripts/cwk_agent_context.py scripts/cwk_credential_broker.py`
  → 空。
- **未新增** cron / feature flag / DocDB / Cloud / 真实 Gateway 通道；
  `grep -R "crontab\|cron\.d\|systemctl\|FEATURE_FLAG\|DocDB\|s3\.\|gcs\.\|cwk_agent_binding\|cwk_credential_broker\|CWK_CRED_"`
  在 RT-014 shipped 源码里的匹配全部命中"never / does not / forbidden / not read"
  等否定语义上下文（`test_A9_1_no_secrets_in_shipped_files` PASS，含 whitelist
  规则）。
- **未修改** `.serena/` / `.spec-workflow/`（`git diff --stat` 空）。

`grep -n "CWORK_APP_KEY\|os\.environ\|os\.getenv\|CWK_CRED\|password\|secret[^a-z]\|credential" scripts/cwk_shared_evidence.py`
只在下列位置命中：

- L28、L35、L41 **docstring**："Never reads `CWORK_APP_KEY` or any credential material."
- 无任何真实 `os.environ[...]` / `os.getenv(...)` 调用；无凭据引用；无 `.env`
  fallback；无 `secret`/`password` 字面量。

## 3. 命令与实际输出（复现指纹）

### 3.1 编译 / 空白 / diff

```
python3.11 -m compileall -q scripts tests    # exit 0
git diff --check                              # exit 0
git status --short --untracked-files=all      # (empty)
```

### 3.2 RT-014 定向测试（62 tests）

```
python3.11 -m unittest discover tests -p "test_rt014_*.py"
# → Ran 62 tests in 4.122s — OK
```

对齐实现方 `实现记录.md` §"测试矩阵与结果"（31 shared_evidence + 23 attacks +
8 concurrency = 62 tests）。

### 3.3 RT-011 / RT-012 / RT-013 定向回归

```
python3.11 -m unittest discover tests -p "test_pr001_*.py"    # 124 OK
python3.11 -m unittest discover tests -p "test_rt012_*.py"    # 111 OK
python3.11 -m unittest discover tests -p "test_rt013_*.py"    # 121 OK
```

三条回归全部 PASS，与 RT-011/012/013 三份 verify 报告基线数字一致。

### 3.4 全量 Python 3.11 回归

```
python3.11 -m unittest discover -s tests -p 'test_*.py'
# → Ran 693 tests in 31.804s — OK (skipped=7)
```

693 = 269（RT-001..RT-010）+ 124（RT-011）+ 111（RT-012）+ 121（RT-013）+
62（RT-014）+ 6 附加；7 skipped 与 RT-013 独立验收 §3.5 完全相同（条件式
fixture 缺失，与 RT-014 无关）。

## 4. 硬性黑盒攻击矩阵（十项，独立复现）

以下十类攻击全部在 `mktemp -d` 临时 instance root 里构造，走 `SharedEvidenceStore`
公开 API（`open / initialize / publish / read_version / recover`），不复用
实现方测试作为"通过"依据；每一项都由本 Agent 用独立脚本重跑并断言结果。所有
临时目录在验收结束时 `rm -rf` 清理。

### 4.1 T-11 / T-12 幂等 + 版本追加 + namespace/report 隔离 — **PASS**

- **A1-1 同 envelope 20 线程并发 publish**：`ThreadPoolExecutor(20)` × 相同
  `(source_namespace=cwork, report_id=2070001, body)` → 20 个 `PublishReceipt`
  全部指向 **同一 opaque `object_id`**；at-most-1 个 `is_new_version=True`；
  `shared/objects/**/o_*.json` 只落 **1 个** 文件；`shared/report-versions/<catalog_key>/catalog.jsonl`
  只有 **1 行**。（`test_A1_1_ab_concurrent_same_envelope` PASS）
- **A1-2 新版本追加保留旧对象**：同 `report_key`、不同 `body` → `object_id` 与
  `canonical_sha256` 均变化；`is_new_report=False`；旧对象仍可通过 `read_version`
  回读原正文；磁盘上出现 2 个 `o_*.json`。（`test_A1_2_new_version_preserves_old`
  PASS）
- **A1-3 namespace 隔离**：`cwork:2070001` vs `other_ns:2070001` 相同正文 →
  两个 `catalog_key`、两个 `object_id`、两个 `catalog.jsonl`；`canonical_sha256`
  因 `source_namespace` 参与哈希而不同。（`test_A1_3_namespace_isolation` PASS）
- **A1-4 report_id 隔离**：`cwork:rep-A` vs `cwork:rep-B` 相同正文 → 两个
  `catalog_key`、两个 `object_id`。（`test_A1_4_report_id_isolation` PASS）
- **A1-5 report_key 交叉读拒绝**：`read_version(rb.report_key, ra.canonical_sha256)`
  → `not_found` 或 `report_key_mismatch`。（`test_A1_5_readback_report_key_mismatch`
  PASS）
- **A1-6 report_id 从不落盘**：以 `report_id=2070001-SECRET-ID` publish 后，
  遍历 `shared/**`，所有 path segment 均不包含 `2070001-SECRET-ID` 或
  `cwork`。目录结构完全 opaque（`objects/<2字符shard>/<object_id>.json`；
  `report-versions/<r_...>/`）。（`test_A1_6_report_id_not_in_paths` PASS）

### 4.2 T-08 / T-09 深层 tenant/view/reply/attachment/URL/credential 注入 + 伪造 verified_shared — **PASS**

- **A2 顶层 36 项禁止字段**：`tenant_id / agent_id / agent_id_hash /
  credential_ref / credentials / app_key / lane / read_status / todo_status /
  allowed_actions / role / roles / reply / replies / reply_overlay / node /
  nodes / node_overlay / attachment / attachments / attachment_permissions /
  attachment_url / preview_url / short_url / presign_url / download_url /
  temporary_url / collected_at / observed_at / path / absolute_path /
  mirror_root / auth_epoch / binding_epoch / cookie / session_token /
  authorization` 全部 → `SharedEvidenceError(code="contract")`；发布前后
  `shared/**` 文件树 snapshot **字节完全一致**（`before == after`）。
- **A2 嵌套 author 深扫**：把 `tenant_id / credential_ref / temporary_url /
  attachment / reply / cookie` 藏进 `author` 子对象；RT-011 深扫依然拦截，
  发布未产生任何文件。
- **A2 伪造 `verified_shared_extensions_ref`**：`bad version / bad sha /
  extra key / missing sha / missing version / not-a-dict` 六种伪造均
  `contract` 拒绝；发布未产生任何文件。
- **A2 未列入 `additionalProperties: false` 的顶层字段**（`attachments_urls /
  download / presign / s3_url / cdn_url / cwork_download_link / malicious_field`）
  → 全部 `contract` 拒绝。
- **A2 schema id 篡改**：`schema: cwk.canonical_report.v2` → `contract` 拒绝。
- **A2 非 dict envelope**（`str / int / None / list / tuple / float`）→ `contract`
  拒绝，未触及文件系统。

（`test_A2_forbidden_top_level_fields`、`test_A2_forbidden_nested_author`、
`test_A2_forbidden_verified_shared_extensions_ref`、
`test_A2_temporary_url_variants_deep`、`test_A2_wrong_schema_id_rejected`、
`test_A2_non_dict_envelope` 六项 PASS）

### 4.3 T-04 / T-11 对象/catalog/SHA/JCS 字节篡改 → reader fail-closed + 错误 opacity — **PASS**

11 项独立攻击全部 PASS：

- **A3-1 object 字节翻转 1 bit** → `sha_mismatch`；错误消息不含 tmp 路径、
  正文、`2070001`。
- **A3-2 object 序列化 pretty-print**（改 key 顺序、加空白）→ `sha_mismatch`
  或 `canonical_drift`。
- **A3-3 object 截断到 0 字节** → `sha_mismatch` / `contract` / `canonical_drift`
  之一（fail-closed）。
- **A3-4 A 对象字节替换成 B 对象字节** → `sha_mismatch`（catalog 里记录的
  `object_bytes_sha256` 与新字节不符）。
- **A3-5 catalog.head 翻转 1 bit** → `corrupt_catalog`（head 字节非 canonical
  JCS 或 schema 违规）。
- **A3-6 catalog.jsonl entry.object_id 换成另一对象** → `corrupt_catalog` /
  `sha_mismatch` / `report_key_mismatch`（head.catalog_jsonl_sha256 也不再匹配）。
- **A3-7 head.report_key 篡改** → `report_key_mismatch` 或 `corrupt_catalog`。
- **A3-8 未知 report_key 读取** → `not_found`；错误不泄露原始 report_id。
- **A3-9 未知 sha 读取** → `not_found`；错误不泄露 tmp 路径。
- **A3-10 catalog.jsonl 去掉末尾换行** → `corrupt_catalog`。
- **A3-11 删除 object 文件** → `orphan_object` 或 `not_found`。

reader 每次调用都**独立完成 5 层校验**（catalog.head schema/JCS →
head.catalog_jsonl_sha256 vs 实际字节 → 目标 entry 的 report_key 一致 →
object 字节 SHA vs entry.object_bytes_sha256 → object body 的
`canonical_sha256` 字段 + `canonical_json_bytes(payload)==raw` +
`compose_report_key(payload.namespace, payload.report_id)==请求 report_key`），
任一失败一律 `SharedEvidenceError`。

### 4.4 T-04 路径 traversal / 绝对路径 / Unicode / soft-link / hard-link / TOCTOU / staging prefill — **PASS**

10 项独立攻击全部 PASS：

- **A4-1 report_id 内插 `../`、`/etc/passwd`、`id/with/slash`、`id with space`、
  `id\x00nul`、`id\nnewline`、`.hidden`、`-startdash`** → `contract` 拒绝。
- **A4-2 namespace 内插 `/etc`、`cwork/../evil`、`CAPS`、`with space`、
  `0start`、`with-dash`、80+ 字符长度** → `contract` 拒绝。
- **A4-3 `read_version` 的 report_key 语法** 攻击（`../../etc/passwd`、
  `cwork:../../evil`、`/absolute`、`cwork:id\x00nul`、空串）→ `contract`。
- **A4-4 object 替换为 symlink 指向外部 canary 文件** → `contract / sha_mismatch /
  not_found / orphan_object` 之一；attacker outside file 未被读回。
- **A4-5 object shard 目录替换为 symlink** → `contract`（`_openat_dir_nofollow`
  的 `O_NOFOLLOW` 拒绝）。
- **A4-6 `report-versions/<catalog_key>/` 目录替换为 symlink** → `contract`。
- **A4-7 object 替换为 hardlink（`nlink>1`）** → `cwk_atomic_file.read_file`
  拒读，`contract` 或 `sha_mismatch`。
- **A4-8 staging 预填 `evil.txt / .cwk-tmp-attacker / .hidden`** → attacker 文件
  未被 write_atomic 覆盖（temp name 是 `secrets.token_hex`）；正常 publish 仍
  成功；attacker 文件保持原内容。
- **A4-9 outside marker 文件在 recover() 后未被删** → recover 只清理 opaque
  staging/report-versions 内的 `.cwk-tmp-*` 孤儿，绝不越界。
- **A4-10 `report_id="/etc/passwd"`** → `contract` 拒绝；`/etc/passwd` 未被打开
  或读取。

### 4.5 staging / object publish / catalog publish 的 fault injection + CAS conflict + 20+ 并发 — **PASS**

6 项独立攻击全部 PASS：

- **A5-1 30 线程同 envelope publish** → 1 object + 1 catalog entry；
  `object_ids` 集合唯一。
- **A5-2 20 线程 20 不同版本 publish**（同 report_key）→ 20 objects + 20
  catalog entries；每条都可通过其 receipt readback。
- **A5-3 20 线程 20 不同 report_id publish** → 20 catalogs + 20 objects。
- **A5-4 fault injection：对象已写入后手工删除 catalog.head + catalog.jsonl**
  → `recover()` 报告 `orphan_object` issue；对象文件**未被 recover 删除**；
  再次 publish 相同 envelope 会创建**新的 object_id**（因为 catalog 已找不到
  幂等匹配），旧孤儿对象继续保留；磁盘上共 2 objects，catalog 只引用最新一个。
- **A5-5 staging 与 per-report `.cwk-tmp-*` 孤儿** → `recover()` 通过
  `_A.recover_orphans` 幂等清理；已提交对象未被触动；`RecoveryReport.
  staging_orphans_removed` 至少 1 项；readback 仍成功。
- **A5-6 healthy state 上 recover() 幂等**：`recover()` 前后遍历 shared/**
  逐文件 sha256 比对，**bytes-identical**；`catalog_issues=[]`；
  `objects_verified` 与 `catalog_dirs_scanned` 值等于实际数量。

**永不出现"半引用" catalog**：所有 `catalog.head + catalog.jsonl` pair 要么都
是旧完整快照，要么都是新完整快照（`cas_write` 顺序：先写 jsonl 全文件重写、
再写 head，两者都携带 `expected_previous_sha256`；任一 CAS 冲突抛
`catalog_conflict`）。

### 4.6 T-12 catalog-key 不直接暴露 report_id、object id opaque、无 list/get-by-sha/existence probe — **PASS**

- **A6-1** `dir(SharedEvidenceStore)`（过滤前导下划线）**精确等于**
  `{initialize, open, publish, read_version, recover}`。所有 46 项 forbidden
  method name（`rollback / delete / delete_object / unlink / truncate / purge /
  list / list_objects / list_versions / list_report_keys / enumerate / iter_objects /
  iter_reports / iter_catalog_keys / all_objects / objects / reports / keys /
  scan / walk / search / exists / has / has_version / has_report / has_object /
  object_exists / report_exists / sha_exists / contains / probe / peek / sniff /
  get_by_hash / get_by_sha / find_by_sha / resolve_hash / sha_to_object /
  hash_to_object / reverse_lookup / read_path / write_path / read_absolute /
  read_file`）**一项都不在 store 或 module `__all__` 中**。
- **A6-2** `cwk_shared_evidence.__all__` **精确等于**
  `{CATALOG_KEY_REGEX, PublishReceipt, RecoveryReport, SharedEvidenceError, SharedEvidenceStore, SHARED_CHILDREN}`（6 项，含 3 个 dataclass/error 类 + 1 个
  正则 + 1 个常量 + 1 个入口类）。
- **A6-3** `scripts/` 下**没有** `cwk_tenant_cmd_rt014*.py` 或
  `cwk_shared_evidence_cli.py`；`cwk_tenant_cli.py` 的 `FROZEN_PROVIDER_SLOTS`
  未新增 shared_evidence provider（复检 zero-drift）；因此 RT-014 **不向
  沙箱、CLI、HTTP 或任何外部通道暴露枚举 / 探测 / 回滚接口**。
- **A6-4** `dir(PublishReceipt)`（过滤前导下划线）只包含 8 个 dataclass
  字段 + `to_dict`；无 `delete / rollback / unpublish`。

Minor-1（见 §5）：module 私有函数 `_collect_referenced_object_ids` 在
`dir(cwk_shared_evidence)` 中可见（Python 私有约定 = leading underscore），
但**不在** `__all__` 中，且明确注明"private helper (module-private, not part
of the store's public API): used solely by SharedEvidenceStore.recover to
detect orphan objects"。它接受 `(store, sfd, catalog_keys)` 参数，需要
调用方持有一个已打开的 shared/ dir FD，普通外部 caller 无法合法构造该 FD。
非安全阻塞，属命名/封装建议。

### 4.7 canonical catalog 的 report_key/version/object SHA/bytes consistency 和重复运行确定性 — **PASS**

- **A7-1** 同一 envelope 在两个独立 store 中发布 → **两个 object 文件字节
  完全一致**（对象 SHA 相同；opaque object_id 因 128-bit 随机而不同，但
  这是设计目标）。canonical JSON 由 RFC 8785 JCS + Unicode NFC 严格规定，
  哈希由 RT-011 `canonical_sha256` 计算，RT-014 内部再用 `canonical_json_bytes`
  重新序列化，因此**不同机器上生成的字节位模式相同**。
- **A7-2** `catalog.head.catalog_jsonl_sha256` **恒等于** `sha256(catalog.jsonl 字节)`；
  `entry_count` **恒等于** catalog.jsonl 的行数。
- **A7-3** `recover()` 幂等：连续两次调用返回结构相等的 `RecoveryReport.to_dict()`。
- **A7-4** `catalog.jsonl` 每行 bytes **恒等于** `canonical_json_bytes(json.loads(line))`
  —— 说明 catalog append 时使用了 JCS 序列化，reader 对每行都做 belt-and-braces
  的 JCS round-trip 校验。

### 4.8 RT-011/012/013 frozen contracts/dispatcher/binding/credential/legacy raw/collector/nightly/DocDB 零漂移 — **PASS**

见 §2.1 + §2.3。以下 3 项 verify test（在 `attack8_9_scan_and_drift.py` 中）
全部 PASS：

- `test_A8_frozen_rt011_012_013_zero_drift`（14 个模块 blob）
- `test_A8_2_frozen_schemas_zero_drift`（4 个 schema/ADR/manifest 目录）
- `test_A8_3_legacy_files_untouched`（9 legacy 模块 + Makefile + install.sh +
  .env.example）

### 4.9 stdout/stderr/receipts/catalog/tmp/git secret & sensitive scan；错误无 traceback/host path/正文/tenant/agent/credential — **PASS**

5 项独立攻击全部 PASS：

- **A9-1 shipped 源码 secret 扫描**：`scripts/cwk_shared_evidence.py`、3 份
  RT-014 tests、4 张 rt014 schema 逐文件 grep 18 项 forbidden pattern
  （`os.environ[CWORK_APP_KEY]`、`os.getenv`、`os.environ.get`、`read_credential`、
  `lease(`、`CWK_CRED_`、`DocDB`、`cloud_upload`、`gcs\.`、`s3\.`、`crontab`、
  `cron\.d`、`systemctl`、`cwk_agent_binding`、`cwk_credential_broker`、
  `cwk_tenant_registry`、`cwk_raw_store` / `cwk_thread_timeline` /
  `cwk_collect_live` / `cwk_nightly_pipeline` / `cwk_sync_mirror_to_docdb`），
  **所有命中均在"never / does not / not read / forbidden"上下文里**（whitelist
  正则 `(never|does not|not\s+read|forbidden|not\s+call|not\s+access|not\s+directly|proscribed|refuses|refuse)`）。
- **A9-2 publish + read_version + 恶意 publish 三条路径的 stdout/stderr canary**
  扫描：注入 `body="SUPER-SECRET-BODY-CANARY-XYZ123"`；重定向 stdout/stderr
  到 StringIO；触发 `sha_mismatch`（wrong sha readback）与 `contract`
  （tenant_id 注入）异常路径。**canary 从未出现在 stdout/stderr**。
- **A9-3 磁盘 canary 隔离**：`CANARY-BODY-ONLY-IN-OBJECT-e7f8a9` 应该 **只**
  出现在唯一的 `shared/objects/**/o_*.json` 里（这本身就是 canonical evidence
  的持久化位置）；对 `shared/report-versions/**/catalog.head` 与
  `catalog.jsonl` 全部 grep，**0 命中**（catalog 只存 SHA/opaque ID/时间戳/
  版本号，不复制正文）；对 `PublishReceipt.__dict__` 序列化，**0 命中**；对
  `shared/**` 所有 path segment 扫描，`2070001` **0 命中**（report_id 从不
  出现在文件系统 path segment）。
- **A9-4 错误 opacity**：`sha_mismatch` 异常的 `str(exc)` **不含** tmp 绝对
  路径、`Traceback`、`File "` 或 report_id `2070001`。异常 `__str__` 只由
  `code` 前缀 + 最小 opaque token（`catalog_key` / `object_id`）组成。
- **A9-5 `git diff --check`** exit 0（无 whitespace/CRLF 冲突）。

### 4.10 RT-014 定向、RT-011/012/013 回归、全量测试和 git diff --check — **PASS**

复现见 §3。总计 693 tests OK（skipped=7）；无任何失败或错误。

## 5. Blocker / Major / Minor 汇总

### Blocker — 无

未复现任何：
- 跨 report_key 泄漏或对象合并（namespace/report_id 完全隔离，见 §4.1）；
- CanonicalEnvelope 深层禁止字段绕过（RT-011 深扫全部拦截，见 §4.2）；
- 篡改后 reader 返回半可信正文（每次 read 走 5 层校验，见 §4.3）；
- symlink/hardlink/TOCTOU 逃逸出临时 root（O_NOFOLLOW + nlink 检测，见 §4.4）；
- 并发下双写或半引用 catalog（per-report `flock` + CAS，见 §4.5）；
- 枚举 / 探测 / 回滚 / 存在性侧信道（公开面仅 5 method，见 §4.6）；
- 非确定性字节 / 版本身份错乱（JCS/NFC + catalog SHA 双校验，见 §4.7）；
- RT-011/012/013 frozen 契约或 legacy 面漂移（14 模块 + 4 schema dir 零漂移，见 §2.1 §4.8）；
- AppKey / raw / traceback / host path / report_id / tenant 泄漏（磁盘全扫 0 命中，见 §4.9）。

RT-014 的所有关键安全不变量与 PRD FR-06 / AC-01 / NFR-03 / NFR-04、
DESIGN §C-07 / §17 / §T-04 / §T-11 / §T-12、`安全威胁模型.md` T-04 / T-11 /
T-12 冻结口径**逐条对齐**。

### Major — 无

未观察到 SG-01（共享存储）、SG-03（文件系统）、SG-08（故障隔离）任何 major
违反。RT-014 边界内的所有关键不变量均通过独立黑盒复核。

### Minor（不阻塞 RT-015 / RT-016 / VG-A；建议在下一 RT 或 RT-024 顺手闭合）

- **Minor-1（封装建议）**：`scripts/cwk_shared_evidence.py` 第 1157 行
  `_collect_referenced_object_ids(store, sfd, catalog_keys)` 定义为 module 级
  函数（下划线前缀 = Python 私有约定），需要调用方持有一个 shared/ dir FD
  才能合法调用，普通外部 caller 无法合法构造该 FD；已明确注释"private
  helper (module-private, not part of the store's public API)"。**非安全阻塞**，
  但为了满足"公开面严格最小"的更保守解读，建议 RT-024 或 RT-025 内联该函数
  为 `SharedEvidenceStore._collect_referenced_object_ids` 实例方法（内部
  helper 更清晰），或在 module 层显式 `del _collect_referenced_object_ids`
  之类的 lint hook。

- **Minor-2（recover() 的孤儿 catalog_key 占位）**：`recover()` 在标记
  orphan_object 时用 `"catalog_key": "r_" + "a" * 26` 作为占位（`shared_evidence.py`
  第 1147 行）。这是因为 orphan_object 的定义是"没有任何 catalog 引用它"，
  所以 `catalog_key` 逻辑上不适用；但 `recovery_report.schema.json` 强制
  `catalog_key` 必填 + 正则匹配。目前用一个语法上合法的常量占位。**非安全
  阻塞**（用户看到相同占位值代表"孤儿归属未知"），但审计者可能误以为它是
  某个真实 catalog。建议 RT-024 / RT-025 将 schema 中 `catalog_key` 改为
  `["string", "null"]` 或引入独立 `orphan_group` 语义，以避免误读。

- **Minor-3（`recover()` 对 corrupt_catalog 的 `object_id` 一律 null）**：
  当某个 catalog dir 的 head 缺失或 head/jsonl SHA 不符时，recover 只发出
  `{"code": "corrupt_catalog", "catalog_key": ..., "object_id": null}`，
  没有进一步描述"是缺 head 还是缺 jsonl 还是 SHA drift"。**非安全阻塞**
  （fail-closed 是正确的，raw 不泄漏是正确的），但审计诊断能力有限。建议
  RT-024 引入 code 细化（`missing_head / missing_jsonl / head_jsonl_sha_drift /
  entry_count_drift`），或提供 admin-only 的深度诊断 CLI（受身份门禁）。

以上 3 项 Minor 均**非安全阻塞**，与 PRD FR-06 / DESIGN §C-07 / 威胁模型
T-12 冻结口径无冲突；不阻止 RT-015 / RT-016 启动与 VG-A 波次门禁。

## 6. 五类外部事实 fact_status（继续 conservative_unknown）

RT-014 明确**不涉及**外部事实证明（其边界是本地 store 层）；下列 fact_status
继续沿用 RT-013 独立验收 PASS 的 `conservative_unknown` 冻结：

- `trusted_agent_identity_openclaw_tool` = `conservative_unknown` — RT-023 / VG-D / G5
- `trusted_agent_identity_uds_peercred` = `conservative_unknown` — 同上
- `sandbox_transport_openclaw_tool` = `conservative_unknown` — 同上
- `sandbox_transport_uds` = `conservative_unknown` — 同上
- `sandbox_transport_loopback_http_self_reported` = `conservative_unknown（政策永禁）`
- `permission_authoritative_events` / `permission_authoritative_api` = `conservative_unknown`
- `report_id_global_uniqueness` = `conservative_unknown` — RT-014 通过
  `catalog_key = base32(sha256(domain-sep + report_key))` 让 report_id 全局
  唯一性成为**非必需条件**（`source_namespace + report_id` 组合即可）。
- `verified_shared_extensions_dual_user_sample` = `conservative_unknown` —
  RT-014 内的 `verified_shared_extensions_ref` 只做**语法校验** + `additionalProperties=false`
  拒绝伪造，**从不主动信任**任何 extension；深扫也从不放行 forbidden 字段。
- 每租户配额数值（`quota.limits.*`）全部 `null` — RT-024 测量、RT-026 冻结。

## 7. 与实现方 `交付验证报告.md` 的 receipt 对齐

| 实现方声明 | 独立复现结果 |
| --- | --- |
| §二 `python3.11 -m unittest discover tests -p "test_rt014_*.py"` = 62 OK | 复现，`Ran 62 tests in 4.122s OK`（略优于实现方的 4.384s，因缓存） |
| §二 `test_pr001*` = 124 OK | 复现，`Ran 124 tests in 1.018s OK` |
| §二 `test_rt012*` = 111 OK | 复现，`Ran 111 tests in 4.651s OK` |
| §二 `test_rt013*` = 121 OK | 复现，`Ran 121 tests in 18.813s OK` |
| §二 全量 = 693 OK (skipped=7) | 复现，`Ran 693 tests in 31.804s OK (skipped=7)` |
| §二 `git diff --check` 干净 | 复现，exit 0 |
| §二 `grep -RIn "CWORK_APP_KEY"` 只在"不读"注释里 | 复现，仅 3 处 docstring |
| §一 需求→实现→测试追溯 14 条 | 复核契约命令依次通过，与 §4 攻击矩阵对齐 |
| §三 新增/未修改文件清单 | 与 `git diff --stat afdc37e..d658b05` 完全一致（RT-014 新增 15 files，未修改 legacy / RT-011/012/013 frozen 或 dispatcher） |

## 8. 允许下一步（RT-015 / RT-016 / VG-A 准入）

RT-014 独立验收 **PASS**：

- 允许启动 **RT-015**（Access Ledger、Tenant View、撤权核心）—— 其 grant
  ledger 通过 `report_key` 引用 canonical evidence；tenant view 引用
  `canonical_sha256`，不复制正文；RT-014 的 `read_version` 已确保 raw 回读
  时校验 `report_key + canonical_sha256`，为 RT-015 的二次 ACL 提供了原子
  reader；
- 允许启动 **RT-016**（Legacy Raw 影子迁移与数据双读核验）—— 通过
  `SharedEvidenceStore.publish` 幂等接口写入 canonical envelope；crosswalk 用
  `legacy_source_sha256` + `canonical_sha256` 分别记录，RT-014 不承担 legacy
  转换；
- 允许开启 **VG-A 波次门禁**（同租户 A/B canonical 共享 + view 独立 + 撤权
  即生效） —— 前提是 RT-015 完成 grant/view 接入；RT-014 的 store 面已就绪；
- 允许保留 RT-011 冻结契约、RT-012 dispatcher / instance layout / atomic file
  primitives、RT-013 binding / credential / agent context / rt013 schemas
  的字节漂移 = 0；
- **不允许** RT-015 / RT-016 修改：
  - `scripts/cwk_shared_evidence.py`；
  - `PR/PR-001-multitenant-knowledge-spaces/contracts/rt014/schemas/*.json`；
  - `SharedEvidenceStore` / `PublishReceipt` / `RecoveryReport` /
    `SharedEvidenceError` 的公开面；
  - RT-011 / RT-012 / RT-013 任何 frozen artefact；
- **不允许** 关闭 SG-02 / SG-07 / G5 / G7；
- **不允许** 批准 `verified_shared_extensions_vN` 升级（属 RT-023 / VG-D / G5）；
- **不允许** 将 5 类外部事实标为 `verified`；本 RT 继续保持 `conservative_unknown`；
- **不允许** 宣称 **G3 / VG-A / M2 完成**（G3 需要 RT-014+015+016 全部独立验收 PASS
  加上 VG-A receipt；M2 需要真实 legacy raw 影子导入，属 RT-016）；
- **不允许** 触发 cron、真实 AppKey、DocDB、Cloud、真实 Gateway 或试点
  enablement（属 G7 独立授权）。

Minor-1 ~ 3 建议在 RT-024 或 RT-025 内顺手闭合，不作为 RT-014 阻塞。

## 9. 最终 Verdict

**PASS。**

RT-014 交付的 `SharedEvidenceStore` + 4 张 v1 schema + `catalog_key` opaque
派生 + per-report `flock` + `cas_write` catalog + 5 层 reader 校验 + `recover()`
只报告不删除，在**十个独立攻击类别全部 fail-closed**：

1. 幂等 + 版本追加 + namespace/report 隔离（20 线程同 envelope → 1 object；
   同 report_key 不同 body → 追加新 object 保留旧对象；不同 namespace/report_id
   隔离）；
2. 深层 tenant/view/reply/attachment/URL/credential/verified_shared 注入
   （36 项禁止字段 + 嵌套 author + 6 种伪造 extensions_ref + 未知顶层字段 +
   非 dict envelope 全部拦截）；
3. 对象/catalog/SHA/JCS 字节篡改（11 项：bit flip / prettify / truncate /
   swap / head bit flip / entry object_id swap / head report_key 篡改 /
   未知 key / 未知 sha / 换行删除 / 对象缺失）；
4. 路径 traversal / 绝对路径 / Unicode / soft-link / hard-link / TOCTOU /
   staging prefill（10 项，包含 object symlink 到外部 canary、shard/catalog
   dir 替换为 symlink、object 替换为 hardlink、staging 预填 attacker 名字、
   outside marker 未被 recover 删除）；
5. 20+ 线程并发 + fault injection + CAS conflict（6 项：30 线程同 envelope、
   20 线程 20 versions、20 线程 20 reports、object 已写但 catalog 消失后
   recover 报告 orphan 而不删对象、staging + per-report `.cwk-tmp-*` 孤儿
   清理、healthy state 幂等 recover）；
6. 无 list / get-by-sha / existence probe：`SharedEvidenceStore` 公开面
   **精确 5 method**；`__all__` **精确 6 项**；46 项 forbidden name **一项
   不在**；无 CLI / HTTP entry point；
7. canonical 确定性：不同机器同 envelope → 字节完全相同 object；catalog
   head.catalog_jsonl_sha256 = actual sha；entry_count = 实际行数；catalog
   每行 = canonical JCS bytes；recover() 幂等；
8. RT-011/012/013 frozen contracts / dispatcher / binding / credential /
   legacy raw / collector / nightly / DocDB **零漂移**；
9. stdout / stderr / receipts / catalog / tmp / git secret & sensitive scan
   全部 0 命中；错误无 traceback / host path / 正文 / tenant / agent /
   credential；
10. RT-014 定向 62 tests + RT-011 回归 124 + RT-012 回归 111 + RT-013 回归 121 +
    全量 693 (skipped=7) 全部 PASS；`git diff --check` exit 0。

以上与 PRD FR-06 / AC-01 / NFR-03 / NFR-04、DESIGN §C-07 / §17、
`安全威胁模型.md` T-04 / T-11 / T-12、以及 `RT/RT-014/rt-intake.md` +
`specs/需求契约.md` + `specs/技术方案.md` 冻结口径**逐条对齐**。

**允许合并 RT-014 并进入 RT-015 / RT-016 与 VG-A 波次门禁**，附前述 3 项
Minor 与 9 项残余 `conservative_unknown` 约束。

**结论字段（机器可解析）**：

- `contract_conformance = PASS`
- `rt011_rt012_rt013_frozen_drift = ZERO_BYTES`
- `attack_matrix = ALL_PASS`（10 类 / 68 test cases，无 Blocker、无 Major、3 项 Minor）
- `regression = PASS`（RT-014 62 + RT-011 124 + RT-012 111 + RT-013 121 +
  全量 693 skipped=7）
- `fact_status_snapshot = conservative_unknown_frozen_by_RT011_RT013_RT014`
- `next_rt_gate = RT-015 && RT-016 open; VG-A can be scheduled after RT-015`
- `not_allowed = { close_SG-02, close_SG-07, close_G5, close_G7,
  declare_G3, declare_VG-A, declare_M2, declare_migration,
  upgrade_verified_shared_extensions_vN, modify_RT-011_frozen_artefacts,
  modify_RT-012_dispatcher_or_atomic_or_instance, modify_RT-013_frozen_schemas,
  modify_RT-013_binding_credential_agentcontext_provider_modules,
  modify_RT-014_shared_evidence_or_schemas,
  declare_real_gateway_identity_verified, declare_sandbox_transport_verified }`

## 10. 附录：本验收 Agent 使用的独立命令一览

```
# 1. baseline
git rev-parse HEAD                            # d658b05...
git log --oneline afdc37e..HEAD               # d658b05 单个 commit
git rev-parse afdc37e d658b05                 # 两处基线 commit
python3.11 --version                          # Python 3.11.14

# 2. RT-011/012/013 frozen byte-for-byte
git rev-parse afdc37e:<path> vs d658b05:<path>       # 14 modules zero-drift
git ls-tree -r afdc37e -- <dir> vs -r d658b05 -- <dir>   # 4 schema/ADR/manifest dirs zero-drift

# 3. Legacy / production untouched
git rev-parse afdc37e:<legacy> vs d658b05:<legacy>    # 9 legacy modules + Makefile + install.sh + .env.example zero-drift

# 4. Compile / whitespace / regression
python3.11 -m compileall -q scripts tests             # exit 0
git diff --check                                       # exit 0
python3.11 -m unittest discover tests -p "test_rt014_*.py"    # 62 OK
python3.11 -m unittest discover tests -p "test_pr001_*.py"    # 124 OK
python3.11 -m unittest discover tests -p "test_rt012_*.py"    # 111 OK
python3.11 -m unittest discover tests -p "test_rt013_*.py"    # 121 OK
python3.11 -m unittest discover -s tests -p 'test_*.py'       # 693 OK (skipped=7)

# 5. Attack matrix (all in mktemp -d /tmp/rt014-verify.XXXXXX/)
python3.11 attack1_idempotency_isolation.py           # 6 OK (T-11/T-12)
python3.11 attack2_deep_injection.py                  # 6 OK (T-08/T-09 深扫 + 伪造 extensions_ref)
python3.11 attack3_corruption.py                      # 11 OK (T-11 篡改 + 错误 opacity)
python3.11 attack4_path_symlink_toctou.py             # 10 OK (T-04)
python3.11 attack5_concurrency_fault.py               # 6 OK (T-11 并发 + fault injection)
python3.11 attack6_public_surface.py                  # 4 OK (T-12 公开面反射)
python3.11 attack7_canonical_determinism.py           # 4 OK (canonical 确定性 + catalog SHA 一致性)
python3.11 attack8_9_scan_and_drift.py                # 8 OK (drift + secret/canary scan)
# 总计 55 独立 attack cases + 62 RT-014 unit tests + 356 RT-011/012/013 回归 + 220
# 遗留 RT-001..010 / attic tests = 693 all OK

# 6. Cleanup
rm -rf /tmp/rt014-verify.CnPlCr /tmp/rt014_scratch.txt /tmp/base_tree.txt /tmp/head_tree.txt
git status --short --untracked-files=all              # only this report
```

（本报告为唯一新增文件；未修改任何实现代码 / schema / fixture / 测试 / PRD /
DESIGN / 计划 / RT index / legacy 文件；已 `git diff --check` 通过；`git status
--short --untracked-files=all` 只显示本报告。）

# RT-014 开发任务

| 编号 | 任务 | 输出 | 完成标志 |
| --- | --- | --- | --- |
| T1 | 定义 RT-014 4 张 JSON schema | `PR/.../contracts/rt014/schemas/*.json` | 每张 schema 满足 draft 2020-12 subset + `additionalProperties:false` + `unevaluatedProperties:false` + 顶层/深层 forbidden 保护 |
| T2 | 实现 `scripts/cwk_shared_evidence.py` | 单文件 (~800 行) | 公开面仅 `SharedEvidenceStore.{open,initialize,publish,read_version,recover}`；无枚举/回滚 |
| T3 | 主链路测试 `test_rt014_shared_evidence.py` | 覆盖 publish / read / recover / public surface | 全绿 |
| T4 | 攻击/负面测试 `test_rt014_attacks.py` | 覆盖深层注入、篡改、symlink/hardlink、错误 opacity | 全绿 |
| T5 | 并发 / fault injection 测试 `test_rt014_concurrency.py` | 多线程 publish、CAS 冲突、mid-publish crash | 全绿 |
| T6 | 交付验证报告 `reports/交付验证报告.md` | 逐条需求映射 + 命令与结果摘要 | 明确标注 implementation_done（禁自证 PASS） |
| T7 | `RT/index.yaml` 添加 RT-014 行 | status=implementation_done | 与其他 RT 风格一致 |
| T8 | 回归：RT-011/012/013 定向 + 全 tests | `python3.11 -m unittest discover tests -v` 全绿 | 无失败/error |
| T9 | `git diff --check` + secret scan | 干净 | 提交前必须通过 |

## 顺序建议

T1 → T2 → T3 → T4 → T5 → T8（先跑一次 sanity） → T6 → T7 → T9 → commit

# RT-008 交付验证报告

## 已交付

- 定义后台批量接口 `POST /open-api/work-report/report/relation/batchQueryCurrentUser`，由 AppKey 解析当前用户，一次最多查询 200 篇。
- 新增 `cwk_report_relation.py`：批量调用、逐条校验、识别漏回 ID，并生成不含凭据的本轮关系 manifest。
- 重写 `cwk_person_relation.py`：只规范化后台结果；旧的汇报人/nodeList/scope 入参仅为兼容而接收，不参与判定。
- 行动中心和每日简报只在后台明确返回 `resolved + visible_only` 时显示“仅权限可见”；接口未配置或不可用时显示“关系待确认”。
- nightly 增加可选关系接口步骤。接口故障按关系能力降级，不阻断 raw、时间线、Wiki 和页面生成。
- 更新环境变量与分发配置模板。

## 验证结果

- 单元测试：130/130 通过。
- Wiki smoke：14/14 通过。
- Python 语法检查与 `git diff --check`：通过。
- 8 月 11 日真实页面重建：49 条关系全部待后台确认；未经后台确认的“仅权限可见 · 与我无关”从 40 条降为 0。
- DocDB 以新版本更新 4 个页面对象，失败 0；云端全文回读中“关系待确认”可见，“与我无关”计数为 0。

## 未完成的外部依赖

工作协同后台尚未实现和发布本 RT 的批量关系接口。因此当前 CWK 侧处于安全降级状态，不能提供精确角色分类。后台接口上线并配置 `CWK_RELATION_API_PATH` 后，nightly 会自动消费权威结果，无需恢复本地推断。

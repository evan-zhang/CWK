# RT-016 独立验收报告：Legacy Raw 影子迁移与数据级对账（v2 anchor 第三轮修复）

> **本报告覆盖的是第三轮候选 `e8a75f6`**（分支 `agent/cwk-rt016-v2-third-fix`；
> 父链 `b734cfc = merge(5a9a689, e12e07b)`，即 v1.1 verify 报告 +
> v2 second-round remediation 的合并结果）。
>
> 作者：独立黑盒验收 Agent（**第三轮验收**；未参与 RT-016 v1 / v1.1 / v2 /
> v2 第二轮 / v2 第三轮任何实现、任何前几轮独立验收）。本 Agent 不采信
> 分支内 `独立验收报告.md` 的既有正文（那是 v2 second-round 的历史记
> 录）、不采信实现 Agent 的 `实现记录.md § 11` 与 `交付验证报告.md § 8/9/11`
> 中的自报结论；所有断言必须由本 Agent 在临时目录内独立复现。
>
> 隔离环境：worktree
> `/Users/evan/.openclaw/gateways/life/state/workspace-life/projects/CWK/.worktrees/openclaw-worktree-cwk-rt016-v3-verify-fresh`
> 上分支 `agent/cwk-rt016-v3-verify-fresh`，`HEAD` = merge of `e8a75f6`
> (第三轮候选) 到本报告分支，保证工作树等同 `e8a75f6` 加本 Agent 后续
> 写入 `独立验收报告.md`。
>
> Python：`Python 3.11.14`（Homebrew，Apple Silicon macOS）。
>
> 环境边界：**仅使用 `python3.11 + tempfile / mktemp` 合成 fixture**；未
> 读取真实 `CWORK_APP_KEY`、真实 legacy raw、工作协同、Cloud/DocDB、
> Gateway、broker、scheduler、installer、cron；`grep`
> `os\.environ|os\.getenv|getenv\(|urllib\.request|requests\.|http\.client|urlopen|\bsocket\.|subprocess\.|os\.system`
> 于 `scripts/cwk_legacy_raw_import.py` 与
> `scripts/cwk_migration_reconciler.py` **零命中**。
>
> **严格写权限**：本 Agent 仅新增/修改
> `RT/RT-016/reports/独立验收报告.md`。未修改任何实现、schema、tests、
> PRD/DESIGN/开发计划、RT/index.yaml、RT-011~015 或 legacy 模块（除本
> 报告 branch 的合并提交外，实现代码即 `e8a75f6` 原样）。

## 一、验收结论（Verdict）

- **Blocker：0**
- **Major：0**
- **Minor：2**（见 §五；均为文档表述与实现之间的静态不一致，不影响
  fail-closed 语义、不影响任一实测断言）
- **Info：1**（见 §五）
- **完整 unittest**：
  - RT-016 定向 `test_rt016_*.py`：**171 / 171 OK**（第三轮定向新增
    `test_rt016_anchor_v3.py` = 16 tests；stable 3/3）
  - 全套 `python3.11 -m unittest discover -s tests`：
    **1063 / 1063 OK**（`skipped=7` 均为 RT-011~015 交付前已存在的
    pre-existing skip，非本 RT 引入）
- **独立黑盒攻击套件**：本 Agent 单独编写 4 个独立脚本 `audit_a/b/c/d`
  于 `/tmp/rt016-v3-audit/`，共 **27 项**未复用任一 RT-016 shipped
  test 类的定向断言，**27 / 27 PASS**（详见 §四）
- **RT-011~015 / legacy 数据面 / RT/index.yaml 零漂移**：
  - `git diff --stat b734cfc..e8a75f6` 覆盖 11 个文件、+1447/−112
    行，全部落在 RT-016 owned 范围（`scripts/cwk_legacy_raw_import.py`
    / `scripts/cwk_migration_reconciler.py` /
    `reconciliation_report_v2.schema.json` + `tests/test_rt016_anchor_v3.py`
    + `tests/test_rt016_schemas.py` + `RT/RT-016/*`）；对 24 个受保护
    RT-011~015 / legacy `scripts/` 模块与 `RT/index.yaml`，第三轮 diff
    命中 = 0 hunks
  - 独立脚本对 24 个 RT-011~015 + legacy `scripts/` vs
    `git show 7ba906f:<path>` 逐字节比对：24/24 identical
  - `RT/index.yaml` 与 `7ba906f` 的差异 = **c51dcb2 时新增的 RT-016 行**
    （尾行 `implementation_done`），第三轮 `e8a75f6` **未再修改**
    `RT/index.yaml`（`git log --oneline -- RT/index.yaml` 证实）
- **RT-016 v1 契约 schema 零漂移**：5 份 v1 schema 对
  `_FROZEN_RT016_V1_SCHEMA_BASELINE_SHAS` 逐字节相同（本 Agent 独立
  哈希核对）
- **Fix C 冻结基线独立扩展**：本 Agent 独立复核
  `_FROZEN_RT011_015_BASELINE_SHAS`（24 项 scripts/*.py + 2 项 top-level
  json）+ `_FROZEN_PR001_SHARED_SCHEMA_BASELINE_SHAS`（14 项）+
  `_FROZEN_RT012_SCHEMA_BASELINE_SHAS`（5 项）+
  `_FROZEN_RT013_SCHEMA_BASELINE_SHAS`（5 项）+
  `_FROZEN_RT014_SCHEMA_BASELINE_SHAS`（4 项）+
  `_FROZEN_RT015_SCHEMA_BASELINE_SHAS`（8 项）+
  `_FROZEN_RT016_V1_SCHEMA_BASELINE_SHAS`（5 项）= **共 67 个 pinned
  SHA，全部 match**；5 个受保护目录 exact-set 校验，**全部 0
  extra / 0 missing**
- **秘密扫描**：以 value-based 正则（`CWORK_APP_KEY\s*[:=]\s*["\'][^"\']{6,}["\']` /
  `eyJ[A-Za-z0-9_\-]{20,}\.` / `Bearer\s+[A-Za-z0-9_\-]{20,}` / `password\s*[:=]`
  等）扫描 12 个 RT-016 owned 代码文件 + 5 v1 + 4 v2 = 21 个 code/schema
  文件：**0 命中**；只有 `RT/RT-016/reports/交付验证报告.md` 内
  `access_token` 是 grep 模式描述（false positive）
- **`git diff --check e8a75f6`**：干净、无 whitespace 冲突

**Verdict = PASS**（2 Minor + 1 Info；均文档层，非功能门；不构成
Reject）。第三轮候选 `e8a75f6` 完全闭合 v2 second-round acceptance
（本轮候选之前 Reject 报告；见 `e8a75f6` commit message 罗列的三 Major）：

1. **Fix A — Manifest replay / dedupe fail-open 关闭**：
   `_load_manifest_line_bound` 是 append/audit 的唯一读入口；append 分支
   在生成 dedupe 集合前先跑完整 bounded replay，任一行 tenant/run 错位、
   非 JCS、entry_seq 断裂或重复、outcome/key 不共现、v2 crosswalk_key/
   review_id 重算不等都在“建 dedupe 集合前”抛
   `LegacyImportError(code="corrupt")`；不再有“先命中 dedupe 后报错”
   的窗口。本 Agent 用 11 条独立断言复现（10 攻击 + 1 幸存路径）全部
   PASS。
2. **Fix B — Review 覆盖绑定 raw SHA**：Reconciler 按
   `(legacy_path_hash, legacy_source_sha256)` 挂 review；raw SHA 漂移的
   v2 review 进入 `review_source_sha_drift`（`reconciliation_report_v2`
   classification enum 新增该值，本 Agent 直接读 schema JSON 确认），
   既不抑制 `only_legacy` 也不计入 `review_count / new_undecomposable_count`；
   SHA-匹配的 v2 review 仍正常抑制。本 Agent 用 3 条独立断言（stale +
   matched + enum 结构）全部 PASS。
3. **Fix C — Frozen exact-set baseline 主动检测“默默漏钉”**：新增
   `_FROZEN_PR001_SHARED_SCHEMA_BASELINE_SHAS` + rt012~015 各自的
   `_FROZEN_RT01X_SCHEMA_BASELINE_SHAS` + `_EXACT_SET_DIRECTORY_MAP`；
   本 Agent 用 6 条独立断言复现（baseline 存在 + 67 SHA 全 match +
   exact-set 全 match + tampered access_tombstone 差异 + 合成
   extra/missing 均可 detect）全部 PASS。

**Verdict 明确不宣称**（第三轮候选文档也未如此宣称）：

- 不认可任何“RT-016 已完成 M3/M4/M5/G3/VG-A/生产迁移准备就绪”宣称。
  生产路径 `authority_receipt=None` ⇒ `tenant_view_written=False` +
  `tenant_view_deferred_reason="no_authority_receipt_available"`；真
  实 authority integration 仍 `conservative_unknown`。
- 不认可任何“Broker 上线后 legacy vs new 查询 diff”结论（RT-022/RT-023/
  RT-026 后续）。
- 不认可任何“clean-room 恢复 / 加密备份 / cron 拉起 / real tenant 切
  换”结论（RT-025/RT-023/RT-024/RT-026 后续）。
- v2 anchor + fresh-decompose + 第三轮 manifest bound-loader / review
  raw-SHA binding 的检测保证在 rt-intake §十 与 需求契约 §5.7 明文
  声明的 **RT-016 registry 被写、但 LegacySource dirfd 只读路径与
  RT-014 `shared/` 目录彼此独立、未在同一时间窗口被同一攻击者篡改**
  前提下成立。三方共谋不在本层保证范围。

## 二、验收范围与方法

### 2.1 阅读闭环（未采信任何先前实现 / 验收结论）

本 Agent 逐字读完：

- `RT/RT-016/rt-intake.md`（含 §九 v1.1 remediation、§十 v2 anchor
  remediation；460 行）
- `RT/RT-016/specs/需求契约.md`（185 行，§五 v2 anchor-bound identity
  含第二轮 fresh-decompose 与第三轮 review coverage / manifest bound
  loader 说明）
- `RT/RT-016/specs/技术方案.md`（424 行，§十三 v2 anchor remediation
  架构增量含第三轮 §13.5 frozen exact-set + meta-guard）
- `RT/RT-016/tasks/开发任务.md`（141 行）
- `RT/RT-016/reports/实现记录.md`（816 行；§十 v2 second-round + §十一
  v3 third-round fixups）
- `RT/RT-016/reports/交付验证报告.md`（559 行；§7 v2 anchor + §8 v2
  second-round + §11 v3 third-round 验证矩阵）
- 候选 diff `git diff b734cfc..e8a75f6`（11 文件、+1447/−112 行）
  完整逐块阅读；
- `scripts/cwk_legacy_raw_import.py`（3894 行）第三轮相关章节
  （`_load_manifest_line_bound` / `_append_manifest_entry` /
  `_verify_crosswalk_matches_fresh_decompose` /
  `canonical_tenant_view_projection` / `_load_review_payload_bound`
  bound reader 全套）；
- `scripts/cwk_migration_reconciler.py`（771 行）新的 review coverage
  分类流程；
- `PR/…/rt016/schemas/reconciliation_report_v2.schema.json`（新增
  `review_source_sha_drift` classification）；
- `tests/test_rt016_schemas.py`（232 行）新的 6 baseline maps +
  `FrozenBaselineExactSetTests`；
- `tests/test_rt016_anchor_v3.py`（709 行；第三轮新增 16 项）；
- RT-011~015 公共契约、pinned frozen baseline 机制的锚定 commit
  `7ba906f`（RT-015 acceptance）确认基线来源正确。

### 2.2 三层验证

1. **静态一致性**：文档与实际实现的 API / test 计数 / schema 数量 /
   frozen baseline 覆盖范围之间的等价关系；
2. **单测复跑**：候选自带 `test_rt016_*.py`（171）+ 全套（1063）+ 3×
   稳定性抽样；
3. **本 Agent 独立黑盒攻击**：4 个脚本 27 断言，未复用任一 shipped
   test class / fixture / helper（只 import `cwk_legacy_raw_import`、
   `cwk_migration_reconciler`、`cwk_pr001_contracts`、`cwk_instance` 等
   模块的公开 symbol，绕过 `tests/_rt016_helpers.py` 的 `Fixture` /
   `RT016TestBase` 类，各自现场搭 InstanceLayout + registry + evidence
   + ledger + view store 完整栈）。

### 2.3 独立攻击脚本清单（`/tmp/rt016-v3-audit/`）

- `audit_a_manifest_replay.py` — 11 项，Fix A 覆盖
- `audit_b_review_sha_binding.py` — 3 项，Fix B 覆盖
- `audit_c_frozen_baseline.py` — 6 项，Fix C 覆盖（含 exact-set meta
  guard + tampered rt015 detection）
- `audit_d_regressions.py` — 7 项，跨轮回归（anchor+fresh-decompose、
  bound review reader、tenant view semantic projection、reconciler
  RT-014 read-only 边界、v2 identity、actor log injection）

## 三、单测执行结果（本 Agent 现场复跑）

```text
$ python3.11 -m unittest discover -s tests -p 'test_rt016_*.py'
Ran 171 tests in 19.184s
OK

$ python3.11 -m unittest discover -s tests
Ran 1063 tests in 76.555s
OK (skipped=7)

$ for i in 1 2 3; do timeout 120 python3.11 -m unittest \
      discover -s tests -p 'test_rt016_*.py' 2>&1 | tail -1; done
Ran 171 tests in 19.402s
Ran 171 tests in 19.607s
Ran 171 tests in 19.024s
OK / OK / OK

$ python3.11 -m unittest tests.test_rt016_schemas.FrozenFilesZeroDriftTests \
                         tests.test_rt016_schemas.FrozenBaselineExactSetTests
Ran 9 tests in 0.008s
OK
```

- RT-016 定向 171 = v2 second-round 148 + v3 third-round 新 16
  （anchor_v3.py 定向）+ 6 项 `test_rt016_schemas` 新方法（5 分割
  baseline SHA 测试 + `FrozenBaselineExactSetTests` 2 项 = 7 - v2 中
  已有的 2 项 = 增量 5；再加 1 项现有 baseline 更名 rewiring）= 差
  23；148 + 23 = 171，匹配。
- 全套 1063 = v2 second-round 1040 + 23 = 1063，匹配。
- `skipped=7` 均为 RT-011~015 交付前既有，非本轮新增。

## 四、独立黑盒攻击套件（本 Agent 编写；不复用 shipped test）

### 4.1 Audit A — Manifest replay / dedupe fail-closed（Fix A）

命令：`python3.11 /tmp/rt016-v3-audit/audit_a_manifest_replay.py`
结果：`[audit A] 11/11 PASS`

| # | 断言 | 结果 |
| --- | --- | --- |
| A-1 | 基线：import_batch 写单行 manifest，`entry_seq=1`, `outcome=complete` | PASS |
| A-2 | 把 manifest 行 re-encode 为非 JCS（pretty-printed）→ `iter_manifest` 抛 `corrupt` | PASS |
| A-3 | 把 payload.tenant_id 改为 foreign valid id → `iter_manifest` 抛 `corrupt` | PASS |
| A-4 | 把 payload.run_id 改为其它 valid run id → `iter_manifest` 抛 `corrupt` | PASS |
| A-5 | 把 entry_seq 改为 7（gap）→ `iter_manifest` 抛 `corrupt` | PASS |
| A-6 | 两行合法 manifest 后把第二行 entry_seq 改为 1（duplicate）→ `iter_manifest` 抛 `corrupt` | PASS |
| A-7 | 保留 crosswalk_key 但改 legacy_path_hash → recomputed key 不等 → `iter_manifest` 抛 `corrupt`（跨身份 replay 关闭） | PASS |
| A-8 | complete outcome 拿掉 crosswalk_key → `iter_manifest` 抛 `corrupt` | PASS |
| A-9 | complete outcome 加 review_id → `iter_manifest` 抛 `corrupt` | PASS |
| A-10 | 篡改后再对同 run_id 追加一个合法 raw → append 也 `corrupt`（不是“先 hit dedupe 后报错”） | PASS |
| A-11 | 合法同 raw 同 run 幂等第二次 append → iter_manifest 仍返回 1 条 entry_seq=1 | PASS |

**结论**：Fix A 的“唯一 bounded 读入口 + append 前完整 bounded replay”
在本 Agent 手写的合成攻击面下全部 fail-closed；任一破坏（tenant / run /
非 JCS / entry_seq / cross-key / outcome-key co-occurrence）都在“形成
dedupe 集合前”抛 corrupt，且不影响幸存路径的幂等 semantics。

### 4.2 Audit B — Review 覆盖绑定 raw SHA（Fix B）

命令：`python3.11 /tmp/rt016-v3-audit/audit_b_review_sha_binding.py`
结果：`[audit B] 3/3 PASS`（含 1 结构级 enum 检查）

| # | 断言 | 结果 |
| --- | --- | --- |
| B-1 | 先写 v2 review（bad raw），再把磁盘 raw 覆盖为不同 bad body（同 path 不同 SHA）→ reconcile 结果：`only_legacy_count >= 1`、`review_source_sha_drift` 出现在 `failure_samples`、`review_count == 0` 且 `new_undecomposable_count == 0` | PASS |
| B-2 | SHA 保持匹配的 v2 review：`only_legacy_count == 0`、`new_undecomposable_count == 1`、无 `review_source_sha_drift` | PASS |
| B-3 | `reconciliation_report_v2.schema.json` 的 classification enum 直接读文件确认包含 `review_source_sha_drift` | PASS |

**结论**：Fix B 的“review 覆盖必须绑定 `(legacy_path_hash,
legacy_source_sha256)`”契约完整落地；stale review 无法伪造 coverage；
匹配 review 仍可抑制 `only_legacy` 并计入分类，行为对称。

### 4.3 Audit C — Frozen exact-set baseline（Fix C）

命令：`python3.11 /tmp/rt016-v3-audit/audit_c_frozen_baseline.py`
结果：`[audit C] 6/6 PASS`

| # | 断言 | 结果 |
| --- | --- | --- |
| C-1 | 7 个 pinned baseline maps + `_EXACT_SET_DIRECTORY_MAP` 全部存在 | PASS |
| C-2 | **67 个 pinned SHA 全部 match** 当前磁盘字节（RT-011~015 scripts 24 + top-level json 2 + PR001 shared 14 + rt012 5 + rt013 5 + rt014 4 + rt015 8 + rt016 v1 5 = 67） | PASS |
| C-3 | 5 个受保护目录 exact-set 校验：**0 extra、0 missing**（`contracts/schemas` + `rt012~015/schemas`） | PASS |
| C-4 | 用 tampered access_tombstone.schema.json 副本（追加 `// evil`）→ SHA ≠ pinned baseline，证明 SHA 检查真的会 fire | PASS |
| C-5 | 合成一个未 pin 的 “hidden_new.schema.json” → set diff 会把它 surface 到 extra | PASS |
| C-6 | 从 baseline pop authority_receipt → set diff 会把它 surface 到 missing | PASS |

**结论**：Fix C 的两条 promise（SHA pin + exact-set meta-guard）都在本
Agent 独立复核下工作；任何“新增未登记文件 / 缺失文件 / SHA 漂移”都会
被检出；access_tombstone tampering 复核确认 SHA-fires。

### 4.4 Audit D — 跨轮回归（防第三轮 diff 静默重开旧洞）

命令：`python3.11 /tmp/rt016-v3-audit/audit_d_regressions.py`
结果：`[audit D] 7/7 PASS`

| # | 断言 | 结果 |
| --- | --- | --- |
| D-1 | 协调篡改：删掉 shared canonical object 后重新 import 同 raw → finder + fresh-decompose + read_version 逐字节比对失败 → `LegacyImportError(code="corrupt")` | PASS |
| D-2 | bound review reader：`review` 文件 payload.tenant_id 改为 foreign → `iter_reviews` 抛 `corrupt` | PASS |
| D-3 | TenantView semantic projection：合法 `observed_at` 单独 bump → reconciler 仍 `both_equal >= 1`；lane 单独 flip → reconciler `both_equal = 0` + `re_decompose_mismatch` 出现 | PASS |
| D-4 | `reconciler.reconcile(anchor=None)` → `LegacyImportError(code="contract")` | PASS |
| D-5 | 新 import 全部写为 v2（`schema=cwk.rt016.migration_crosswalk.v2`、`identity_version=v2`） | PASS |
| D-6 | 源码 grep：`scripts/cwk_migration_reconciler.py` 只调 RT-014 `read_version`，无 `.publish(` / `.recover(` 私有访问 | PASS |
| D-7 | actor 包含换行符 `admin\nEVIL` → `import_batch` 抛 `LogInjectionDetected` | PASS |

**结论**：v1.1/v2/v2 second-round 关闭的 guard 全部在第三轮 diff 之后
仍然工作；第三轮修改只对 manifest append / iter 与 review coverage
分类做了 targeted 增强，没有回退任一先前的 fail-closed edge。

## 五、发现（Blocker / Major / Minor / Info）

### 5.1 Blocker / Major：无

第三轮候选没有引入任何 Blocker 或 Major。v2 second-round acceptance
明确点名的 Reject 项（协调 tamper 已由 A/B/C fresh-decompose 强化
close；review coverage 已由 B 修复；frozen baseline 已由 C 修复）都
在本 Agent 27 项独立黑盒断言下全部 fail-closed 或按预期 PASS。

### 5.2 Minor-1 — `rt-intake.md § 四 公开接口` 中 reconcile 签名与实际实现不一致

- 位置：`RT/RT-016/rt-intake.md` line 176。
- 现有文本：`reconcile(*, tenant_id, run_id, source) -> ReconciliationReport`。
- 实际实现（`inspect.signature` 现场取回）：`reconcile(self, *, anchor,
  run_id, source) -> ReconciliationReport`。
- 影响：只是 rt-intake §四 那个静态 “公开接口” 代码块没有跟着 v2
  anchor remediation 更新；`需求契约.md § 一 & § 5.2` 与
  `技术方案.md § 7 & § 13.2` 均正确写为 `reconcile(*, anchor, run_id,
  source)`；同一个 rt-intake 的 § 十 v2 anchor remediation 段落
  （line 405-407）也正确说明 `reconcile(anchor=None) → contract`。所以
  只是静态章节漏改，不会真的误导消费者，也不影响任一测试或攻击结果。
- 建议：把 rt-intake § 四 line 176 改为
  `reconcile(*, anchor, run_id, source) -> ReconciliationReport`
  并在同文件 § 四 中同步引入 `ReconciliationAnchor` frozen dataclass
  的字段清单，与 § 十保持一致。**本 Agent 严格不修复实现层，只报告**。

### 5.3 Minor-2 — v2 schema 数量描述“五份 / 四份”前后不一致

- 位置一：`RT/RT-016/specs/技术方案.md § 13.1` heading 写为
  `五份 v2 契约 schema`，但其下正文只列 4 份 v2 schema（正确）。
- 位置二：`RT/RT-016/reports/实现记录.md § 10.5` line 727 写为
  `覆盖五份 v2 契约 schema`。
- 事实：`PR/PR-001-multitenant-knowledge-spaces/contracts/rt016/schemas/`
  目录下 v1=5 份、v2=4 份，共 9 份文件（`ls` 现场核对）。
- 其它同一话题的 rt-intake § 十 line 395 (`v2 契约（新增 4 份 schema）`)
  与 交付验证报告 § 7.1 line 314 (`四份 v2 契约 schema`) 都是正确的
  “四份”。
- 影响：无 functional 影响；仅两处 heading / 段落文字与实际数量不
  匹配，容易在后续 review 时引起误读。
- 建议：把两处 “五份 v2” 改为 “四份 v2”（或 “v2 新增四份、v1 保留
  五份” 明确区分）。

### 5.4 Info-1 — `实现记录.md § 1 / § 5.1` 仍保留 v1 交付时“75 tests / Ran 892 tests”的历史截图

- 位置：`RT/RT-016/reports/实现记录.md § 1` line 34
  `六份测试文件（共 75 项 targeted tests）`；`§ 5.1` line 258
  `Ran 75 tests in ~6s`；`§ 8.5` line 430 `Ran 93 tests`；`§ 9.7`
  line 632 `Ran 126 tests`；`§ 10.11` line 803 `Ran 148 tests`；
  `§ 11` 已明示第三轮实际计数。
- 影响：这些都是历史 snapshot，可读性 OK；rt-intake § 七 line 306 已
  改为 “测试计数以本次交付验证报告为准”，`开发任务.md § D-01/D-02`
  也已改为 `每轮实际计数以对应交付验证报告为准`。因此已经不再有
  “硬编码 75 tests” 的 forward-going 承诺，只是 § 1 概览段仍是 v1
  视角。属信息级，非必须修复。
- 建议（可选）：在 § 1 后置一句 “v1 = 75；v1.1 = 93；v2 = 126；v2
  second-round = 148；v2 third-round = 171。计数以对应交付验证报告
  为准”，或把 “六份 …75 项” 移到 § 5.1 历史小节。

### 5.5 明确不承诺项（继承候选文档；本 Agent 完全同意）

- 三方共谋（RT-016 registry + LegacySource dirfd + RT-014 shared/）
  同一时间窗口被同一攻击者篡改：不在本轮保证范围。
- 不宣称 G3 / VG-A / M3 / M4 / M5 完成；不宣称真实租户切换；不宣
  称真实 authority integration 从 `conservative_unknown` 状态改变；
  `AccessLedger.observe` 仍只写 `granted`；生产路径下
  `tenant_view_written=False` + `tenant_view_deferred_reason=
  "no_authority_receipt_available"`。
- 本次 exact-set frozen 保护范围仅覆盖当前 shipped 的 5 个
  contract-schema 目录；未来 RT 若引入新的受保护目录，必须同步
  扩展 `_EXACT_SET_DIRECTORY_MAP`（第三轮 commit message 已明示此
  边界）。

## 六、其它客观结果

- `git diff --check e8a75f6` → 无输出（无 whitespace 冲突 / 无冲突
  marker）。
- 反射审计：`ShadowImporter` 公开可调用方法集合 `{initialize,
  import_one, import_batch, read_crosswalk, iter_crosswalks,
  iter_reviews, iter_manifest, recover}` = intake § 四 白名单；无
  `enumerate_*` / `delete_*` / `unrevoke` / `bypass` / `list_all_*`
  等违禁名字。`MigrationReconciler` 公开可调用方法 `{reconcile}`。
- `scripts/cwk_legacy_raw_import.py` 与
  `scripts/cwk_migration_reconciler.py` 对
  `os.environ / os.getenv / getenv\( / urllib / requests / http.client /
  urlopen / socket. / subprocess. / os.system` **零命中**——RT-016
  代码路径不读环境变量、不发起网络调用、不 spawn subprocess。
- 秘密扫描（value-based，非 field-name-based）在 21 个 code/schema
  文件上 **0 命中**；`RT/RT-016/reports/交付验证报告.md` 里的
  `access_token` 是 grep pattern description（`grep -RiE
  "app_secret|api_token|api_key|password|access_token|bearer"`），无实
  际 token 明文。

## 七、验收命令快照（可复现）

```bash
# 1. 定向
python3.11 -m unittest discover -s tests -p 'test_rt016_*.py'
# Ran 171 tests in 19.184s / OK

# 2. 全套
python3.11 -m unittest discover -s tests
# Ran 1063 tests in 76.555s / OK (skipped=7)

# 3. 稳定性 3x
for i in 1 2 3; do
  timeout 120 python3.11 -m unittest discover -s tests -p 'test_rt016_*.py' 2>&1 | tail -1
done
# Ran 171 tests in 19.4/19.6/19.0s / OK / OK / OK

# 4. Frozen baseline（第三轮扩展后 9 项）
python3.11 -m unittest tests.test_rt016_schemas.FrozenFilesZeroDriftTests \
                       tests.test_rt016_schemas.FrozenBaselineExactSetTests
# Ran 9 tests in 0.008s / OK

# 5. 独立黑盒（本 Agent /tmp 编写；不复用 shipped test class）
python3.11 /tmp/rt016-v3-audit/audit_a_manifest_replay.py    # 11/11 PASS
python3.11 /tmp/rt016-v3-audit/audit_b_review_sha_binding.py #  3/3 PASS
python3.11 /tmp/rt016-v3-audit/audit_c_frozen_baseline.py    #  6/6 PASS
python3.11 /tmp/rt016-v3-audit/audit_d_regressions.py        #  7/7 PASS
```

## 八、`RT/index.yaml` 建议

- 第三轮候选 `e8a75f6` 完全闭合 v2 second-round Reject 的三个 Major，
  未引入任何 Blocker/Major，仅 2 项 Minor 与 1 项 Info 都是文档静态
  不一致（不影响 fail-closed 语义、不影响 171/1063/27 断言）。
- 本 Agent 建议 `RT/index.yaml` 中 RT-016 行由 `implementation_done`
  升至 `completed`（RT-016 owned 范围内所有 shipped 单测与本 Agent
  独立复核均 PASS，Minor 属可选修复）。
- **升级动作本身由后续记录 acceptance 的 commit 承担；本 Agent 严格
  只写 `独立验收报告.md`，未修改 `RT/index.yaml`**。

## 九、结束语

第三轮候选 `e8a75f6`（相对 `b734cfc = merge(5a9a689, e12e07b)`）+1447/−112
行、11 个文件，全部落在 RT-016 owned 范围。三个 Major Fix（A: manifest
bounded replay + JCS/tenant/run/entry_seq/outcome-key/cross-key 全绑定；
B: review coverage 按 `(path_hash, raw_sha)` 绑定，raw drift 走
`review_source_sha_drift`；C: frozen exact-set + meta-guard + 44 项新
pin）在本 Agent 27 项独立黑盒断言下全部 fail-closed 或按预期 PASS；跨
轮回归（anchor+fresh-decompose、tenant view semantic projection、bound
review/manifest reader、v2 identity、log-injection、reconciler
read-only 边界）也全部保住。

**Verdict：PASS**（Blocker=0, Major=0, Minor=2, Info=1）。可将 RT-016
在 `RT/index.yaml` 中标记为 `completed`；Minor-1/2 与 Info-1 属文档
静态一致性建议，不构成阻断。

---

_报告分支：`agent/cwk-rt016-v3-verify-fresh`；本报告首次提交 SHA 在本
commit 落定后由 `git rev-parse HEAD` 给出。_

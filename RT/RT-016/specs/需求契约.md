# RT-016 需求契约：Legacy Raw 影子迁移与数据级对账

## 一、动作契约

| 动作 / 触发 | 系统响应 | 成功标准 | 失败处理 |
| --- | --- | --- | --- |
| Decompose 合法 legacy Markdown | `LegacyRawDecomposer.decompose` 依次执行：安全字节解码、frontmatter 解析（拒绝 malformed 与控制字符）、`## Original Full Content For AI` 正文抽取、`## List Row Metadata` / `## Record Simple Info` / `## Node / Opinion Chain` JSON 解析、字段权威源仲裁（frontmatter > row metadata > event VO）、`created_at`/`source_updated_at` 严格 RFC3339 UTC 归一化、`author.source_user_id` 匹配 `^[A-Za-z0-9_\-.]+$`、正文 ≤ 1 MiB、reply/node 事件哈希（可选） | `test_rt016_decomposer.HappyPathTests`；返回 `DecomposeResult(status="ok", canonical_envelope, tenant_view_envelope, access_observation, decompose_report)` | 任一验证失败 → `DecomposeResult(status="quarantined", canonical_envelope=None, tenant_view_envelope=None, access_observation=None)`，`decompose_report.quarantine_reasons` 携带明确原因（无副作用） |
| 观察永远不能被 RT-016 直接升为 `active` | 每次 `decompose` 产生的 `AccessObservation` 严格 `initial_status="granted"` + `observation_source="legacy_raw_decomposition"`；即便传入其他值，schema 校验会拒绝 | `test_rt016_decomposer.HappyPathTests.test_produces_full_tuple` | 若上层试图伪造 initial_status → RT-011 `validate_access_observation` 拒绝；若上层直接调用 RT-015 `promote_to_active` 无 authority receipt → RT-015 `AuthorityRejected` |
| `ShadowImporter.import_one` 对合法 raw 完成三步 | 1) RT-014 `SharedEvidenceStore.publish` 幂等发布 canonical；2) RT-015 `AccessLedger.observe`（`initial_status=granted`）→ `granted` grant；3) RT-016 durable crosswalk 写入 `registry/rt016-crosswalk/<tenant_id>/crosswalks/<crosswalk_key>.json`（`crosswalk_key = H(domain, tenant_id, view_key, legacy_source_sha256)[:16]`）；4) 追加式 manifest 条目 → `manifests/<run_id>.jsonl`；5) 若未提供 `authority_receipt`（生产路径） `tenant_view_written=False` + `tenant_view_deferred_reason="no_authority_receipt_available"`（视图仍完整保存在 crosswalk 内） | `test_rt016_importer.ImportOneHappyPathTests.*` | RT-014 publish 失败 → 抛 `LegacyImportError(code="corrupt")` 并追加 review manifest；RT-015 observe 失败（非 macOS 竞态）→ 抛 `LegacyImportError(code="ledger_denied")`；crosswalk CAS 冲突或字节漂移 → 抛 `LegacyImportError(code="corrupt")` |
| 同 legacy_source_sha256 重复导入（同 run / 跨 run） | 在 `_find_existing_crosswalk_for_legacy` 命中已有 crosswalk（按 tenant_id + legacy_source_sha256 匹配）时直接返回既有 `ImportReceipt`；manifest 中同 (legacy_source_sha256, outcome) 二元组只出现一次 | `test_rt016_importer.ImportOneHappyPathTests.test_idempotent_*` | 若 crosswalk 已存在但字节漂移（canonical_sha256 / observe_grant_key / object_id 不一致）→ `LegacyImportError(code="corrupt")` |
| Overlay-only diff：不同 legacy raw 产生同一 canonical | 两个 legacy raw 只有 frontmatter 差异 → 两个 crosswalk 共用同一 `canonical_sha256` / `object_id`；RT-014 catalog 只写一条 report_version | `test_rt016_decomposer.OverlayEquivalenceTests.test_two_legacy_files_with_same_body_produce_same_canonical` + `test_rt016_importer.ImportOneHappyPathTests.test_multiple_legacy_sha_same_canonical` | RT-014 publish 检测到 canonical_sha256 已存在时复用 object_id；RT-016 不会尝试重发布 |
| 正文变化（同 report_id 不同 body） | RT-014 catalog 新增第二条 report_version，crosswalks 独立记录 | `test_rt016_importer.ImportOneHappyPathTests.test_body_change_creates_new_catalog_version` | RT-014 内部并发失败 → `SharedEvidenceError(code="catalog_conflict")` 上抛为 `LegacyImportError(code="corrupt")` |
| 无法可信反解 | 只写 `registry/rt016-crosswalk/<tenant_id>/review/<review_id>.json`（`review_id = H(domain, tenant_id, legacy_source_sha256, run_id)[:16]`），零 canonical / view / grant 写入；manifest outcome ∈ {`review`, `undecomposable`} | `test_rt016_importer.QuarantinePathTests.test_quarantined_raw_writes_review_only` | 若 review 幂等冲突则复用既有 review payload；不允许 partial write |
| 双哈希 / 字段篡改 crosswalk / review / manifest | 读取时对 raw bytes 做 JCS 逆向复核 + schema 校验；任一失败抛 `LegacyImportError(code="corrupt")` 或 `code="contract"` | `test_rt016_importer.CrosswalkReadIntegrityTests.test_read_crosswalk_detects_tamper` + `test_rt016_attacks.TamperingTests.*` | 不静默恢复；不重写篡改文件 |
| 路径 / 符号链接 / 硬链接 / TOCTOU | `LegacySource` root 必须绝对路径且非 symlink；子树 `O_DIRECTORY|O_NOFOLLOW`；symlink 与 `nlink>1` 文件跳过；`legacy_path_hint` 域分隔 hash 化后不能穿透到 RT-016 命名空间外 | `test_rt016_attacks.PathContainmentTests.*` | `LegacyImportError(code="path_containment")` 或 `code="not_found"`；不允许 mkdir 到不存在的父目录 |
| Actor / reason 注入 | 长度上限 128 / 256；仅允许 `\x20-\x7e`；违反抛 `LogInjectionDetected` | `test_rt016_attacks.LogInjectionTests.*` | 早期拒绝，永不写入 event / manifest / crosswalk |
| Recovery：sweep `.cwk-tmp-*` | `ShadowImporter.recover(actor, reason)` 幂等清理 staging / crosswalk / review / manifest 目录下的临时孤儿；返回 `RecoveryReport` | `test_rt016_importer.RecoveryTests.test_recover_sweeps_orphans_and_reports` | 无向后回滚；durable crosswalk / review 不会被恢复流程删改 |
| RT-015 fake authority 端到端 upsert | `TenantViewStore.upsert_overlay` 需要 `promote_to_active` 成功 → 依赖 fake `AuthorityAdapter` + 匹配 `signer_id`；成功后 `ImportReceipt.tenant_view_written=True` | `test_rt016_importer.TenantViewUpsertViaFakeAuthorityTests.test_valid_receipt_upserts_view` | Fake authority 未注册 / 签名不符 / receipt binding 不匹配 → `tenant_view_deferred_reason ∈ {authority_receipt_rejected, in_flight_revocation, access_ledger_denied, canonical_missing}`；canonical + observation 仍持久化 |
| `MigrationReconciler.reconcile` | 依赖 caller 提供 `LegacySource`（先 snapshot），再执行：1) `verify_no_drift`；2) 递归扫描合成 legacy 树重算 `legacy_source_sha256`；3) 枚举 RT-016 crosswalk（new-side 全集）；4) 对每条 crosswalk 调用 RT-014 `SharedEvidenceStore.read_version` 校验 canonical；5) 交叉分类 `both_equal / only_legacy / only_new / new_undecomposable`；6) 输出 `ReconciliationReport`（严格 schema） | `test_rt016_reconciler.*` | RT-014 报错 → `failure_samples` 采样 `canonical_missing/canonical_sha_mismatch/object_bytes_sha_mismatch`；drift → `zero_drift_verified=False`；无副作用 |
| 反射断言无枚举 / 回滚 / 删除接口 | 公共 API 严格白名单，无 `enumerate_shared / delete_crosswalk / unrevoke / bypass / list_all_tenants` 等属性 | `test_rt016_schemas.OpaqueIdTests` + `test_rt016_attacks.*` | 违反 = P0 |
| 零漂移 RT-011~015 冻结物 | `git ls-tree HEAD` + 现文件 SHA-256 全部一致 | `test_rt016_schemas.FrozenFilesZeroDriftTests.test_no_drift_from_head` | 任何被修改文件即断言失败 |

## 二、schema 契约

`PR/PR-001-multitenant-knowledge-spaces/contracts/rt016/schemas/` 独占：

- `decompose_report.schema.json` — `cwk.pr001.rt016.decompose_report.v1`：
  `additionalProperties:false + unevaluatedProperties:false`；必填
  `schema/decomposer_version/normalizer_version/legacy_source_sha256/
  canonical_sha256/object_bytes_sha256/body_bytes_length/
  body_truncation_would_occur/field_sources/hit_rules/
  unknown_frontmatter_keys/unknown_row_keys/unknown_reply_keys/
  unknown_node_keys/timeline_event_hash_check/decomposition_status/
  quarantine_reasons`；`decomposition_status ∈ {ok, quarantined}`；
  `quarantine_reasons` 枚举覆盖 missing / malformed / oversize / control /
  timeline / path / TOCTOU / Unicode 冲突等 25 项；
  `deepForbiddenProperties` 拦截 credential/body/temporary_url/path。
- `migration_crosswalk.schema.json` —
  `cwk.pr001.rt016.migration_crosswalk.v1`：`crosswalk_key ^cw_[a-z2-7]{26}$`；
  必填三类 hash + `publish_receipt`（结构由 Python 端二次校验为 RT-014
  publish_receipt.v1）+ `tenant_view_envelope`（由 RT-011
  `validate_tenant_view` 二次校验）+ `decompose_report`（由本 RT
  decompose_report schema 二次校验）+ `observe_grant_key`（`^g_[a-z2-7]{26}$`）+
  `observation_source ≡ "legacy_raw_decomposition"` +
  `initial_status ≡ "granted"` + `tenant_view_written` 布尔 +
  `tenant_view_deferred_reason ∈ {null, no_authority_receipt_available,
  authority_receipt_rejected, access_ledger_denied, canonical_missing,
  in_flight_revocation, not_attempted}`；`timeline_snapshot_hashes` /
  `timeline_event_hashes` 各自 unique 上限 4096；
  `deepForbiddenProperties` 拦截 credential / canonical_body /
  reply_body / node_body / auth_epoch / binding_epoch / cookie 等。
- `review_entry.schema.json` —
  `cwk.pr001.rt016.review_entry.v1`：`review_id ^qe_[a-z2-7]{26}$`；
  `migration_status ∈ {review, undecomposable}`；`quarantine_reasons`
  至少一项；`deepForbiddenProperties` 严于 crosswalk（额外拦截
  `reply / node / attachment / temporary_url` 等），因为 review
  永远不应携带这些字段。
- `migration_manifest_entry.schema.json` —
  `cwk.pr001.rt016.migration_manifest_entry.v1`：一行一 JSON；
  `outcome ∈ {complete, review, undecomposable}`；`entry_seq >= 1`；
  可选字段 `crosswalk_key / review_id / canonical_sha256 / view_key /
  quarantine_reasons` 依 outcome 出现。
- `reconciliation_report.schema.json` —
  `cwk.pr001.rt016.reconciliation_report.v1`：所有 count 字段 `>=0`；
  三类 rate ∈ [0, 1]；`zero_drift_verified` 布尔；
  `failure_samples[]` classification ∈ 9 类枚举；`opaque_key` 只允许
  `^[a-z0-9_.:/\-]+$`；`deepForbiddenProperties` 与 review 相同。

## 三、错误 taxonomy（`LegacyImportError.code`）

| code | 语义 |
| --- | --- |
| `contract` | schema / 字段 / JCS / 类型违反（含 unknown source_kind / bad run_id 等） |
| `not_initialized` | RT-016 或依赖子目录尚未创建（例如未调用 `initialize()`） |
| `not_found` | 请求的 tenant / crosswalk / review 子目录不存在 |
| `conflict` | 内部并发冲突（保留字段，当前实现不主动触发） |
| `state` | 非法状态迁移（保留字段） |
| `io` | 底层 I/O 出错（打开子目录、fstat 失败） |
| `corrupt` | crosswalk / review / manifest 字节漂移或 schema 反向复核失败 |
| `log_injection` | actor / reason 含控制字符或超长 |
| `path_containment` | LegacySource root 是 symlink / 不是目录 / 子路径穿透 |
| `legacy_drift` | Pre-scan 与 re-scan 不一致 |
| `duplicate_report_id` | 保留：预留给未来强隔离；当前依 RT-014 catalog 语义处理 |
| `authority_deferred` | 保留：预留给未来 tenant view 强制 upsert 流程 |
| `ledger_denied` | RT-015 observe / promote 失败被 RT-016 视作 fail-closed 的对外错误 |
| `canonical_missing` | 保留 |
| `internal` | RT-016 自身不变量被打破（例如 opaque_id 长度失败） |

## 四、非目标

- 不实现真实 authority integration；不引入新 credential / secret
  backend。
- 不迁移 legacy timeline 的完整 reply/node payload；不改 RT-007。
- 不新增 CLI / HTTP / cron；不加入 tenant CLI provider registry。
- 不做 legacy 与 RT-022 Broker 的查询 diff（属 RT-026 后续工作）。
- 不宣称 M3 / M4 / M5 / G3 / VG-A / 生产迁移准备完成。

# RT-018 开发任务：宿主机 Tenant Scheduler

> 状态：planned；无任务已完成。

## A. ABI 与 schema

- [ ] A-01：冻结 JobProviderV1、JobSpec/Result/StateEvent/RunManifest/RecoveryDecision schema 与语义 validator。
- [ ] A-02：实现显式对象注入 Registry；拒绝动态 import、未知/重复/version mismatch provider。
- [ ] A-03：冻结 domain-separated length-prefix idempotency key 并加入边界碰撞测试。

## B. Scheduler 核心

- [ ] B-01：实现 tenant candidate/state 三次复核、稳定错峰与 dry-run plan。
- [ ] B-02：实现 tenant 单写 lock + durable lease、CAS 状态机和原子 manifest。
- [ ] B-03：实现 deficit round-robin、公平性、resource quota 与 tenant-scoped circuit。
- [ ] B-04：实现有界 persistent retry、deadline、clock anomaly 与 restart recovery。
- [ ] B-05：实现 CollectorJobProviderV1，只调用 RT-017 公开 Worker。
- [ ] B-05a：构造注入 `PilotAdmissionProviderV1(purpose="collector_run")`；`pilot` 在
  Worker 启动与 manifest/终态提交前各重验，`active` 0 次。

## C. 安全与隔离测试

- [ ] C-01：双触发/双进程单 writer、lease stale、CAS collision、crash-boundary 测试。
- [ ] C-02：A 的 429/deadlock/timeout/bad config/provider crash/disk full 不影响 B。
- [ ] C-03：tenant 状态矩阵、revision/auth_epoch 变化、credential disabled 均 fail closed。
- [ ] C-04：fairness/配额/重试/熔断的 deterministic property tests，无 starvation/unbounded retry。
- [ ] C-05：provider/job/path/log 注入、symlink/hardlink/TOCTOU、manifest secret scan。
- [ ] C-05a：覆盖 PilotAdmission missing/deny/expiry/wrong tenant/purpose/hash、revision
  回退、第二次漂移、第二次拒绝后的零半提交，以及 active/profile_pending 调用次数。

## D. 回归与交付

- [ ] D-01：确认 `cwk_nightly_pipeline.py`、installer、cron、统一 flag 相对 RT-018 入场 commit 零漂移；RT-026 必须在 RT-018/VG-B 后单独提交其预声明 evolution receipt，不能回写本 RT 候选。
- [ ] D-02：RT-011～018 定向测试、全量 unittest、Wiki lint/smoke、legacy nightly fixture。
- [ ] D-02a：复跑 Wave-0 PilotAdmission contract test，断言 neutral runtime/schema/test
  SHA 与构造期 purpose ABI 零漂移。
- [ ] D-03：实现 Agent 填写实现记录和自测；全新 Agent 做独立黑盒验收。
- [ ] D-04：独立 PASS 后更新交付报告；另行运行 VG-B，不能把 RT-018 PASS 替代 VG-B。
- [ ] D-05 **RT-018 安全门 receipt（SG registry 声明的生产任务）。**
  由本 RT 的独立验收 Agent 在 `rt_independent_acceptance` 阶段按
  `cwk.pr001.security_gate_receipt.v1` 生成
  `PR/PR-001-multitenant-knowledge-spaces/security-receipts/RT-018/receipt.json`，
  `receipt_kind = rt-security`，consumer 恰为 `RT-026`（go/no-go）与 `G6`（final acceptance）。
  - `claims` 必须与 registry 中 RT-018 的集合逐字相等：`SGC-018-01`/SG-03、`SGC-018-02`/SG-05、`SGC-018-03`/SG-08。
  - `filesystem_policy.mode = applicable`，因此**没有任何 claim 可以标 N/A**；
    SG-03 覆盖必须绑定本 RT 的真实运行面与可执行用例引用，逐类给出
    `path_traversal`、`symlink_component`、`symlink_leaf`、`hardlink`、`toctou`、`special_file` 六类证据，泛泛散文不算覆盖。
  - `independent_security_verification_pass` 是独立安全/预检结论，不等价于
    RT 最终 PASS；`verifier` 必须存在且不等于 `producer`；
    `tests_run > 0`、`tests_failed = 0`、`artifacts` 非空且逐条 sha256 匹配；
    `tested_subject_commit` 为被测候选 commit（evidence-only commit 的祖先）。
  - 未生成该 receipt 时，RT-018 在 SG 视角下为 NOT_RUN，RT-026 只能给 NO_GO。

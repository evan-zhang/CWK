# RT-022 开发任务：Query Broker 授权核心

- [ ] T022-01 冻结 `QueryRequest v2`、error、snapshot、evidence bundle schema；
  为 v1 写零漂移断言和 migration note。
- [ ] T022-02 实现 schema + 强语义 validator，覆盖 selector、disposition
  budgets、时间范围、NFC、deep-forbidden 和规范化输出。
- [ ] T022-03 冻结并实现 `SpaceIndexProvider`（仅 `retrieve`）/`EvidenceReader`/
  `AuditSink` ABI 与仅测试可用 fake provider。
- [ ] T022-03a 冻结 `ProfileSpaceSnapshotProviderV1` Protocol 与
  `contracts/rt022/schemas/profile_space_snapshot_v1.json`（RT-022 唯一 owner）；
  实现构造注入点、返回值不变式 validator（`default ⊆ queryable`、
  `membership_versions`/`index_versions` 键集合精确等于 `queryable_space_ids`、
  0 space 仍必须带非空 `profile_sha256`、deep-forbidden 无 slug/路径/正文/credential）
  与仅测试可用 fake provider；附 RT-021 消费用 migration note。
- [ ] T022-03b 静态检查断言 Broker 未 import `cwk_knowledge_profile.py`/
  `cwk_space_registry.py`/`cwk_space_projector.py`，未读取其私有目录，
  且不存在 per-space 回调或 N+1 调用。
- [ ] T022-04 实现 binding/tenant/profile/space 快照与 pilot allowlist 门禁；
  profile SHA、space IDs、membership/index versions 全部取自 T022-03a 的单次返回值。
- [ ] T022-04a 零漂移消费 central `PilotAdmissionProviderV1`，构造绑定
  `purpose="query_broker"`；invalid request/active=0 次，pilot success/cache hit=2 次，
  第一次失败时 Profile/retrieval/cache=0 次。
- [ ] T022-05 实现 active grant∩membership 前置过滤和 provider allowset。
- [ ] T022-06 实现分 disposition 预算、deadline、semaphore 与返回子集验证。
- [ ] T022-07 实现 Top-K 二次 ACL、lease/epoch/profile/membership 竞态拒绝。
- [ ] T022-08 实现 canonical SHA 回读、evidence bundle 和稳定错误映射。
- [ ] T022-09 实现 domain-separated cache key、命中复核与 cleanup outbox 消费。
- [ ] T022-09a 将 tenant status、admission revision/snapshot SHA 绑定 QuerySnapshot/cache
  key，维护 `(tenant_id,purpose)` revision high-water；deny/unavailable 不缓存。
- [ ] T022-10 适配 `cwk_wiki_query.py` 库函数，证明无任意路径入口且 legacy
  CLI 行为不变。
- [ ] T022-10a 为 `cwk_wiki_query.py` 追加 policy v1 预声明的 evolution receipt
  与 migration note；不得修改中央 policy/schema/helper/guard 或旧 SHA 表。
- [ ] T022-11 增加正常/异常/恶意/竞态测试及 A/B 双租户夹具；验证对象存在性
  错误等价和 cold archive 无 shared raw 扫描。
- [ ] T022-11a 针对 `ProfileSpaceSnapshotProviderV1` 的敌意 fake 矩阵：0 space、
  缺失/空 `profile_sha256`、`index_versions` 缺键/多键、未知 space ID、
  `default ⊄ queryable`、二次调用漂移、返回 slug/路径/正文/credential/他租户 ID，
  全部 fail closed 且外部错误与未授权等价（对应 AC-022-10）。
- [ ] T022-11b PilotAdmission 决定性矩阵：invalid/active 0 次，pilot first deny 1 次，
  success/cache hit/second deny 2 次；expiry/wrong tenant/purpose/hash、revision 回退、
  policy/snapshot 漂移、第二次拒绝后 EvidenceReader/partial bundle=0。
- [ ] T022-12 运行定向、相关 RT、Wiki smoke、全库回归、secret scan、
  `git diff --check`；记录真实命令与结果。
- [ ] T022-13 由独立 Agent 在未复用实现测试逻辑的临时环境执行黑盒攻击，
  出具 PASS/FAIL；实现 Agent 不得自批。

- [ ] T022-14 **RT-022 安全门 receipt（SG registry 声明的生产任务）。**
  由本 RT 的独立验收 Agent 在 `rt_independent_acceptance` 阶段按
  `cwk.pr001.security_gate_receipt.v1` 生成
  `PR/PR-001-multitenant-knowledge-spaces/security-receipts/RT-022/receipt.json`，
  `receipt_kind = rt-security`，consumer 恰为 `RT-026`（go/no-go）与 `G6`（final acceptance）。
  - `claims` 必须与 registry 中 RT-022 的集合逐字相等：`SGC-022-01`/SG-02、`SGC-022-02`/SG-03、`SGC-022-03`/SG-04、`SGC-022-04`/SG-06。
  - `filesystem_policy.mode = not_applicable_permitted`，且**仅** `SGC-022-02` 可标 N/A；
    标 N/A 时必须同时给出 registry 允许的 `reason_code`（`no_filesystem_write_surface`、`read_only_injected_provider`、`in_memory_only`、`owned_by_other_rt_by_design`）、
    自然语言 reason 与静态证据。其余 claim 仍须逐类给出
    `path_traversal`、`symlink_component`、`symlink_leaf`、`hardlink`、`toctou`、`special_file` 六类真实覆盖。
  - `independent_security_verification_pass` 是独立安全/预检结论，不等价于
    RT 最终 PASS；`verifier` 必须存在且不等于 `producer`；
    `tests_run > 0`、`tests_failed = 0`、`artifacts` 非空且逐条 sha256 匹配；
    `tested_subject_commit` 为被测候选 commit（evidence-only commit 的祖先）。
  - 未生成该 receipt 时，RT-022 在 SG 视角下为 NOT_RUN，RT-026 只能给 NO_GO。

## 完成定义

只有 T022-01～13（含 T022-03a/03b/04a/09a/11a/11b）全部有可复现证据，且独立验收报告明确
PASS，RT-022 才可标记 completed。fake provider PASS 不等于 RT-023/VG-D/G5 PASS，
也不等于 `ProfileSpaceSnapshotProviderV1` 生产 adapter 已存在（属 RT-021）。

# RT-012 独立验收报告（agent-rt012-verify-opus）

> Verdict：**PASS**（含 2 项 Minor，均非安全阻塞）
> 是否允许进入 RT-013 / RT-014：**是**（受下文 Minor 与残余 conservative_unknown 约束）
> 是否允许关闭 SG-02 / SG-07 / G5 / G7、批准 `verified_shared_extensions_vN` 升级：**否**（属 RT-023 / VG-D / G5 责任）
> 是否允许修改 RT-011 冻结契约：**否**（14 张 v1 schema、`security_defaults.json`、`verified_shared_extensions_v1.json`、`ADR-0001`、`cwk_pr001_*.py` 均须保持零字节漂移）
> 验收 Agent：`agent-rt012-verify-opus`（独立、未参与实现；仅新增本报告一个文件）
> 分支：`agent/cwk-rt012-verify-opus`（git worktree）
> 唯一新增文件：`RT/RT-012/reports/独立验收报告.md`

## 1. 基线与环境

| 项 | 值 |
| --- | --- |
| 验收 worktree | `/Users/evan/.openclaw/gateways/life/state/workspace-life/projects/CWK/.worktrees/openclaw-worktree-cwk-rt012-verify-opus` |
| RT-012 impl 基线 | `1894576b20a9e00d396718daeca0c781a7d233a6`（"RT-012 doc: strip trailing whitespace in delivery verification report"）；实现主 commit：`ed39946fc6fb6a0b67ce918154cdfa012dd3bb7e`（"RT-012 implementation: instance layout + tenant registry (G2)"） |
| RT-011 验收基线 | `e3aa3e029ec4e3ea3edac47b6a3fddf2f0b5be34`（RT-011 独立 PASS r2） |
| 上游 RT-010 hotfix | `6bae513` / `6835faa`（在 RT-011 verify 之后合入；本 RT 不修改也不回归 RT-010 覆盖） |
| Python | `Python 3.11.14` (`/opt/homebrew/bin/python3.11`) |
| Platform | `Darwin evandeMac-mini.local 25.5.0 arm64` |
| 依赖 | 仅 stdlib；RT-012 源码经 `grep -R` 确认无第三方 pip 依赖 |

写入策略：**仅新增本报告**；未编辑任何 RT-012 或 RT-011 的实现代码 / schema / fixture / 测试 / PRD / DESIGN / 计划 / RT index / legacy 文件；所有攻击夹具、隔离测试均在 `mktemp -d` 下创建并在结束时删除。全程未读取真实 `CWORK_APP_KEY`，未接入 Cloud/DocDB/工作协同/cron。

## 2. 基线与冻结契约漂移复核

### 2.1 RT-011 冻结产物零字节漂移（byte-for-byte）

对 e3aa3e0..1894576 内的 55 个 RT-011 blob 做 tree-object SHA 对齐：

```
diff <(git ls-tree e3aa3e0 -r -- PR/PR-001-multitenant-knowledge-spaces/contracts/ \
       scripts/cwk_pr001_*.py tests/test_pr001_*.py tests/fixtures/pr001/) \
     <(git ls-tree 1894576 -r -- PR/PR-001-multitenant-knowledge-spaces/contracts/ \
       scripts/cwk_pr001_*.py tests/test_pr001_*.py tests/fixtures/pr001/)
```

结果：新基线只新增 5 张 RT-012 自有 schema，55 个 RT-011 blob **object SHA 全部相同**：

- 14 张 v1 schema（canonical / tenant_view / access_grant / knowledge_profile / route_decision / query_request / capability_probe / sample_manifest / verified_shared_extensions / probe_receipt_envelope / conformance_receipt / probe_aggregate / dual_user_observation / redacted_probe）
- `security_defaults.json`、`verified_shared_extensions_v1.json`、`adr/ADR-0001-*`
- `cwk_pr001_contracts.py`、`cwk_pr001_probes.py`、`cwk_pr001_view_compare.py`、`cwk_pr001_cli.py`
- `tests/test_pr001_{contracts,probes,view_compare,malicious_fixtures,cli}.py`
- 28 项 fixture + manifest + canary

### 2.2 RT-012 impl 变更清单（`ed39946^..1894576`）

23 个新增文件、5658 行新增、0 行删除，全部落在 RT-012 边界内：

```
PR/PR-001-multitenant-knowledge-spaces/contracts/rt012/schemas/*.schema.json   (5 files)
scripts/cwk_atomic_file.py                                                     ( 589 lines)
scripts/cwk_instance.py                                                        ( 539 lines)
scripts/cwk_tenant_registry.py                                                 (1085 lines)
scripts/cwk_tenant_cli_api.py                                                  ( 160 lines)
scripts/cwk_tenant_cli.py                                                      ( 263 lines)
scripts/cwk_tenant_cmd_core.py                                                 ( 489 lines)
tests/test_rt012_{atomic_file,instance,registry,cli,attacks}.py                (5 files)
RT/RT-012/{rt-intake.md,specs/*,tasks/*,reports/{实现记录,交付验证报告}.md}     (7 files)
RT/index.yaml                                                                  ( +1 line, implementation_done)
```

RT-012 未触碰：`cwk_pr001_*.py` × 4、14 张 v1 schema、`security_defaults.json`、`verified_shared_extensions_v1.json`、`ADR-0001`、`cwk_collect_live.py`、`cwk_nightly_pipeline.py`、`cwk_wiki_query.py`、`cwk_entity_catalog.py`、`cwk_wiki_search_index.py`、`cwk_sync_mirror_to_docdb.py`、`Makefile`、`install.sh`、`.env.example`、`.serena/`、`.spec-workflow/`、`config/`、`docs/`、`knowledge/`、`RT/RT-001..RT-011`。

> 注：`git log e3aa3e0..1894576` 期间 `scripts/cwk_wiki_query.py` 出现于 diff，是 RT-010 hotfix（`6835faa`）合入所致，**非 RT-012 impl 修改**——`git diff ed39946^..ed39946 --name-only` 明确不含 `cwk_wiki_query.py`。

### 2.3 RT-012 公共 dispatcher / provider registry 归 RT-012

- `scripts/cwk_tenant_cli.py` 冻结 `FROZEN_PROVIDER_SLOTS = ("cwk_tenant_cmd_core",)`；RT-013/019/026 只能新增自己的 provider 模块名到该 tuple，不得改写 dispatcher。
- `scripts/cwk_tenant_cli_api.py` 冻结 `COMMAND_PROVIDER_API_VERSION = "v1"`；ABI 类型 `CommandContext / CommandSpec / DoctorFinding / CommandProviderV1 / CliError` + `STABLE_EXIT_CODES = (0, 2, 3, 4, 5, 6)`；接口结构可安全扩展（downstream 只填入 slot 名 + 提供 `list_commands`）。
- `CommandContext.env` 只透传 `CWK_INSTANCE_ROOT`；`CWK_APP_KEY` 等敏感环境变量不进入 provider handler。

## 3. 命令与实际输出（复现指纹）

### 3.1 编译 / 静态

```
python3.11 -m compileall -q scripts tests    # exit 0，无输出
git diff --check                              # exit 0，无输出
```

### 3.2 RT-012 定向测试

```
python3.11 -m unittest tests.test_rt012_atomic_file tests.test_rt012_instance \
  tests.test_rt012_registry tests.test_rt012_cli tests.test_rt012_attacks
# → Ran 111 tests in 4.692s — OK
```

### 3.3 RT-011 定向测试（冻结契约回归）

```
python3.11 -m unittest tests.test_pr001_contracts tests.test_pr001_probes \
  tests.test_pr001_view_compare tests.test_pr001_malicious_fixtures tests.test_pr001_cli
# → Ran 124 tests in 0.899s — OK
```

### 3.4 全量 Python 3.11 回归

```
python3.11 -m unittest discover -s tests -p 'test_*.py'
# → Ran 510 tests in 6.233s — OK (skipped=7)
# 分解：269（历史 RT-001..RT-010）+ 124（RT-011）+ 111（RT-012）+ 6 附加 = 510；
# 7 skipped 属既有条件式 fixture 缺失，与 RT-012 无关。
```

### 3.5 CLI 黑盒基线

在 `mktemp -d` 得到的 opaque `CWK_INSTANCE_ROOT` 下执行：

- `--help` → exit 0；输出 5 子命令（doctor/init/list/show/state-graph）；无 traceback。
- `state-graph` → exit 0；输出 `schema=cwk.rt012.state_graph.v1`；`states=[draft, profile_pending, pilot, active, suspended, offboarded]`；`terminal_state=offboarded`；`forbidden_aliases=[enabled, disabled, provisioning, retiring]`；`transitions` 与 PRD FR-02 精确一致。
- `init --actor admin --reason "tenant A"` → exit 0；opaque `tenant_id=t_35147y13ajvk36fujp3pucoob7`；`status=draft`；`auth_epoch=1`；`record_revision=1`；`provision_txn_id=txn_…`；`provision_receipt_sha256=8e52f979c4a3…`。
- `init` 再次执行 → exit 0；另一 opaque `tenant_id=t_uax588ui9z3vv80lkq30e8cats`。
- `list` → exit 0；`count=2`；`tenant_ids=[…]`。
- `doctor` → exit 0；`issue_count=0`。

## 4. 攻击矩阵与门禁复核（十项）

以下十项均由本 Agent 独立构造攻击夹具复现，不复用实现方测试作为“通过”依据。

### 4.1 T-04 路径穿越 / opaque ID 攻击 — **PASS**

12 项 `CWK_INSTANCE_ROOT` 攻击（真实子进程 env 注入，NUL 因 Python subprocess 边界拦截，其余原样传入）：

| Case | 输入 | 实际输出 | 判定 |
| --- | --- | --- | --- |
| unset | — | exit 4, `error: CWK_INSTANCE_ROOT is not set` | PASS |
| empty | `""` | exit 4, `error: CWK_INSTANCE_ROOT must not be empty` | PASS |
| whitespace | `"   "` | exit 4, `error: CWK_INSTANCE_ROOT has leading/trailing whitespace` | PASS |
| trailing/leading ws | `" /tmp "` | exit 4, 同上 | PASS |
| relative | `relative/path` | exit 4, `must be an absolute path` | PASS |
| nonexistent | `/does/not/exist/anywhere/xyz` | exit 4, `does not exist` | PASS |
| CR | `"/tmp\rattack"` | exit 4, `contains NUL/CR/LF` | PASS |
| LF | `"/tmp\nattack"` | exit 4, `contains NUL/CR/LF` | PASS |
| NUL | `"/tmp\x00attack"` | Python subprocess 边界即拒；resolver 亦有显式 `contains NUL/CR/LF` 分支（单元测试覆盖） | PASS |
| backslash Windows | `C:\Windows\Temp` | exit 4, `must be an absolute path` | PASS |
| backslash only | `\hostname\share\x` | exit 4, `must be an absolute path` | PASS |
| UNC | `//server/share` | exit 4, `does not exist` | PASS |
| URL 编码穿越 | `/tmp/%2e%2e/legit` | exit 4, `contains encoded traversal` | PASS |
| URL 编码 slash | `/tmp/%2fetc/passwd` | exit 4, `contains encoded traversal` | PASS |
| URL 编码 backslash | `/tmp/%5c` | exit 4, `contains encoded traversal` | PASS |
| 全角 slash | `／tmp／legit` | exit 4, `must be an absolute path` | PASS |
| 超长 | `"/" + "a"*5000` | exit 4, `is too long` | PASS |
| 非目录 | `/etc/hosts` | exit 4, `is not a directory` | PASS |

十七项 tenant_id 攻击（对 `show --tenant-id` 注入）：

| Case | 输入 | 实际输出 |
| --- | --- | --- |
| `traversal_rel` | `../../../etc/passwd` | exit 2, `does not match '\At_[a-z0-9]{26}\Z'` |
| `traversal_only` | `..` | exit 2, 同上 |
| `abs_path` | `/etc/passwd` | exit 2, 同上 |
| `backslash` | `t_\..\etc` | exit 2, 同上 |
| `double_encoded` | `t_%2e%2e` | exit 2, 同上 |
| `url_encoded_slash` | `t_%2fx` | exit 2, 同上 |
| `url_encoded_backslash` | `t_%5cx` | exit 2, 同上 |
| `wrong_prefix` | `u_ABCDEFGHIJKLMNOPQRSTUVWXYZ` | exit 2, 同上 |
| `uppercase` | `t_ABCDEFGHIJKLMNOPQRSTUVWXY` | exit 2, 同上 |
| `too_short` | `t_abcd` | exit 2, 同上 |
| `too_long` | `t_a` × 100 | exit 2, 同上 |
| `trailing_newline` | `t_…\n` | exit 2, 同上 |
| `leading_space` | ` t_…` | exit 2, 同上 |
| `fullwidth` | `t_ａbcdefghij…` (U+FF41) | exit 2, 同上 |
| `slash_inline` | `t_abc/def` | exit 2, 同上 |
| `empty` | `""` | exit 2, 同上 |
| `valid_but_nonexistent` | `t_abcdefghijklmnopqrstuvwxyz` | exit 5, `tenant '…' not found`（正确 TenantNotFound → EXIT_IO） |

**验证**：所有攻击 stderr **不出现宿主机绝对路径**；错误消息只回显合法逻辑名 / 稳定 regex 文本。tenant 目录树在 12 项攻击后 `os.walk` 无新增。

### 4.2 T-04 符号 / 硬链接攻击 — **PASS**

- **root 是 symlink**（`CWK_INSTANCE_ROOT=./symlink_to_dir`）：exit 4, `is a symbolic link`；`target_dir` 内保持为空。
- **`registry/tenants/` 被替换为符号链接指向 `/tmp/escape_dst`**：exit 6, `internal failure (LayoutError)`（fail-closed，见 Minor-1）；`escape_dst` 全程为空目录（**无字节写入到 root 外部**）。仅 `attack_root/` 内的 7 个冻结顶层子目录 `initialize()` 创建（预期行为——`layout.initialize()` 早于 registry 打开）。
- **A 的 `tenants/<TA>/state` 被替换为符号链接**：直接编程 API 调 `TenantLayout.child_fd("state")` 返回 `LayoutError: child 'state' is not a directory`；随后 `cwk-tenant doctor` exit 2, `issue_count=1`, `tenant_child_symlink:<TA>/state: child is a symlink`；`attack_state_dst` 全程为空；B 的 `state/` 目录 inode 与 mtime 未变。
- **tenant record 硬链接**（`ln registry/tenants/<TA>.json /tmp/hardlink_target/stolen.json`）：`registry.get(<TA>)` 抛 `ContainmentError: child has more than one hard link; refusing to read`（`read_file` 里 `st.st_nlink != 1` 检查）。
- **provider 模块被替换为 symlink**（把 `scripts/cwk_tenant_cmd_shim.py` 指向 tempdir 内的假 provider，再调 `_load_provider("cwk_tenant_cmd_shim")`）：`_ProviderLoadError: provider file is a symlink: cwk_tenant_cmd_shim.py`。

### 4.3 T-11 TOCTOU / atomic 断点 — **PASS**

- **孤儿 `.cwk-tmp-*`**：手工在 `registry/tenants/`、`registry/provision-journal/`、`registry/provision-receipts/` 各塞一个 `.cwk-tmp-fake.*` 文件 → `TenantRegistry.recover()` 返回 `orphans_removed=3`；重跑 → 全部 `0`（幂等）；registry 目录只剩合法 tenant record / lock。
- **半提交崩溃 + 空 staging 树**：手工模拟 `journal → dir tree → (crash)` 状态（journal 存在，tenant 目录树只有 15 个空子目录，无 receipt、无 record）→ `recover()` 返回 `uncommitted_rolled_back=1`；tenant 目录树被安全回滚；A/B 未受影响。
- **半提交崩溃 + 已被 RT-013+ 填充的 staging 树**：同上，但先在 `tenants/<T>/state/important.json` 写入 `{"downstream": "data"}` → `recover()` 返回 `uncommitted_rolled_back=0`；tenant 目录树**保留**（含 `important.json` 内容与 sha 不变）；journal 被 unlink。**规则符合技术方案 §6：只回滚"完全未提交且完全为冻结空 dir"的树，任何 downstream 数据都不动。**
- **半 JSON**：直接向 `registry/tenants/<TA>.json` 写 `\x00\x01\x02not-json` → `show --tenant-id <TA>` exit 2, `error: tenant record for '…' is not JSON: Expecting value`（`RecordCorruption` → `EXIT_CONTRACT`）。RT-012 不把损坏 record 当空（不能被攻击者 truncate 后 re-init）。
- **duplicate JSON key**（`{"schema":"…v1","schema":"different"}`）→ exit 2, `is not strict JSON: <json>: duplicate JSON key 'schema'`。

**注**：`recover()` summary 计数器在 "journal 无 receipt 且 tenant tree 从不存在" 时未增 `journal_swept`（仍会 unlink journal），只是计数器 undercount，功能安全——记为 Minor-2。

### 4.4 A/B 物理隔离 — **PASS**

- 在双租户 `legit_root` 中：
  1. 用 `printf '\x00\x01\x02not-json' > registry/tenants/<TA>.json` **破坏** A 的记录；
  2. 用 `printf 'garbage' > tenants/<TA>/config/tenant.projection.json` 破坏 A 的投影；
  3. 后台进程持 `flock(EX)` 抓住 `.<TA>.lock` 5 秒。
- 期间：
  - `show --tenant-id <TA>` → exit 2, `RecordCorruption`（受 Minor-1 影响，具体 stderr 为 `is not JSON: Expecting value`），无 traceback；
  - `show --tenant-id <TB>` → exit 0，正常返回；
  - Python API `reg.bump_auth_epoch(<TB>, expected_auth_epoch=1)` → 成功；auth_epoch=1→2，`record_revision=1→2`；`state_history` 追加。
- B 目录树完整快照对比（before vs after）：**唯一变化就是 `/config/tenant.projection.json`**（因 B 的 auth_epoch 主动 bump）；A 的破坏未泄漏 inode/content/mtime 到 B。
- Lock/state/run/log/temp/config/view/space 目录完全独立——A 的 lock 抓住不阻塞 B（B 的 `.<TB>.lock` 独立）。

### 4.5 registry 载荷篡改 fail-closed — **PASS**

16 项 tampering 案例（在 tenant 记录里注入或改写）：

| Case | 篡改 | rc | stderr 关键字 |
| --- | --- | --- | --- |
| `unknown_top_key` | 顶层加 `attacker_field` | 6 | `SchemaError`（Minor-1 exit code taxonomy） |
| `duplicate_json_key` | 双 schema key | 2 | `duplicate JSON key 'schema'` |
| `tenant_id_mismatch` | payload 里 `tenant_id=<TB>` | 2 | `tenant record filename does not match tenant_id` |
| `wrong_schema` | `schema="attacker.v99"` | 6 | `SchemaError` |
| `neg_epoch` | `auth_epoch=-1` | 6 | `SchemaError` |
| `zero_epoch` | `auth_epoch=0` | 6 | `SchemaError` |
| `bool_epoch` | `auth_epoch=True` | 6 | `SchemaError` |
| `huge_epoch` | `auth_epoch=2**60` | 6 | `SchemaError` |
| `cred_set` | `credential_ref="secret://leaked"` | 6 | `SchemaError` |
| `profile_set` | `active_profile_version="v1"` | 6 | `SchemaError` |
| `path_field` | 加 `mirror_root="/etc/passwd"` | 6 | `SchemaError` |
| `agent_field` | 加 `agent_id="root"` | 6 | `SchemaError` |
| `bad_quota_scheme` | quota.scheme=`attacker.v1` + `disk_bytes=0` | 6 | `SchemaError` |
| `bad_status_alias_disabled` | `status="disabled"` | 6 | `SchemaError` |
| `bad_status_alias_provisioning` | `status="provisioning"` | 6 | `SchemaError` |
| `nul_in_actor` | `state_history[0].actor="root\x00owned"` | 0 | 见 Minor-3 |

**结论**：15/16 攻击 fail-closed（要么 EXIT_CONTRACT=2 要么 EXIT_INTERNAL=6，均不返回 0、均不显示 tenant 记录内容、均不泄漏路径）。所有 credential_ref / active_profile_version / agent_id / mirror_root / 别名 status / 错误 quota / 超范围 epoch / bool 冒充 int 都被 schema 严格拒绝。CAS `expected_auth_epoch=0`（回退攻击）也被拒（`RegistryConflict: expected auth_epoch 0, got 2`）。

### 4.6 FR-02 六态 FSM + 操作矩阵 — **PASS**

- `TENANT_STATES == ('draft','profile_pending','pilot','active','suspended','offboarded')`；`TERMINAL_STATE == 'offboarded'`；`TENANT_ALLOWED_TRANSITIONS` **精确匹配** PRD FR-02（6 键，`suspended→{profile_pending,pilot,active,offboarded}` 与 offboarded=∅ 均一致）。
- 36 对 `(src,dst)` 全枚举 `assert_valid_transition`：**零非法边被接受、零合法边被拒**。
- 四类别名 `enabled/disabled/provisioning/retiring` 在 src 与 dst 位置都被 `assert_valid_transition` 抛 `RegistryError code=state`。
- `TENANT_OPERATION_MATRIX['draft'] == {'admin_configure'}`；`['suspended'] == {'admin_configure'}`；`['offboarded'] == frozenset()`；`profile_pending` 允许 `sampling_collect_bounded/profile_ai/profile_confirm/credential_resolve`（**不含** `collector_run/scheduler_run/query_broker`）；`pilot` 与 `active` 都包含数据面完整集合。**下游 `credential_resolve/collector_run/scheduler_run/query_broker` 不出现在 draft/suspended/offboarded**（本 RT 属矩阵冻结）。
- 恢复 verifier：`VerifierRequired` 保留类存在，但 RT-012 无 `resume_from_suspended` 公共 API；不存在任何在没有 receipt 的情况下把 `suspended → *` 静默 flip 的路径。**未知恢复情境不会虚构 `verified`。**
- **`TenantRegistry` 没有任意 `transition_to(state)` API**；只有 `init_tenant`（写 `draft`）+ `bump_record_revision` + `bump_auth_epoch`（各带 CAS）。要执行合法状态跃迁必须由 RT-013+ 新增 provider，并通过策略函数校验。

### 4.7 dispatcher / provider — **PASS**

- `cwk-tenant --help` → exit 0；5 子命令齐全；无 traceback。
- `cwk-tenant`（无子命令）→ exit **4**（成功修复 RT-011 遗留的 Minor-1，不再回落 0）。
- `cwk-tenant nonexistent` → exit 4；argparse `invalid choice`。
- `cwk-tenant init` (无 --actor) → exit 4；`the following arguments are required: --actor`。
- `cwk-tenant show --not-a-flag` → exit 4。
- **PYTHONPATH 注入**：在 tempdir 写一个 `cwk_tenant_cmd_core.py`（`raise RuntimeError('OWNED')`），设 `PYTHONPATH=<tempdir>` → `cwk-tenant list` exit 0；无 stderr；**dispatcher 完全忽略 PYTHONPATH**（只从 `Path(__file__).resolve().parent` 加载）。
- **CWD 注入**：在 tempdir 写同名 shadow 模块，`cwd=<tempdir>` → 同上 exit 0，无污染。
- **secret env 注入**：`CWK_APP_KEY=SUPER-SECRET-…` → 不出现在 stdout/stderr（`_safe_env_snapshot()` 只白名单 `CWK_INSTANCE_ROOT`）。
- Provider 名 grammar：`/etc/passwd` / `../evil` / `cwk_tenant.malicious` / `cwk-tenant-cmd` / `with spaces` / `UPPER` 全被 `_ProviderLoadError: provider name '…' out of grammar` 拒。
- Provider 符号链接：`ln -s /tmp/fake scripts/cwk_tenant_cmd_shim.py` + `_load_provider("cwk_tenant_cmd_shim")` → `_ProviderLoadError: provider file is a symlink`（我在测试结束前 `os.unlink(sym)`，未污染仓库）。
- ABI 版本校验：假 provider `API_VERSION='v99'` → `_ProviderLoadError: provider … API_VERSION='v99' != 'v1'`。
- `FROZEN_PROVIDER_SLOTS = ('cwk_tenant_cmd_core',)`；未来 RT-013/019/026 只能加入 tuple。
- STABLE_EXIT_CODES = `(0, 2, 3, 4, 5, 6)`；任何 handler 返回非法码 → 强制 `EXIT_INTERNAL=6` 且打印稳定 `error: handler returned non-stable exit code …`。

### 4.8 并发 init/CAS + 中断恢复 — **PASS**

- **8 个并行 `init`**（同 root，`ThreadPoolExecutor(max_workers=8)`）：`success=8 fail=0`；`list.count=8`；生成 8 个不同 opaque tenant_id；每个 tenant 的 record_revision=1、auth_epoch=1、state_history 只有一条 `draft` 记录。
- **8 个并行 `bump_auth_epoch(expected_auth_epoch=1)`** 同一 tenant：**恰好 1 个成功，7 个 `RegistryConflict`**；成功者观察 `auth_epoch=1→2`；`record_revision=1→2`；record + receipt + projection 三处 sha 一致。CAS 语义正确（fcntl.flock + read-verify）。
- `TenantRegistry.recover()` 幂等：连续 3 次调用后计数器均为 `{'journal_swept': 0, 'orphans_removed': 0, 'uncommitted_rolled_back': 0}`；所有 tenant 的 `auth_epoch/record_revision` 未回退。
- `init_tenant(tenant_id=<existing>)` → `TenantExists` → EXIT_CONFLICT=3。
- `init_tenant(tenant_id="t_user-derived-name")` → `TenantIdError` → EXIT_CONTRACT=2（用户命名的 tenant_id 被 RT-011 regex 拒）。
- Epoch 回退：`bump_auth_epoch(expected_auth_epoch=0)` → `RegistryConflict: expected auth_epoch 0, got 2`（不会覆盖 active record）。

### 4.9 显式 root 与 legacy 隔离 — **PASS**

- 全过程使用 `mktemp -d` 出的 opaque root；`cwk-tenant init/list/doctor` 系列命令执行前后：worktree 内 `.env / runs / state / knowledge` 保持"不存在"；`.env.example`（唯一存在的 legacy 文件）+ `config/entity-family-registry.json` **SHA-256 完全不变**（`before` == `after` `diff -u` 空）。
- `grep -R` 确认 RT-012 全部 6 个 script 文件（`cwk_atomic_file / cwk_instance / cwk_tenant_registry / cwk_tenant_cli / cwk_tenant_cli_api / cwk_tenant_cmd_core`）**不引用** `cwk_collect_live / cwk_nightly_pipeline / cwk_wiki_query / cwk_entity_catalog / cwk_wiki_search_index / cwk_sync_mirror_to_docdb`。
- 同样 `grep -R` 确认 RT-012 源码**不引用** `CWORK_APP_KEY / crontab / cron.d / DocDB / feature.flag / FEATURE_FLAG`。
- `CWK_INSTANCE_ROOT=/etc/hosts` → exit 4, `is not a directory`；stderr 不含宿主机路径回显（`_safe_path` 只保留尾部 2 段，实际这里根本不触发路径回显）。
- Range 未触及 collector/nightly/query/cron：RT-012 全部代码不导入这些模块，也未写 `Makefile` / `install.sh` / `.env.example` / `docs/`。

### 4.10 定向测试 + 全量回归 + git diff --check + Wiki smoke — **PASS**

- RT-012 定向 `Ran 111 tests in 4.692s — OK`。
- RT-011 定向 `Ran 124 tests in 0.899s — OK`。
- 全量 discover `Ran 510 tests in 6.233s — OK (skipped=7)`。
- `python3.11 -m compileall -q scripts tests` → 0。
- `git diff --check` → 0。
- **Wiki smoke**：本 worktree 缺 `knowledge/工作协同镜像/wiki` 未提交镜像数据（`ls: knowledge: No such file or directory`），`python3.11 scripts/cwk_wiki_smoke_test.py` 输出 `overall=FAIL failed=9/9`——**全部因数据缺失**，与 RT-012 修改无关。RT-012 未修改 `cwk_wiki_query.py / cwk_entity_catalog.py / cwk_wiki_search_index.py / cwk_wiki_smoke_test.py`（`git diff --name-only ed39946^..1894576` 已确认），因此声明"隔离 worktree 缺未提交镜像，Wiki smoke 无法作为回归证据；实现方 `交付验证报告 §1.12` 里的主 worktree PASS 与本 RT 代码变更无冲突"。**未将其算入 PASS。**

## 5. Blocker / Major / Minor 汇总

### Blocker — 无

未复现任何跨租户泄漏、跨租户串扰、路径穿越、symlink 逃逸、身份伪造、密钥泄漏、状态别名、offboarded 出边、缓存串扰、缺文件当空、legacy 回退、cron/DocDB/AppKey 触达。**所有关键安全不变量全部保持。**

### Major — 无

未观察到 SG-03（文件系统）/ NFR-01（隔离）/ NFR-03（完整性）/ NFR-04（故障隔离）任何 major 违反。

### Minor（不阻塞 RT-013 / RT-014；建议在下一 RT 顺手处理）

- **Minor-1（新观察）**：`SchemaError` / `LayoutError` 未被 `_cmd_show` / `_cmd_init` 精确捕获，会被 dispatcher 顶层 `except Exception` 兜底为 `error: internal failure (<Exception>)` + `EXIT_INTERNAL=6`。行为 fail-closed（拒读 / 不写数据），无 traceback、无绝对路径，但退出码语义不精确——**契约违反本应 `EXIT_CONTRACT=2`**。示例：`registry/tenants/<TA>.json` 内 `auth_epoch=-1` / `credential_ref="secret://x"` / `status="disabled"` 等 tampering 全部走 exit 6 而非 exit 2；同理 `registry/tenants/` 被替换为 symlink 后 `init` 走 exit 6 而非 exit 5。修法建议：`_cmd_show / _cmd_init / _cmd_list / _cmd_doctor` 在 `except R.RegistryError` 之外再 `except (R.SchemaError, I.LayoutError, R.RecordCorruption)` 精确分派。**属退出码 taxonomy 校准，不动数据面语义**，可与 RT-013 一起做。

- **Minor-2（新观察）**：`TenantRegistry.recover()` 的 summary 计数器在"孤儿 journal 无对应 tree 也无对应 record"分支下未增 `journal_swept`，虽 journal 会被 `unlink_at` 正确清理（下一次 recover 观测为空）。属日志/审计观测项，不影响安全语义。

- **Minor-3（新观察）**：RT-012 tenant record schema `state_history[].actor` 只声明 `type:string, minLength:1, maxLength:128`，未禁 NUL / 控制字符；attacker 若能直接改写磁盘记录，可将 `actor="root\x00owned"` 通过 `_validate_schema`（Python `json` 不禁 NUL 字符）。字段仅用作审计文字，未参与路径/tenant_id/身份决策，不构成安全逃逸；但与 RT-011 `strict_json_loads` 的 NFC 严格性精神一致，建议 RT-012 或 RT-024 在 `validate_tenant_record` 深度扫描 `actor / reason` 拒 NUL / 控制字符。

上述三项 Minor 均**非安全阻塞**，独立验收判定"允许进入 RT-013 / RT-014"。

## 6. 与实现方 `交付验证报告.md` 的 receipt 对齐

- §1.1 `compileall` — 复现，exit 0。
- §1.3 RT-012 定向 111 tests — 复现，`Ran 111 tests in 4.692s — OK`。
- §1.4 RT-011 定向 124 tests — 复现，`Ran 124 tests in 0.899s — OK`。
- §1.5 全量 510 tests — 复现，`Ran 510 tests in 6.233s — OK (skipped=7)`。
- §1.7 `git diff --check` — 复现，exit 0。
- §1.8 `--help` 5 子命令 — 复现。
- §1.9 `state-graph` 输出 — 复现，全部字段一致。
- §1.10 `init → show → list → doctor` — 复现，`issue_count=0`。
- §1.11 CLI 攻击矩阵 8 行 — 独立复现（并追加 12 项 InstanceRoot + 17 项 tenant_id 变体，全部符合表述）。
- §1.12 Wiki smoke — 与实现方一致：本 worktree 因缺镜像数据 FAIL；主 worktree PASS。本报告不把 worktree 内 FAIL 计为回归。

## 7. 残余 conservative_unknown（禁止升级）

以下事实继续由 RT-011 冻结为 `conservative_unknown`，**本 RT-012 独立验收不解锁**（RT-011 verify r2 §7 已锁死）：

- `report_id_global_uniqueness`；
- `permission_authoritative_events` / `permission_authoritative_api`；
- `trusted_agent_identity_openclaw_tool` / `trusted_agent_identity_uds_peercred`；
- `sandbox_transport_openclaw_tool` / `sandbox_transport_uds`；
- `verified_shared_extensions_dual_user_sample`；
- `sandbox_transport_loopback_http_self_reported`（政策永禁）；
- 每租户配额数值（`quota.limits.*` 全部 `null`；RT-024 测量，RT-026 冻结）。

RT-012 未提供也未使用任何"伪造 verified"通道；tenant 配额结构冻结为 `cwk.rt012.quota.unset.v1`，所有 limit `null`。

## 8. 允许下一步（RT-013 / RT-014 准入）

RT-012 独立验收 **PASS**：

- 允许启动 **RT-013**（Agent binding + credential broker）与 **RT-014**（canonical evidence store），两者可并行；
- 允许保留 RT-011 冻结契约（zero drift 已核）；
- 允许 RT-013 通过新增 `cwk_tenant_cmd_binding.py` provider 加入 `FROZEN_PROVIDER_SLOTS`（保持 dispatcher 不变）；
- **不允许** RT-013/014 修改：
  - `scripts/cwk_atomic_file.py` / `scripts/cwk_instance.py` / `scripts/cwk_tenant_registry.py` / `scripts/cwk_tenant_cli.py` / `scripts/cwk_tenant_cli_api.py` / `scripts/cwk_tenant_cmd_core.py`；
  - `PR/PR-001-multitenant-knowledge-spaces/contracts/rt012/schemas/*`；
  - RT-011 冻结的 14 张 schema、`security_defaults.json`、`verified_shared_extensions_v1.json`、`ADR-0001`、`cwk_pr001_*.py`；
  - RT-012 状态机与操作矩阵（`TENANT_STATES / TENANT_ALLOWED_TRANSITIONS / TENANT_OPERATION_MATRIX / TERMINAL_STATE`）；
- **不允许** 关闭 SG-02 / SG-07 / G5 / G7；
- **不允许** 批准 `verified_shared_extensions_vN` 升级（属 RT-023 / VG-D / G5）。

Minor-1/Minor-2/Minor-3 建议在 RT-013 或 RT-024 内顺手闭合，不作为 RT-012 阻塞。

## 9. 最终 Verdict

**PASS。**

- RT-012 交付的 Instance Layout / Tenant Registry / dispatcher / provider ABI / doctor 在
  路径穿越（12 项 root + 17 项 tenant_id）、symlink / hardlink（root / registry-tenants /
  tenant-subtree / provider）、TOCTOU 半提交（4 项 recover 场景）、A/B 隔离（含 lock /
  registry / projection）、registry 载荷篡改（16 项）、六态 FSM（36 对 + 4 项别名）、
  dispatcher 恶意 CWD/PYTHONPATH/env/secret（7 项）、并发 CAS（8×8）、legacy 隔离
  ——**九个独立攻击类别全部 fail-closed**，与 PRD FR-02 / FR-04 / FR-05 / NFR-01 /
  NFR-03 / NFR-04、DESIGN §C-01 / C-02、`安全威胁模型.md` T-04 / T-06 / T-07 / T-11、
  以及 `RT/RT-012/rt-intake.md` 与 `specs/需求契约.md` 冻结口径一致。
- RT-011 冻结 55 个 blob 零字节漂移；RT-012 impl 未触碰 RT-011 / legacy / cron / AppKey /
  DocDB；`grep -R` 独立复核，`cwk_pr001_*.py` 与冻结 schema/security_defaults 未被读
  写为可变。
- **允许合并 RT-012 并进入 RT-013 / RT-014**，附前述 3 项 Minor 与 8 项残余
  `conservative_unknown` 约束。

**结论字段（机器可解析）**：

- `contract_conformance = PASS`
- `rt011_frozen_drift = ZERO_BYTES`
- `attack_matrix = ALL_PASS`（无 Blocker、无 Major、3 项 Minor）
- `fact_status_snapshot = conservative_unknown_frozen_by_RT011`
- `next_rt_gate = RT-013 && RT-014 open`
- `not_allowed = { close_SG-02, close_SG-07, close_G5, close_G7, upgrade_verified_shared_extensions_vN, modify_RT-011_frozen_artefacts, modify_RT-012_dispatcher_or_state_machine }`

## 10. 附录：本验收 Agent 使用的独立命令一览

```
# 1. baseline
git rev-parse HEAD                                # 1894576b20a9e00d…
git log --oneline 92cf40a..HEAD                   # 1894576 → ed39946 → 6bae513 → 6835faa → e3aa3e0
git rev-parse e3aa3e0 ed39946 1894576             # 三处基线 commit
python3.11 --version                              # Python 3.11.14

# 2. RT-011 frozen byte-for-byte
diff <(git ls-tree e3aa3e0 -r -- <RT-011 paths>) \
     <(git ls-tree 1894576 -r -- <RT-011 paths>)  # +5 new RT-012 schemas only

# 3. compile / whitespace / regression
python3.11 -m compileall -q scripts tests         # exit 0
git diff --check                                  # exit 0
python3.11 -m unittest tests.test_rt012_*         # 111 OK
python3.11 -m unittest tests.test_pr001_*         # 124 OK
python3.11 -m unittest discover -s tests -p 'test_*.py'   # 510 OK (7 skipped)

# 4. CLI baseline
CWK_INSTANCE_ROOT=<mktemp -d> python3.11 scripts/cwk_tenant_cli.py --help          # exit 0
CWK_INSTANCE_ROOT=<mktemp -d> python3.11 scripts/cwk_tenant_cli.py state-graph     # exit 0
CWK_INSTANCE_ROOT=<mktemp -d> python3.11 scripts/cwk_tenant_cli.py init --actor…   # exit 0 → 2 tenants
CWK_INSTANCE_ROOT=<mktemp -d> python3.11 scripts/cwk_tenant_cli.py list            # exit 0, count=2
CWK_INSTANCE_ROOT=<mktemp -d> python3.11 scripts/cwk_tenant_cli.py doctor          # exit 0, issue=0

# 5. Instance root attacks (Python subprocess env injection; NUL blocked at boundary)
env CWK_INSTANCE_ROOT=<unset|""|"   "|" /tmp "|relative|/nonexistent|"/tmp\rattack"|"/tmp\nattack"|
      "C:\\..."|"\\hostname\\share\\x"|"//server/share"|"/tmp/%2e%2e/legit"|"/tmp/%2fetc/passwd"|
      "/tmp/%5c"|"／tmp／legit"|"/aaaa..."|"/etc/hosts"> \
      python3.11 scripts/cwk_tenant_cli.py init/list/show  # every case → exit 4, no cwd leak

# 6. tenant_id attacks
python3.11 scripts/cwk_tenant_cli.py show --tenant-id <traversal|abs|backslash|url_encoded|
      wrong_prefix|uppercase|too_short|too_long|newline|leading_space|fullwidth|slash_inline|empty>
      # → exit 2 tenant_id regex reject (except valid but nonexistent → exit 5 TenantNotFound)

# 7. Symlink / hardlink attacks
CWK_INSTANCE_ROOT=<symlink_to_dir> …               # exit 4, "is a symbolic link"
ln -s <escape_dst> <root>/registry/tenants; init   # exit 6 (Minor-1), escape_dst untouched
ln -s <attack_state_dst> <root>/tenants/<TA>/state; doctor
     # exit 2, "tenant_child_symlink"; state target untouched; B unaffected
ln <root>/registry/tenants/<TA>.json /tmp/stolen.json; reg.get(<TA>)
     # ContainmentError: nlink>1 refusing to read
ln -s /tmp/fake scripts/cwk_tenant_cmd_shim.py; _load_provider("cwk_tenant_cmd_shim")
     # _ProviderLoadError: symlink refused

# 8. TOCTOU / atomic
touch <root>/registry/tenants/.cwk-tmp-fake.123456 …; reg.recover()   # orphans_removed=3
manual journal + empty tree; reg.recover()                            # uncommitted_rolled_back=1
manual journal + populated tree; reg.recover()                        # uncommitted_rolled_back=0
printf '\x00\x01…' > <TA>.json; show --tenant-id <TA>                 # exit 2
echo '{"schema":"…","schema":"other"}' > <TA>.json; show               # exit 2 duplicate key

# 9. A/B isolation
Corrupt A record + projection + hold flock on A; show B; bump B.auth_epoch
     # B success, only tenant.projection.json changed for B, all A protected

# 10. Registry payload tampering (16 cases; see §4.5)
JSON edits: unknown_key / dup_key / tenant_id_mismatch / wrong_schema / neg_epoch /
            zero_epoch / bool_epoch / huge_epoch / cred_set / profile_set / path_field /
            agent_field / bad_quota_scheme / bad_status_alias(disabled|provisioning) /
            nul_in_actor
     # 15/16 fail-closed (exit 2 or 6 with SchemaError/RecordCorruption); nul_in_actor → Minor-3

# 11. FSM
Enumerate 36 (src,dst) pairs; enumerate 4 aliases × {src,dst}; enumerate operation matrix
     # all match PRD FR-02; aliases rejected everywhere; no arbitrary transition API

# 12. Dispatcher / provider
--help / no-cmd / unknown / missing --actor / bad flag / doctor --help
     # all stable rc (0 / 4)
PYTHONPATH=<tempdir with shadow cwk_tenant_cmd_core.py>; list          # exit 0 (ignored)
cwd=<tempdir with shadow>; list                                        # exit 0 (ignored)
CWK_APP_KEY=SUPER-SECRET-…; list                                       # not leaked
_load_provider("/etc/passwd" | "../evil" | …)                          # grammar rejected
_load_provider("cwk_tenant_cmd_shim" symlink)                          # symlink rejected
_load_provider("cwk_tenant_cmd_wrongabi" v99)                          # ABI mismatch rejected
FROZEN_PROVIDER_SLOTS                                                  # ('cwk_tenant_cmd_core',)

# 13. Concurrent init / CAS
concurrent.futures ThreadPoolExecutor(8) × init                        # 8 unique tenants
concurrent.futures ThreadPoolExecutor(8) × bump_auth_epoch(expected=1) # exactly 1 ok, 7 conflict
reg.recover() × 3                                                      # idempotent
init_tenant(existing) / init_tenant("t_user-name") / bump(expected=0)  # all rejected

# 14. Legacy isolation
sha256 snapshot of .env/.env.example/runs/state/knowledge/config       # unchanged before/after
grep -R RT-012 sources for cwk_collect_live / cwk_nightly_pipeline /
      cwk_wiki_query / cwk_entity_catalog / cwk_wiki_search_index /
      cwk_sync_mirror_to_docdb / CWORK_APP_KEY / crontab / cron.d /
      DocDB / FEATURE_FLAG                                             # zero matches

# 15. Cleanup
rm -rf <all mktemp dirs>; git status --short --untracked-files=all      # clean
```

（本报告为唯一新增文件；未修改任何实现代码 / schema / fixture / 测试 / PRD / DESIGN / 计划 / RT index / legacy 文件；已 `git diff --check` 通过；`git status --short --untracked-files=all` 只显示本报告。）

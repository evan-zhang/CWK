# RT-012 交付验证报告（实现方）

> 状态：实现完成；**独立验收 PASS 由 `agent-rt012-verify` 单独出具**。
> 本文件只记录客观 receipt；不包含 “通过 / PASS” 结论。
> 基线提交：RT-011 独立 PASS r2（`92cf40a` / `e3aa3e0`）+ RT-010 hotfix。
> 分支：`agent/cwk-rt012-impl-opus`（git worktree）。

## 1. 命令与结果

### 1.1 `python3.11 -m compileall -q scripts tests`

- 结果：无输出（成功）。

### 1.2 `python3.11 -m py_compile scripts/cwk_atomic_file.py scripts/cwk_instance.py scripts/cwk_tenant_registry.py scripts/cwk_tenant_cli_api.py scripts/cwk_tenant_cli.py scripts/cwk_tenant_cmd_core.py`

- 结果：无输出（成功）。

### 1.3 RT-012 定向测试

```
python3.11 -m unittest \
  tests.test_rt012_atomic_file \
  tests.test_rt012_instance \
  tests.test_rt012_registry \
  tests.test_rt012_cli \
  tests.test_rt012_attacks
```

- 结果：`Ran 111 tests in 4.429s — OK`。
- 覆盖：
  - `test_rt012_atomic_file`（20 tests）：leaf 校验、`O_NOFOLLOW` 拒 symlink、hardlink 拒读、CAS 冲突、
    fcntl.flock 非阻塞冲突与进程死亡释放、orphan recovery 幂等。
  - `test_rt012_instance`（26 tests）：`CWK_INSTANCE_ROOT` 12 攻击变体（unset/relative/whitespace/
    NUL/CR-LF/backslash/UNC/编码 traversal/symlink/非目录/不存在/normal）、opaque tenant_id 
    全部拒绝矩阵、initialize 幂等 + 0o700、child_fd 拒绝未知 leaf/symlink、layout descriptor。
  - `test_rt012_registry`（30 tests）：六态断言、无 alias、CAS bump（含 overflow/bool 冒充 int 拒）、
    损坏不当空、recover 幂等 + 未提交回滚 + 已填充不动、init 拒 user-derived ID、provisioning
    receipt SHA 与 record 互锁、projection 与 record revision 一致。
  - `test_rt012_cli`（14 tests）：help/no-cmd/unknown 稳定退出码 + 无 traceback、init→show→list 
    正流程、doctor 三种情形、provider ABI/symlink/缺 API_VERSION 拒绝、
    恶意 CWD/PYTHONPATH 提供同名模块被忽略。
  - `test_rt012_attacks`（21 tests）：RT-011 frozen files 存在且无 RT-012 marker 泄漏、
    instance root 攻击矩阵（unicode/Windows/UNC/relative/unset）、tenant_id 完整攻击矩阵
    （traversal/URL 编码/大小写/尾换行/全角）、双租户隔离（bump/lock/损坏）、softlink 攻击
    （state 目录/registry json）、schema 攻击（duplicate JSON key/负数/超 2^53/未知 status）、
    secret scan（CLI 输出/源码不读 AppKey）、rollback 安全性（refuse-to-touch-populated-tree /
    idempotent）。

### 1.4 RT-011 定向测试（drift check）

```
python3.11 -m unittest \
  tests.test_pr001_contracts tests.test_pr001_probes \
  tests.test_pr001_view_compare tests.test_pr001_malicious_fixtures \
  tests.test_pr001_cli
```

- 结果：`Ran 124 tests in 0.879s — OK`。RT-011 冻结契约在 RT-012 变更下未回归。

### 1.5 现有全量回归

```
python3.11 -m unittest discover -s tests -p 'test_*.py'
```

- 结果：`Ran 510 tests in 6.303s — OK (skipped=7)`。
- 分解：269（历史 RT-001..RT-010）+ 124（RT-011）+ 111（RT-012）= 504 主测试 + 7 skipped
  条件式（可选 fixture 缺失，与 RT-012 无关）。

### 1.6 `PYTHON=python3.11 make test`

- 结果：`make-test exit=0`。
- 步骤：`doctor` PASS → `py_compile scripts/*.py` PASS → 510 unittests → `smoke` /
  `smoke-ai` / `smoke-ai-degraded` 均通过（生成的 `runs/ci-smoke*` 是运行副产品，未提交）。

### 1.7 `git diff --check`

- 结果：无输出（无空白错误）。

### 1.8 `python3.11 scripts/cwk_tenant_cli.py --help`

- 退出码 0；stdout 列出 5 个子命令（doctor/init/list/show/state-graph）；无 traceback；
  无宿主机绝对路径。

### 1.9 `python3.11 scripts/cwk_tenant_cli.py state-graph`

- 退出码 0；JSON 输出 `schema=cwk.rt012.state_graph.v1`；`states=[draft, profile_pending,
  pilot, active, suspended, offboarded]`；`terminal_state=offboarded`；`forbidden_aliases=
  [enabled, disabled, provisioning, retiring]`。

### 1.10 init → show → list → doctor 端到端

```
export CWK_INSTANCE_ROOT=$(mktemp -d)
python3.11 scripts/cwk_tenant_cli.py init --actor admin       # exit 0，返回 opaque tenant_id
python3.11 scripts/cwk_tenant_cli.py show --tenant-id <tid>   # exit 0，status=draft, auth_epoch=1
python3.11 scripts/cwk_tenant_cli.py list                     # exit 0，count=1
python3.11 scripts/cwk_tenant_cli.py doctor                   # exit 0，issue_count=0
```

### 1.11 攻击 CLI 冷启动黑盒

| 命令 | 观察到的行为 |
| --- | --- |
| `cwk_tenant_cli.py` 无子命令 | exit 4，打印 help；无 traceback |
| `cwk_tenant_cli.py foo` | exit 4（未知 command）；无 traceback |
| `cwk_tenant_cli.py show --tenant-id not-a-tenant` | exit 2（tenant_id 语法拒） |
| `cwk_tenant_cli.py show --tenant-id t_zzzzzzzzzzzzzzzzzzzzzzzzzz`（未存在） | exit 5（IO：TenantNotFound） |
| `CWK_INSTANCE_ROOT=/nonexistent cwk_tenant_cli.py init --actor a` | exit 4，`error: CWK_INSTANCE_ROOT does not exist`；无宿主机绝对路径 |
| `CWK_INSTANCE_ROOT=relative cwk_tenant_cli.py init --actor a` | exit 4，`error: CWK_INSTANCE_ROOT must be an absolute path` |
| `unset CWK_INSTANCE_ROOT; cwk_tenant_cli.py init --actor a` | exit 4，`error: CWK_INSTANCE_ROOT is not set`；绝不 fallback repo runs/state/.env |
| 恶意 CWD 提供 `cwk_tenant_cmd_core.py`（`raise RuntimeError('evil')`） | exit 0（dispatcher 忽略 CWD/PYTHONPATH，只加载 `scripts/`） |

### 1.12 Wiki smoke

- 主 worktree（`/Users/evan/.openclaw/gateways/life/state/workspace-life/projects/CWK/`）：
  `python3.11 scripts/cwk_wiki_smoke_test.py` → `overall=PASS failed=0/14`。RT-012
  未修改 `cwk_wiki_query.py` / `cwk_entity_catalog.py` / `cwk_wiki_search_index.py`，
  查询链路未变。
- 本 worktree：由于 git 分支不携带 `knowledge/工作协同镜像/wiki` 数据，smoke 
  报出 `9/13 FAIL`（全部因数据缺失，非 RT-012 回归）。

## 2. 交付文件

### 2.1 代码

- `scripts/cwk_atomic_file.py`（新增，518 行）
- `scripts/cwk_instance.py`（新增，437 行）
- `scripts/cwk_tenant_registry.py`（新增，789 行）
- `scripts/cwk_tenant_cli_api.py`（新增，141 行）
- `scripts/cwk_tenant_cli.py`（新增，244 行）
- `scripts/cwk_tenant_cmd_core.py`（新增，394 行）

### 2.2 契约

- `PR/PR-001-multitenant-knowledge-spaces/contracts/rt012/schemas/tenant_record.schema.json`
- `PR/PR-001-multitenant-knowledge-spaces/contracts/rt012/schemas/provision_receipt.schema.json`
- `PR/PR-001-multitenant-knowledge-spaces/contracts/rt012/schemas/instance_layout.schema.json`
- `PR/PR-001-multitenant-knowledge-spaces/contracts/rt012/schemas/layout_doctor_report.schema.json`
- `PR/PR-001-multitenant-knowledge-spaces/contracts/rt012/schemas/command_spec.schema.json`

### 2.3 测试

- `tests/test_rt012_atomic_file.py`（20 tests）
- `tests/test_rt012_instance.py`（26 tests）
- `tests/test_rt012_registry.py`（30 tests）
- `tests/test_rt012_cli.py`（14 tests）
- `tests/test_rt012_attacks.py`（21 tests）

### 2.4 RT 标准包

- `RT/RT-012/rt-intake.md`
- `RT/RT-012/specs/需求契约.md`
- `RT/RT-012/specs/技术方案.md`
- `RT/RT-012/tasks/开发任务.md`
- `RT/RT-012/reports/实现记录.md`
- `RT/RT-012/reports/交付验证报告.md`
- `RT/index.yaml` 追加 RT-012，状态 `implementation_done`

## 3. Scope diff（与开发计划 §RT-012 边界的对齐）

- 未新增：credential broker、canonical store、agent binding、access ledger、
  tenant view、collector、scheduler、router、projector、query broker、backup、
  release / feature flag、cron。
- 未修改：`cwk_pr001_*.py` × 4、14 张 v1 schema、`security_defaults.json`、
  `verified_shared_extensions_v1.json`、`ADR-0001`、`cwk_collect_live.py`、
  `cwk_nightly_pipeline.py`、`cwk_wiki_query.py`、`cwk_wiki_search_index.py`、
  `cwk_entity_catalog.py`、`cwk_sync_mirror_to_docdb.py`、`Makefile`、
  `install.sh`、`.env.example`、`CHANGELOG.md`、`RT/RT-001..RT-010`、`config/`、
  `docs/`、`knowledge/`。
- 未触碰：`.serena/`、`.spec-workflow/`。
- 未新增：cron / feature flag / 环境变量 / 生产开关；未读取任何真实 `CWORK_APP_KEY`；
  未接入 Cloud/DocDB/生产镜像；未引入第三方 pip 依赖（stdlib only）。

## 4. 残余 conservative_unknown

以下事实继续 `conservative_unknown`，禁止在 RT-012 内升级：

- `report_id_global_uniqueness`（RT-011）；
- `permission_authoritative_events` / `permission_authoritative_api`（RT-011）；
- `trusted_agent_identity_openclaw_tool` / `trusted_agent_identity_uds_peercred`（RT-011）；
- `sandbox_transport_openclaw_tool` / `sandbox_transport_uds`（RT-011）；
- `verified_shared_extensions_dual_user_sample`（RT-011）；
- `sandbox_transport_loopback_http_self_reported`（RT-011，政策永禁）；
- 每租户配额数值（`quota.limits.*` 全部 `null`；由 RT-024 测量、RT-026 冻结）。

## 5. 已知 minor

- CLI 默认 argparse 错误码是 2；dispatcher 强制转为 EXIT_USAGE=4 以保留“2 =
  契约错误”的 RT-011 语义，与 RT-011 CLI 保持一致的 `{0, 2, 3, 4, 5}` 基础上
  显式增加 `EXIT_CONFLICT=3` 与 `EXIT_INTERNAL=6`。
- Wiki smoke 需要本地 `knowledge/工作协同镜像/wiki/` 数据；worktree 内 smoke 报
  `FAIL`（数据缺失），主 worktree 里 PASS，与 RT-012 无关。

## 6. 独立验收前置

- 本报告不出具 PASS 结论；等待 `agent-rt012-verify` 独立黑盒验收。
- 一切验收命令与结果可复现：仅需 `PYTHON=python3.11 make test` +
  `python3.11 -m unittest discover -s tests` + `git diff --check` +
  `python3.11 scripts/cwk_tenant_cli.py --help/state-graph/init/show/list/doctor`。

# RT-012 开发任务

- [x] T1：新建 `PR/PR-001-multitenant-knowledge-spaces/contracts/rt012/schemas/` 目录并落地 5 张 JSON schema：
      `tenant_record`、`provision_receipt`、`instance_layout`、`layout_doctor_report`、`command_spec`。
      全部 `additionalProperties:false` + `unevaluatedProperties:false`；使用 RT-011 冻结的
      Draft 2020-12 subset 引擎在 registry 校验时执行。
- [x] T2：实现 `scripts/cwk_atomic_file.py`：
      - `open_dir_nofollow` 使用 `O_DIRECTORY|O_NOFOLLOW` + 事后 `fstat` 保底；
      - `mkdir_at` + `os.chmod(dir_fd=..., follow_symlinks=False)` 或回退 `fchmod`；
      - `write_atomic` 六步顺序：`O_EXCL|O_NOFOLLOW` temp → EINTR 循环 write → `os.fchmod` → `_fsync_fd` →
        anchored `os.rename(src_dir_fd, dst_dir_fd)` → `_fsync_fd(parent_fd)`；
      - `cas_write` 需在同目录 `exclusive_lock` 内使用；
      - `exclusive_lock` 用 `fcntl.flock`，进程死亡自动释放；
      - `recover_orphans` 只 unlink `.cwk-tmp-*` 前缀 orphan；
      - macOS 下优先使用 `F_FULLFSYNC` 提高持久化保障。
- [x] T3：实现 `scripts/cwk_instance.py`：
      - `resolve_instance_root()` 拒绝 unset/相对/whitespace/NUL/CR/LF/backslash/UNC/编码变体/symlink/非目录/不存在；
      - `validate_tenant_id` 复用 RT-011 `TENANT_ID_REGEX` 并叠加 `../`、`%2e`、`%2f`、`%5c`、`\x00`、
        `/`、`\` 显式拒；
      - `validate_space_id` 复用 RT-011 `SPACE_ID_REGEX`；
      - `InstanceLayout.open()` + `root_fd()` + `child_fd(name)` + `registry_fd(sub)`；
      - `TenantsRoot.list_tenant_ids()`；`TenantLayout.tenant_fd()`/`child_fd(name)`/`space_fd(sid)`；
      - `initialize()` 幂等，7 root + 15 tenant + registry 子目录；全部 0o700。
- [x] T4：实现 `scripts/cwk_tenant_registry.py`：
      - `new_tenant_id`（26 chars `[a-z0-9]`, opaque）；`new_txn_id`（`txn_[a-z0-9]{26}`）；
      - `TENANT_STATES / TENANT_ALLOWED_TRANSITIONS / TENANT_OPERATION_MATRIX / TERMINAL_STATE`；
      - `TenantRegistry.init_tenant(actor, reason)`：journal → dir → CAS record → receipt → 移除 journal → 写投影；
      - `get(tenant_id)` 校验 record + tenant_id 一致；损坏拒绝，绝不当空；
      - `bump_record_revision` / `bump_auth_epoch` 单调 CAS；bump auth 隐式 bump revision；
      - `recover()` 幂等；orphan 清理 + 未提交且未被填充的 staging 回滚；
      - `credential_ref` / `active_profile_version` 只能 `null`；`quota` scheme `cwk.rt012.quota.unset.v1`；
      - `TenantRecord.on_disk_sha256` 用于 CAS，`canonical_sha256()` 用于 receipt；
      - `_monotonic` 拒 bool 冒充 int / 负数 / 超 2^53-1。
- [x] T5：实现 `scripts/cwk_tenant_cli_api.py`：
      - `COMMAND_PROVIDER_API_VERSION = "v1"`；
      - `CommandContext` / `CommandSpec` / `DoctorFinding` / `CommandProviderV1` Protocol；
      - `CliError` + `STABLE_EXIT_CODES = (0, 2, 3, 4, 5, 6)`。
- [x] T6：实现 `scripts/cwk_tenant_cli.py`：
      - `_TRUSTED_SCRIPTS_DIR = Path(__file__).resolve().parent`；
      - `FROZEN_PROVIDER_SLOTS = ("cwk_tenant_cmd_core",)`；
      - `_load_provider` 校验：name grammar、文件存在、非 symlink、`spec_from_file_location`、
        import 无异常、`API_VERSION == "v1"`、`PROVIDER_NAME/PROVIDER_VERSION` 匹配、`list_commands` 存在；
      - `_collect_commands` 拒 duplicate command name；
      - `main` 稳定退出码；`allow_abbrev=False`；无 traceback；`_safe_path` 只保留尾部两段；
      - `_safe_env_snapshot` 只透传 `CWK_INSTANCE_ROOT`。
- [x] T7：实现 `scripts/cwk_tenant_cmd_core.py`：
      - `list_commands()` 返回 5 个 CommandSpec；
      - `init`：只创建 draft；调用前先 `layout.initialize()` + `reg.recover()`；
      - `show` / `list`：只读；
      - `doctor`：layout / 权限 / symlink / non-dir / registry-tenant / 事务残留 / schema / revision / quota；
      - `state-graph`：dump `TENANT_STATES + TENANT_ALLOWED_TRANSITIONS + TENANT_OPERATION_MATRIX + forbidden_aliases`。
- [x] T8：编写测试族：
      - `tests/test_rt012_atomic_file.py`：leaf 校验、O_NOFOLLOW、hardlink 拒读、CAS 冲突、
        fcntl.flock 非阻塞冲突与进程死亡释放、orphan recovery 幂等；
      - `tests/test_rt012_instance.py`：`CWK_INSTANCE_ROOT` 12 攻击变体、opaque tenant_id 全部拒绝矩阵、
        initialize 幂等 + 0o700、child_fd 拒绝未知/symlink；
      - `tests/test_rt012_registry.py`：六态断言、无 alias、CAS bump、损坏不当空、recover 三种情形；
      - `tests/test_rt012_cli.py`：help/no-cmd/unknown 稳定码、init→show→list、doctor 三种、
        provider ABI/symlink/缺 API_VERSION 拒绝、恶意 CWD/PYTHONPATH 忽略；
      - `tests/test_rt012_attacks.py`：RT-011 frozen files 无 marker 泄漏、
        instance root 攻击矩阵（unicode/Windows/UNC）、tenant_id 完整攻击矩阵、
        双租户隔离（bump/lock/损坏）、softlink 攻击、schema 攻击（duplicate/负数/超 2^53）、
        secret scan（CLI 输出/源码不读 AppKey）、rollback 安全性。
- [x] T9：编写标准 RT 包：
      - `rt-intake.md` / `specs/需求契约.md` / `specs/技术方案.md` / `tasks/开发任务.md` /
        `reports/实现记录.md` / `reports/交付验证报告.md`；
      - `RT/index.yaml` 追加 RT-012，status = `implementation_done`（不自称 completed）。
- [x] T10：验证：全套 RT-012 定向测试通过、RT-011 定向测试通过、`make test`（doctor+py_compile+
      unittest+smoke×3）通过、`python3.11 -m compileall -q scripts tests`、`git diff --check`、
      secret scan、真实 Wiki smoke（`cwk_wiki_smoke_test.py`）通过（RT-012 不改查询代码，只做 sanity）。
- [x] T11（2026-08-21 pre-checkpoint remediation）：修复 `CWK_INSTANCE_ROOT` 祖先 symlink
      与 post-open root/ancestor replacement：逐组件 `lstat/openat/fstat`、完整 identity chain、
      retained root anchor FD、每次访问重走比对、显式幂等 `close()`/context lifecycle；补充
      race、replacement、copy/pickle、close-after-use 攻击测试，并将 macOS 临时目录 fixture
      规范为显式 resolved canonical path。
- [x] T12：在尚未建立远端 checkpoint 前一次性重开中央 script-evolution trust root，声明
      stage-09 `RT-012 → scripts/cwk_instance.py → ordinal 1`；迁移说明与 receipt 最后生成并
      绑定最终 policy/script/note SHA 及 canonical unittest refs。此任务不等于独立验收 PASS。

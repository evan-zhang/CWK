# RT-024 开发任务：安全审计、可观测性与容量基准

- [ ] T024-01 以 `contracts/gates/gate_registry_v1.json` 为唯一索引，按
  `receipt_path` 机读 VG-B/C/D 的 `cwk.pr001.verification_gate_receipt.v1`
  receipt 并绑定：校验 schema、gate/feeder 冻结映射、`status=pass`、
  `feeder_rt_independent_pass=true`、`receipt_sha256` domain-separated 重算、
  `artifacts[].sha256`、commit/build 兼容。RT-024 只消费，不创建/补写/猜测
  receipt，不从 Markdown 推断状态。同时冻结 audit/health/benchmark schema。
- [ ] T024-01a 负向测试：`receipt_path` 缺失（NOT RUN）、schema 非法、
  `status != pass`、hash 重算不匹配、artifact 漂移时正式基准拒绝启动并返回
  稳定错误；`synthetic=true`/`conservative_unknown` 时报告显式标注限制。
  静态检查断言 RT-024 未写入任何 `gate-receipts/**/receipt.json`。
- [ ] T024-02 实现安全 append、hash chain、segment checkpoint、verify 与 crash
  recovery。
- [ ] T024-03 实现关键事件 adapters 和 read-only replay，证明不改权威 SoR。
- [ ] T024-04 实现 metrics allowlist、tenant health 与 tenant/admin 授权边界。
- [ ] T024-05 实现有界异步 exporter spool、drop/backlog 指标及故障注入。
- [ ] T024-06 建立日志/审计/metrics forbidden corpus 与自动扫描器。
- [ ] T024-07 实现确定性 10×10,000 合成 fixture 和 production-root 拒绝。
- [ ] T024-08 实现 benchmark runner，分 core/E2E、warm/cold、disposition、并发
  和撤权阶段输出样本。
- [ ] T024-09 实现报告聚合与独立 percentile 重算；禁止默认目标数字。
- [ ] T024-10 运行篡改/串扰/日志注入/exporter 故障/满盘/恢复黑盒测试。
- [ ] T024-11 运行正式基准、全库回归、Wiki smoke、secret scan、回滚；归档
  实际环境与 receipt。
- [ ] T024-12 由独立 Agent 复算报告和攻击审计/指标边界，明确 PASS/FAIL。

- [ ] T024-13 **RT-024 安全门 receipt（SG registry 声明的生产任务）。**
  由本 RT 的独立验收 Agent 在 `rt_independent_acceptance` 阶段按
  `cwk.pr001.security_gate_receipt.v1` 生成
  `PR/PR-001-multitenant-knowledge-spaces/security-receipts/RT-024/receipt.json`，
  `receipt_kind = rt-security`，consumer 恰为 `RT-026`（go/no-go）与 `G6`（final acceptance）。
  - `claims` 必须与 registry 中 RT-024 的集合逐字相等：`SGC-024-01`/SG-03、`SGC-024-02`/SG-04、`SGC-024-03`/SG-06、`SGC-024-04`/SG-08。
  - `filesystem_policy.mode = applicable`，因此**没有任何 claim 可以标 N/A**；
    SG-03 覆盖必须绑定本 RT 的真实运行面与可执行用例引用，逐类给出
    `path_traversal`、`symlink_component`、`symlink_leaf`、`hardlink`、`toctou`、`special_file` 六类证据，泛泛散文不算覆盖。
  - `independent_security_verification_pass` 是独立安全/预检结论，不等价于
    RT 最终 PASS；`verifier` 必须存在且不等于 `producer`；
    `tests_run > 0`、`tests_failed = 0`、`artifacts` 非空且逐条 sha256 匹配；
    `tested_subject_commit` 为被测候选 commit（evidence-only commit 的祖先）。
  - 未生成该 receipt 时，RT-024 在 SG 视角下为 NOT_RUN，RT-026 只能给 NO_GO。

## 完成定义

只有真实 baseline 非空、VG-B/C/D 的机读 receipt 全部验证通过、AC-024-01～09
全部可复现且独立 PASS 才 completed。没有测量不得填数字，测得数字也不自动成为
业务 SLA；`synthetic` receipt 支撑的结论不得当作生产可用。

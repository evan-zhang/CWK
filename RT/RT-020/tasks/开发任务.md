# RT-020 开发任务：Holdout 预览、Router 与决策日志

> 状态：planned；所有任务未完成。

## A. 契约

- [ ] A-01：在 `contracts/rt020/schemas/` 新增 route_decision.v2、preview **result set**、activation receipt、route diff、review/archive、projection request schemas。
- [ ] A-01b：以 consumer 身份引入 RT-019 冻结的 `PreviewProviderV1` ABI 与 profile preview receipt schema，并加断言测试锁定其 schema 文件 SHA；**禁止**在 rt020 命名空间重定义同名 preview receipt。
- [ ] A-02：实现 schema+semantic validator；v1 保持不变，冻结 v1→v2 fail-to-review migration。
- [ ] A-03：实现并固定 LP/domain-separated route/projection key helper与跨RT测试向量。
- [ ] A-04：零漂移消费 shared `PilotAdmissionProviderV1`，构造绑定
  `purpose="profile_workflow"`；不得实现 production adapter。

## B. Preview 与 activation

- [ ] B-01：实现联合 manifest holdout-only reader、样本泄漏检查与ACL双检。
- [ ] B-02：实现 per-item preview、完整 result set，并按 RT-019 冻结 schema 填写 PreviewReceipt；失败项不得被过滤。
- [ ] B-03：实现 user confirmation + ActivationReceipt + Profile pointer 原子事务/recovery。
- [ ] B-03a：对 `pilot` 在 preview/result-set 发布前和 activation commit 前重验 admission；
  `profile_pending/active` 0 次，失败零半激活。

## C. Router 与 log

- [ ] C-01：实现 explicit/confirmed entity/structured/AI engines和固定优先级combiner。
- [ ] C-02：实现AI zero-tool classifier；conflict/low-confidence/failure统一review。
- [ ] C-03：实现immutable decision/current pointer/diff/review/archive及crash-safe journal。
- [ ] C-04：实现reroute triggers、Projection/Cleanup request ABI与Profile rollback replay。
- [ ] C-04a：对 `pilot` 在 decision/diff/projection-request 持久化前重验 admission。

## D. 测试/交付

- [ ] D-01：holdout leak/receipt tamper/expiry/partial failure与激活绑定矩阵。
- [ ] D-02：路由优先级、多空间、archive/review、cross-field、AI攻击与ACL race。
- [ ] D-03：key boundary collision、duplicate/reordered events、CAS/crash/recovery、双tenant隔离。
- [ ] D-04：RT-011～020、全量unittest、Wiki lint/smoke、secret/path scan。
- [ ] D-04a：PilotAdmission deny/unavailable/expiry/revision 回退/二次漂移/目的错配矩阵，
  central runtime/schema/test SHA 零漂移。
- [ ] D-05：实现记录与全新Agent独立验收；PASS后才进入RT-021。
- [ ] D-06 **RT-020 安全门 receipt（SG registry 声明的生产任务）。**
  由本 RT 的独立验收 Agent 在 `rt_independent_acceptance` 阶段按
  `cwk.pr001.security_gate_receipt.v1` 生成
  `PR/PR-001-multitenant-knowledge-spaces/security-receipts/RT-020/receipt.json`，
  `receipt_kind = rt-security`，consumer 恰为 `RT-026`（go/no-go）与 `G6`（final acceptance）。
  - `claims` 必须与 registry 中 RT-020 的集合逐字相等：`SGC-020-01`/SG-03。
  - `filesystem_policy.mode = applicable`，因此**没有任何 claim 可以标 N/A**；
    SG-03 覆盖必须绑定本 RT 的真实运行面与可执行用例引用，逐类给出
    `path_traversal`、`symlink_component`、`symlink_leaf`、`hardlink`、`toctou`、`special_file` 六类证据，泛泛散文不算覆盖。
  - `independent_security_verification_pass` 是独立安全/预检结论，不等价于
    RT 最终 PASS；`verifier` 必须存在且不等于 `producer`；
    `tests_run > 0`、`tests_failed = 0`、`artifacts` 非空且逐条 sha256 匹配；
    `tested_subject_commit` 为被测候选 commit（evidence-only commit 的祖先）。
  - 未生成该 receipt 时，RT-020 在 SG 视角下为 NOT_RUN，RT-026 只能给 NO_GO。

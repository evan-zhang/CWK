# RT-046 收口复盘

## 结果

格式工厂升级为转换器表并支持无后缀文件嗅探，投前库 viewer shell 类文件可通过全文接口重新取得正文；生产验收提交为 `8ed8a52`。

## 验证

- 转换器选择、降级 placeholder、空文本失败、magic-byte 嗅探和三链回执均有测试。
- 生产样本重新摄取后可由网关读取并形成实时引文。
- `receipts/acceptance-20260905.json` 保存收口证据。

## 教训

后缀和首次下载结果都不能直接等同于真实文档类型；遇到 viewer shell 必须识别并走全文获取路径，同时保留确定性回执。

## 遗留

图片视觉识别保持结构位，不属于 v1.1 关闭条件。

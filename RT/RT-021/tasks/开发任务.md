# RT-021 开发任务：多知识空间 Projector 与索引

> 状态：planned；无已完成任务。

## A. Space contracts/registry

- [ ] A-01：新增SpaceRecord/change/slug tombstone/membership/removal/projection/index/cleanup/rebuild schemas与semantic validators。
- [ ] A-02：实现可信actor/profile activation创建、CSPRNG opaque ID、slug grammar/unique/tombstone/reserved、32上限、no nesting。
- [ ] A-03：实现active↔disabled、rename、multi-space activation all-or-nothing与crash recovery。

## B. Projector/Index

- [ ] B-01：实现RouteDecision v2 consumer、membership/current/history、RT-020 projection_key复用。
- [ ] B-02：实现summary/topic/entity/search/views projection与artifact/manifest完整性。
- [ ] B-03：适配`cwk_entity_catalog.py`/`cwk_wiki_search_index.py`，多租户不fallback全局entity registry。
- [ ] B-03a：只为上述两个预声明脚本追加各自 evolution receipt/migration note；不得修改中央 policy/schema/helper/guard 或旧 SHA 表。
- [ ] B-04：实现archive/review最小views，默认无membership/index/日报。
- [ ] B-05：实现ProjectorJobProviderV1，不做生产注册。
- [ ] B-06：实现 `cwk_space_snapshot_adapter.py`——RT-022 冻结的
  `ProfileSpaceSnapshotProviderV1` 生产 adapter。零漂移复用 RT-022 的 Protocol 与
  `cwk.profile_space_snapshot.v1` schema 文件（断言 schema 文件 SHA 与 RT-022 一致，
  不在 `contracts/rt021/schemas/` 重定义同名 schema）；一次性返回 profile
  SHA/版本、default/queryable active space IDs、membership/index versions，
  0 space 时仍带非空 `profile_sha256`；纯只读，不创建/修改任何 space 或 Profile。
- [ ] B-06a：adapter 正反例测试——0 space、Profile pointer 读取中途切换、
  space disable/rename 竞态、`default ⊆ queryable` 与键集合精确相等断言、
  返回值 deep-forbidden（无 slug/标题/正文/路径/credential/他租户 ID）、
  裸 `tenant_id=` kwarg 拒绝；并用 RT-022 的 fake-provider 契约测试做互换验证。
- [ ] B-07：构造注入 `PilotAdmissionProviderV1(purpose="profile_workflow")`；`pilot`
  在 staging 前与 membership/projection/index current publish 前各重验，`active` 0 次。

## C. Cleanup/Rebuild

- [ ] C-01：实现revoke `space_index/review_queue/archive_metadata` consumers、分阶段receipt与safe ack。
- [ ] C-02：实现route退出/Profile rollback/space disable cleanup与re-enable rebuild。
- [ ] C-03：实现new-generation deterministic full rebuild与atomic pointer。

## D. 测试/交付

- [ ] D-01：space主体/slug/state/cap/nesting/rename tombstone全部正常与恶意测试。
- [ ] D-02：同report多space同canonical、无raw副本、projection完整性/失败/rebuild。
- [ ] D-03：revoke/route/rollback/disable crash/retry/reorder/early ack与双tenant隔离。
- [ ] D-04：RT-007/008/010、Wiki lint/smoke、RT-011～021、全量unittest、secret/path/raw-copy scan。
- [ ] D-04a：PilotAdmission deny/unavailable/expiry/revision 回退/二次漂移矩阵、零半
  pointer，及 central runtime/schema/test SHA 零漂移。
- [ ] D-05：实现记录与全新Agent独立验收；PASS后另行执行VG-C。
- [ ] D-06 **RT-021 安全门 receipt（SG registry 声明的生产任务）。**
  由本 RT 的独立验收 Agent 在 `rt_independent_acceptance` 阶段按
  `cwk.pr001.security_gate_receipt.v1` 生成
  `PR/PR-001-multitenant-knowledge-spaces/security-receipts/RT-021/receipt.json`，
  `receipt_kind = rt-security`，consumer 恰为 `RT-026`（go/no-go）与 `G6`（final acceptance）。
  - `claims` 必须与 registry 中 RT-021 的集合逐字相等：`SGC-021-01`/SG-03、`SGC-021-02`/SG-06。
  - `filesystem_policy.mode = applicable`，因此**没有任何 claim 可以标 N/A**；
    SG-03 覆盖必须绑定本 RT 的真实运行面与可执行用例引用，逐类给出
    `path_traversal`、`symlink_component`、`symlink_leaf`、`hardlink`、`toctou`、`special_file` 六类证据，泛泛散文不算覆盖。
  - `independent_security_verification_pass` 是独立安全/预检结论，不等价于
    RT 最终 PASS；`verifier` 必须存在且不等于 `producer`；
    `tests_run > 0`、`tests_failed = 0`、`artifacts` 非空且逐条 sha256 匹配；
    `tested_subject_commit` 为被测候选 commit（evidence-only commit 的祖先）。
  - 未生成该 receipt 时，RT-021 在 SG 视角下为 NOT_RUN，RT-026 只能给 NO_GO。

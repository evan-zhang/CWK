# 开发任务

- [x] T1：新增 `scripts/cwk_entity_catalog.py`，从 `wiki/summaries/*.md`
  的 `候选实体` 段落抽取 surfaces、应用同 type 家族规则、加载并应用
  **`config/entity-family-registry.json`**（仓库内数据文件，
  `wiki/_system/entity-family-registry.json` 仅为测试覆盖点）显式跨
  type 合并、使用自研 Aho-Corasick 扫描 raw 精确锚点（按 `raw_sha256`
  幂等缓存到 `wiki/_system/entity-anchors-cache.json`），最后写
  `entity-catalog.json(.gz)` + `entity-catalog-meta.json`。
- [x] T2：在 `scripts/cwk_wiki_search_index.py` 的 `build_index` 中
  调用 catalog builder，把 `catalog_sha256 / catalog_schema /
  catalog_registry_version` 绑定到索引 payload，并将稳定逻辑
  `catalog_registry_source`（如 `repo:...`）写入索引 payload、
  `index-meta.json` 与 `manifest.json` 三处；catalog 以 registry
  version/applied 决策表达授权语义，不重复保存 source 路径。
  **Local-First**：`entity-catalog*.json*`、
  `entity-anchors-cache.json`、`wiki/_system/entity-family-registry.
  json` 均为本地派生 / 覆盖，**不**进入 `changed_relative_paths`、
  **不**通过 `cwk_sync_mirror_to_docdb.iter_items` 上云；本地索引快照
  校验依赖 manifest 上的 `entity_catalog_*` 字段检测 drift，字段本身
  仅作构建审计，不授权 cloud/shadow 查询；查询运行时以 payload 声明的
  catalog SHA 为权威门禁。
- [x] T3：在 `scripts/cwk_wiki_query.py` 中新增 catalog 加载与解析器
  `resolve_entity`，实现 `unscoped / resolved / ambiguous /
  unsupported_multi_entity / unknown / resolved_empty` 六态。
- [x] T4：`resolved` 分支执行 scope 硬过滤、剔除实体 token、禁用
  `nav_bonus`、附加 `results[].scope_support` 与顶层
  `entity_resolution / scope / intents / support` 字段。
- [x] T5：`ambiguous / unknown / resolved_empty / filters_removed_all
  / no_query_evidence_in_scope` 分支统一返回结构化安全空结果；
  `global_fallback_used = false`。
- [x] T6：管理意图检测（进展/规划/风险/负责/next plan/owner/下一步/
  进度）与逐意图支持度评估；`confidence` 三支联合决策；高置信度
  还要求 `resolution_kind ∈ {exact, approved_alias}`、`source_
  integrity=verified`、`scope_integrity=verified`、`query_support=
  verified`。
- [x] T7：目录/索引 hash 一致性校验；新增 CLI `--require-catalog`；
  bound snapshot 的 catalog 缺失、不可读、内部 schema/hash 校验失败或
  与 payload 声明的 SHA 不一致时
  无条件 fail-closed；ASCII/NFKC 与 `>=2` 字 CJK 实体形态不得回落全库。
- [x] T8：新增 `tests/test_wiki_entity_scope.py` 覆盖 property/synthetic
  用例（>30 postings、单条 postings、陈旧页面被忽略、括号缩写、
  规范化变体、同名跨类型无注册表 → ambiguous、有注册表 → resolved、
  未知实体、`resolved_empty(no_indexed_reports)`、
  `filters_removed_all`、off-scope 高分干扰、raw 精确锚点（含边界
  拒绝）、目录/索引不一致 fail-closed、Aho-Corasick 与朴素扫描
  一致性、意图顺序/标点稳定性、registry evidence/members 校验、
  真实镜像回归）。
- [x] T8a：**RT-010 release blocker 修复的测试**——
  `test_verified_intent_bucket_beats_verified_no_intent_distractor`
  覆盖三桶排序 + bucket_3 被丢弃 + `dropped_unverified_rows` 上报；
  `test_no_verified_evidence_in_scope_becomes_resolved_empty` 覆盖
  all-unverified fail-closed；`test_top_k_masking_bounded_pool_
  rescues_late_verified_row` 覆盖有界评估池防遮蔽。
- [x] T8b：覆盖 Local-First 与审计不变量：catalog/meta/cache/镜像 registry
  覆盖不进入 `changed_relative_paths` 或 DocDB sync；cold/warm build
  semantic hash 一致；registry source 在 index payload/meta/manifest 三处
  审计绑定；每条 approved posting
  edge 携带审批 provenance；最终 Top-K 重新计算意图与 confidence。
- [x] T9：在本地真实镜像的**隔离副本**上跑重建 + TBS 变体查询审计，验证 scope
  100% 精度、复合意图不误判高、`resolved_empty` 场景正常，主要
  查询（`TBS 项目进展`、`TBS 项目进展、下一步计划、风险`）待最终
  代码冻结后复验；不得用暴露过的 TBS 用例证明总体准确率。
- [x] T10：最终代码冻结后执行
  `python3.11 -m unittest discover -s tests -q`、实体专项、真实镜像只读测试、
  Wiki smoke、`compileall`、
  `git diff --check`；最终数量与性能回执在执行后写入交付报告，本任务
  文档不预填数字。
- [x] T11：撰写 `RT/RT-010/reports/交付验证报告.md`。
- [ ] T12：独立审查无 blocker 后再合并；合并后在目标分支复跑 T10，
  保持 cloud/shadow/cron/DocDB 与生产 mirror 不变。

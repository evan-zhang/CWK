# RT-017 开发任务：Per-tenant Collector Worker

> 状态：planned；所有复选框保持未完成，直到有对应代码与可复核 receipt。

## A. 契约与兼容修订

- [ ] A-01：新增 RT-017 schema：tenant event evidence、event review、stage/purge receipt、collector run request/manifest/recovery、authority capability result。
- [ ] A-02：为所有 schema 增加 `additionalProperties:false`、deep forbidden 与 Python 语义 validator；冻结 domain-separated hash 公式。
- [ ] A-03：Stage-01 compatibility commit 为 RT-015 增加生产 `authority_adapter` 注入；默认仍 fail closed，禁止复用 test sentinel。A-03～A-04e 必须同一 evolution stage 完成。
- [ ] A-04：在同一 Stage-01 新增 cleanup outbox v2 + migration note，增加 `staged_event` consumer；v1 不改，reader 双版本兼容。
- [ ] A-04b：在同一 `cwk_access_ledger.py` evolution stage 内新增版本化 snapshot-only 公共
  只读 ABI `list_profile_eligible(*, snapshot)` 与 `cwk.profile_eligible_record.v1`
  （字段/禁止字段见 `specs/技术方案.md` §2.1）；与 `list_query_eligible` 共用同一 grant
  校验 helper，仅在允许的 tenant 状态集分叉。附 migration note。
- [ ] A-04c：为 A-04b 增加正反例测试：`{profile_pending, pilot, active}` 放行；
  `draft/suspended/offboarded` 与跨 tenant snapshot 统一 `AccessDenied`；过期 lease /
  epoch 漂移 / revoked grant 被排除；裸 `tenant_id=` kwarg 抛 `TypeError`；返回字段集合
  精确断言且 secret/path scan 为 0。**并断言 `list_query_eligible` 在 `profile_pending`
  下仍拒绝**（证明 query eligibility 未被放宽）。
- [ ] A-04d：在同一 Stage-01 注入 `CanonicalVersionProviderV1`；冻结五字段 snapshot，
  证明 current SHA 只来自点查并经 RT-014 public `read_version` 验证，拒绝 private
  catalog、枚举、grant/view 推断和全部路径/完整性攻击。
- [ ] A-04e：在同一 Stage-01 注入 constructor-bound
  `PilotAdmissionProviderV1(purpose="profile_workflow")`；仅 `pilot` 强制 admitted，
  `profile_pending/active` 无额外调用；覆盖 deny/unavailable/expiry/revision 回退。
- [ ] A-05：先跑 RT-015 定向回归和兼容黑盒验收；失败不得继续实现绕过。
- [ ] A-06：仅为 `cwk_access_ledger.py`、`cwk_collect_live.py` 追加 policy v1 预声明的 evolution receipt 与 migration note；不得修改中央 policy/schema/helper/guard 或旧 SHA 表。
- [ ] A-07：**RT-017 是 `cwork-authority-source` 能力激活 receipt 的唯一生产者。**
  在 E-02 的独立验收 PASS 之后、且仅当
  `contracts/gates/synthetic_closure_map_v1.json` 中该能力的全部
  `production_conditions` 均已被真实（非 mock、非 sentinel、非 conservative_unknown）
  证据满足时，按 `cwk.pr001.capability_activation_receipt.v1` schema 生成
  `PR/PR-001-multitenant-knowledge-spaces/capability-receipts/cwork-authority-source/receipt.json`。
  - 生成时机：RT-017 独立验收 PASS → 本 receipt → RT-026；**VG-B 是 RT-026 的同级
    独立输入，既不是本 receipt 的前置，也不得作为其证据角色**（无环）。
  - `evidence_refs` 必须精确覆盖 5 个角色：`authority_snapshot_api_probe`、
    `revocation_event_replay`、`ab_visibility_isolation`、
    `real_credential_header_only_scan`、`independent_acceptance_report`；
    每条为仓库相对普通文件并附匹配 sha256。
  - `verifier` 必须是 E-02 的独立验收 Agent，且**不得等于**实现 Agent
    （`producer`）；`closes_gate_ids` 恰为闭包图为该能力映射的 gate 集合；
    `consumers` 恰为 `["RT-026"]`（schema 中是数组，没有 `consumer_rt` 标量）。
  - `tested_subject_commit` 绑定被测候选 commit（该 receipt 所在 evidence-only
    commit 的祖先），并与本 receipt 自身的 evidence/artifacts 一致。
  - `expires_at - created_at` 必须落在 `0 < Δ ≤ 90 天`（7776000s，闭区间上界）。
    到期后不得原地覆盖：按 `activation_history_rule` 先归档
    `capability-receipts/cwork-authority-source/archive/<旧 receipt sha256>.json`，
    再以新的真实探测结果续签（`sequence` 递增、`previous_receipt_sha256` 链接）。
  - **缺失、未知或仅部分验证时，本任务的正确产出是"完全不生成 receipt"**；
    严禁把 `conservative_unknown` / blocked 结果写成 `capability_activated`。

## B. Source、Authority 与 Worker

- [ ] B-01：实现只读 `CWorkSourceV1`，严格分页、limits、response allowlist 与无 mutation verb 测试。
- [ ] B-02：实现 authority capability probe、`AuthorityReceiptProviderV1`、`CWorkAuthorityAdapter`；未知结果明确 `conservative_unknown`。
- [ ] B-03：实现 `TenantCollectorWorker.collect_once/recover`、tenant state/run/retry/lock 与 cursor 最后提交。
- [ ] B-04：拆分并校验四路 envelope；只调用 RT-014/015 公共 API。
- [ ] B-05：改造 `cwk_collect_live.py` 的显式多租户入口并保留默认 legacy façade。
- [ ] B-06：实现 `cwk_collection_state.py` tenant adapter，证明无仓库级多租户状态回退。
- [ ] B-07：Worker 构造注入 `purpose=collector_run`，对 `pilot` 在启动与发布前各重验；
  两次 snapshot 漂移或第二次 deny 时整次 run 零部分输出。

## C. Staged event evidence

- [ ] C-01：实现 secret-backed opaque event key、body/event hash 与 immutable version store。
- [ ] C-02：实现 HTML/URL/token/unknown/oversize review 路径；正文不进 review/manifest/error。
- [ ] C-03：实现 cleanup outbox v2 consumer、crash-safe purge receipt 和成功后 ack。
- [ ] C-04：证明本 RT 没有供 Router/AI/Index/Query 读取 staged body 的公开 API。

## D. 测试

- [ ] D-01：双 tenant 同/不同 report、overlay-only diff、event diff、正文/event 版本升级与 canonical 去重。
- [ ] D-02：authority verified/unknown/rejected/expired/replay/wrong-binding 与 lease refresh。
- [ ] D-03：bounded list 缺项不 revoke；in-flight revoke 先 deny 后清理；重跑不复活。
- [ ] D-04：429/timeout/bad JSON/disk full/lock/crash/restart 单 tenant 故障隔离。
- [ ] D-05：argv/log/manifest/temp/error secret scan；路径、symlink、hardlink、TOCTOU、Unicode、prompt/log injection。
- [ ] D-06：RT-011～017 定向测试、全量 unittest、Wiki lint/smoke、legacy façade 回归。
- [ ] D-06a：复跑 Wave-0 PilotAdmission 与 CanonicalVersionProvider contract tests，并
  断言三件工件各自 SHA 零漂移；验证 Stage-01 不可拆分、不能二次演进。
- [ ] D-07：**RT-017 安全门 receipt（SG registry 声明的生产任务）。**
  由 E-02 的独立验收 Agent 在 `rt_independent_acceptance` 阶段按
  `cwk.pr001.security_gate_receipt.v1` 生成
  `PR/PR-001-multitenant-knowledge-spaces/security-receipts/RT-017/receipt.json`，
  `receipt_kind = rt-security`，consumer 恰为 `RT-026`（go/no-go）与 `G6`（final acceptance）。
  - `claims` 必须与 registry 中 RT-017 的集合逐字相等：
    `SGC-017-01/SG-00`、`SGC-017-02/SG-01`、`SGC-017-03/SG-03`、
    `SGC-017-04/SG-05`、`SGC-017-05/SG-06`、`SGC-017-06/SG-08`。
    RT-017 的 `filesystem_policy.mode = applicable`，因此**没有任何 claim 可以标 N/A**。
  - SG-03 覆盖必须绑定 D-05 的真实运行面与可执行用例引用，逐类给出
    `path_traversal`、`symlink_component`、`symlink_leaf`、`hardlink`、`toctou`、
    `special_file` 六类证据；泛泛的散文描述不算覆盖。
  - `independent_security_verification_pass` 为独立安全/预检结论，不等价于
    RT 最终 PASS；`verifier` 必须存在且不等于 `producer`；
    `tests_run > 0`、`tests_failed = 0`、`artifacts` 非空且逐条 sha256 匹配。
  - 未生成该 receipt 时，RT-017 在 SG 视角下为 NOT_RUN，RT-026 只能给 NO_GO。

## E. 交付

- [ ] E-01：实现 Agent 填写实现记录与自测证据，但不得填写独立验收结论。
- [ ] E-02：全新验收 Agent 依据需求逐条执行黑盒正常/异常/恶意测试并出具 PASS/FAIL。
- [ ] E-03：仅在独立 PASS 后更新交付验证报告与 RT 状态；仍需把真实 authority unknown 单独列为生产 blocker。
- [ ] E-04：确认无真实密钥、网络写、cron、nightly、tenant enable 或部署变更。

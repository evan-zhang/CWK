# U1 任务包 — fixture：出口判据缺溯源声明（G113 事故原型）

> 冻结件（fixture 数据，复刻 RT-125 unit-U6 判据③整条消失且无人声明的事故形态，非真实任务包）。

## 1. 任务

把 `guide.md:12` 的死命令表述替换为指向真实脚本的表述，被删除的旧判据须在下表如实登记。

## 2. 硬约束

1. 就地替换，行数不变。

## 3. 出口判据

1. `grep -rn "aodw-skill guard" docs/` → **0**（改前 3）
2. pytest 基线一致

（本包故意不写溯源声明行——G113 应在此报 WARN）
